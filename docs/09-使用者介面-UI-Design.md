# 09 使用者介面设计

**版本**：v0.1.4
**日期**：2026-08-09
**状态**：✅ 已审查批准（完整性 + 上游一致性 + 铁律落实 + E2E 覆盖四项通过）
**上游**：[02-PRD v0.1.4 §2/§3](./02-PRD-产品需求-Product-Requirements.md)、[03-Architecture v0.1.3 §6](./03-架构设计-Architecture-Design.md)、[06-API v0.1.6 §3](./06-API契约-API-Contracts.md)、[07-Workflow v0.1.2 §2-§5](./07-工作流-Workflow.md)、[08-Test v0.1.4 §6/§7](./08-测试验收-Test-Plan.md)
**下游**：04-Todo
**架构依据**：pi-desktop 五件骨架（Apache-2.0，搬运改名）+ 学习场景业务化

---

## 1. 概述

### 1.1 设计目标

pi-studybuddy 的 UI 是学生每天打开的桌面学习工作台。它必须做到三件事：

1. **打开即进入对话**：学生打开应用默认进入"💬 对话"，可直接问答；需要结构化学习动作时再进入 S1 每日首页及 S2-S7 工作台，不需要在菜单里找对话入口
2. **朗读随时可击发**：任何有文字内容的位置都有朗读按钮（TTS 跨子系统，02-PRD §3.9）
3. **隐私边界可视**：家长报告界面只展示脱敏聚合，学生侧也看不到完整 UUID（02-PRD §5.2）

### 1.2 设计原则（铁律）

1. **骨架稳定、业务可演化**：pi-desktop 五件骨架组件直接搬运改名（AppShell / SessionSidebar / FileExplorer / MarkdownBody 等），业务 UI 在其上自建（03-Architecture §6.1）
2. **学习场景优先**：会话语义从 coding agent 对话改为学习对话——附加学科标签/学习目标/错题关联（03-Architecture §6.7）
3. **规则优先 UI 不撒谎**：每日首页/速背卡/冲刺计划由规则聚合，UI 不展示 AI 幻觉为事实
4. **防泄露先于交互**：作答前 DTO 不含正确答案；家长报告界面不含原文/题干/答案/作答/错因/完整 UUID
5. **TTS 无处不在**：每个 Markdown/纯文本内容区都内嵌朗读入口，全局朗读控制条常驻
6. **单机桌面交互**：不模拟 Web 多标签页浏览器行为；文件经 `@` 引用进入对话
7. **中文优先**：所有界面文案中文；错误消息中文可操作（06-API §2.3）

### 1.3 技术栈

**借鉴 pi-desktop**（03-Architecture §6.2）：

| 技术 | 用途 | 来源 |
|---|---|---|
| React 19 + Vite | renderer 框架 | pi-desktop 直接复用 |
| react-markdown + remark-gfm + remark-math + rehype-katex | Markdown 渲染 + 公式 | pi-desktop 直接复用 |
| rehype-raw + rehype-sanitize | HTML 安全渲染 | pi-desktop 直接复用 |
| SyntaxHighlighter | 代码高亮 | pi-desktop 直接复用 |
| mermaid | 图表渲染 | pi-desktop 直接复用 |
| mammoth | docx 预览 | pi-desktop 直接复用 |
| zustand（或 Context） | 状态管理 | pi-studybuddy 自选（学习状态较复杂） |

---

## 2. 整体布局

### 2.1 主窗口三栏结构

```
┌──────────────────────────────────────────────────────────────────────┐
│  标题栏（学期名 / 课程名 / 窗口控制）                                    │
├────────┬───────────────────────────────────────────────┬─────────────┤
│        │                                   ┌─────────┐ │             │
│ 左侧栏  │         主内容区                   │ 朗读    │ │  右侧面板    │
│ (导航)  │  (学习工作台 / 对话 / 文件预览)      │ 控制条  │ │ (上下文)    │
│        │                                   └─────────┘ │             │
│        │                                                               │
│        │  ┌─────────────────────────────────────────────────────────┐ │
│        │  │  输入区（@文件引用 / 学科标签 / 发送）                      │ │
│        │  └─────────────────────────────────────────────────────────┘ │
├────────┴───────────────────────────────────────────────┴─────────────┤
│  状态栏（模型 / 备份状态 / 调度提醒 / TTS 引擎）                          │
└──────────────────────────────────────────────────────────────────────┘
```

### 2.2 布局组件映射（pi-desktop 骨架）

| 区域 | pi-desktop 组件 | pi-studybuddy 落点 |
|---|---|---|
| 应用壳 | `AppShell.tsx` | 直接复用，业务化标题栏 |
| 左侧栏 | `SessionSidebar.tsx` | 改造为"学期/课程/会话"三级导航 |
| 主内容区 | `ChatWindow.tsx` + `TabBar.tsx` | **对话 Tab（默认）+ 学习工作台 Tab** |
| 右侧面板 | `SessionProfiler.tsx` + `FileViewer.tsx` | 改造为"学习上下文"（当前课程/目标/薄弱点） |
| 文件浏览 | `FileExplorer.tsx` | 直接复用（lazy 加载 + @引用） |
| Markdown 预览 | `MarkdownBody.tsx` | 直接复用 + 内嵌朗读按钮 |
| **输入区** | `ChatInput.tsx` | **核心组件，pi 原生 AI 对话能力的 UI 承载**（见 §4.2） |
| 状态栏 | 新增 | 模型/备份/调度/TTS 状态 |

### 2.3 省略的 pi-desktop 组件（03-Architecture §6.8）

| 组件 | 处置 |
|---|---|
| `PluginsConfig.tsx` | **省略**（不让学生接触 pi 插件机制） |
| `SkillsConfig.tsx` | **替换为学习技能包管理**（学科/章节/题型模板语义） |
| `BranchNavigator.tsx` | **省略**（Git 分支对学生无意义） |
| `ChatMinimap.tsx` | **省略**（学习对话不需要代码导航小地图） |
| `browser/` 目录 | **视需要**（TRD §7 决策 2，学生端以本机内容为主） |
| `channels/` 目录 | **省略**（飞书/微信渠道删除，推送改为桌面通知） |

---

## 3. 左侧栏：学习导航

### 3.1 三级导航结构

```
┌──────────────────┐
│  📚 pi-studybuddy │
├──────────────────┤
│  学期             │
│  ├ 2026 秋 (active)│
│  │  ├ 高等数学     │
│  │  ├ 大学英语     │
│  │  └ 数据结构     │
│  └ 2026 春 (archived)│
├──────────────────┤
│  会话             │
│  ├ 今天            │
│  │  ├ 高数-极限练习  │
│  │  └ 英语-听力笔记  │
│  ├ 昨天            │
│  └ 本周            │
├──────────────────┤
│  ⚙ 设置           │
└──────────────────┘
```

### 3.2 学期/课程树

**数据来源**：`global.db:semesters` + `semester.db:course_instances`

**交互**：
- 点击学期 → 展开课程列表
- 点击课程 → 主区域切换为该课程的工作台
- 学期状态标识：`active`（绿点）/ `teaching_ended`（黄点）/ `follow_up`（蓝点）/ `archived`（灰点，只读）
- 右键课程 → 备份此课程 / 归档学期 / 重命名

### 3.3 会话列表（借鉴 pi-desktop SessionSidebar）

**借鉴**：pi-desktop `SessionSidebar.tsx`（按日期分组、模糊搜索、unread 计数）

**业务化**：
- 会话附加**学科标签**（颜色标识：数学=蓝/英语=红/...）
- 会话附加**学习目标**（如"极限练习"/"听力笔记"）
- 会话可关联**错题 ID**（从 S4 跳转）
- 模糊搜索支持学科+目标+内容

**API**：`sessions.list`（06-API §3.1）

---

## 4. 标签页结构（对话 + 学习工作台）

### 4.1 标签页总览

主内容区顶部 TabBar（借鉴 pi-desktop `TabBar.tsx`）：

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 💬 对话（默认）│ 📊 首页 │ 📁 资料 │ 📝 笔记 │ ✏️ 练习 │ ❌ 错题 │ 🎯 冲刺 │ 📋 报告 │ 🎤 采集 │
└─────────────────────────────────────────────────────────────────────────┘
```

| Tab | 角色 | 触发条件 | 主要 RPC |
|---|---|---|---|
| **💬 对话** | **pi 原生 AI 对话**（默认打开，零碎问答的常驻入口） | 应用启动即默认 | `agent.events` 流式 + pi 原生工具 |
| 📊 首页 | S1 每日简报 | 切换到该 Tab | `studybuddy_daily_brief` |
| 📁 资料 | S2 资料上传与管理 | 选课程后 | `materials.*` |
| 📝 笔记 | S2 笔记预览/导图 | 选资料后 | `notes.*` / `notes.getMindMap` |
| ✏️ 练习 | S3 限时练习 | 选模块后 | `practice.*` |
| ❌ 错题 | S4 错题改错 | S3 批改后有错题 | `mistakes.*` / `weakPoints.*` |
| 🎯 冲刺 | S5 期末冲刺 | confirmed 考试+距考≤N天 | `mockExams.*` / `cramCards.*` / `cramPlan.*` |
| 📋 报告 | S6 家长报告（学生侧） | 学生触发/调度自动 | `reports.*` / `deliveries.*` |
| 🎤 采集 | S7 课堂采集 | 学生选课程后 | `classCapture.*` |

**设计意图**（响应用户反馈）：
- pi 本身就是 AI coding agent，**天生自带对话能力**——这是 pi-studybuddy 作为"专属 studybuddy"的根基，不能废弃
- 学生在"💬 对话"里随时与 AI 自由问答（"帮我理解极限的 ε-δ 定义"/"这道题怎么做"/"导数和积分的关系"），不必每次走结构化的 S1-S7 标签页
- S1-S7 标签页是"结构化学习工具"区（规则驱动 + 工具化），对话 Tab 是"自由探索"区（AI 原生）
- 两层并存：学生既能在结构化工具里完成闭环（上传→练习→错题→冲刺），又能在对话里零碎提问——避免被迫使用别的 AI

### 4.2 对话 Tab：通用 AI 问答（pi 原生，默认）

**依据**：pi 底座原生能力 + 03-Architecture §6.7（会话管理业务化）+ 02-PRD §1.2（AI 受约束辅助）

```
┌─────────────────────────────────────────────────────────────────┐
│  💬 学习对话  |  📐 高数  |  目标：极限练习  |  关联错题：#极限-001│
├─────────────────────────────────────────────────────────────────┤
│  🤖 你好，今天想学点什么？（上下文已注入：L1 画像 + 当前学期/课程） │
│                                                                   │
│  学生：帮我理解极限的 $\epsilon-\delta$ 定义                       │
│                                                                   │
│  🤖 极限的 $\epsilon-\delta$ 定义是...                             │
│  [工具调用：studybuddy_tts_speak]  🔊 已朗读                       │
│                                                                   │
│  学生：@第2章笔记.pdf 第3页那个例子                                │
│                                                                   │
│  🤖 根据笔记第3页...                                               │
│  [工具调用：studybuddy_generate_note] ✅ 已生成笔记 [查看]         │
│                                                                   │
│  学生：给我出 5 道导数定义的练习题                                 │
│  [工具调用：studybuddy_generate_questions] ✅ 已生成 5 题 [去练习] │
│                                                                   │
├─────────────────────────────────────────────────────────────────┤
│  [📎 @文件]  [📐 学科]  [输入消息...]                    [发送]   │
└─────────────────────────────────────────────────────────────────┘
```

**pi 原生能力承载**（不废弃，全保留）：

| 能力 | 来源 | pi-studybuddy 落地 |
|---|---|---|
| **流式回复** | pi `Streams["agent.events"]` | ChatWindow 直接复用 pi-desktop |
| **工具调用视图** | pi `countToolCallBlocks` | 直接复用 + 学习工具图标（studybuddy_*） |
| **上下文压缩** | pi `onContextUsageChange` | 直接复用，长对话自动压缩 |
| **@文件引用** | pi-desktop `allowed-roots.ts` + `session-file-references.ts` | 限当前课程资料 |
| **多模型切换** | pi `modelsConfig.get/set`（业务数据根 `<dataRoot>/config/models.json`） | 设置页配置（§9.2） |
| **registerTool 工具** | pi 原生工具调用 | S1-S7 + TTS + 备份恢复全部工具可在对话中由 AI 自主调用 |

**学习场景业务化**（在 pi 原生之上叠加）：

| 业务化项 | 说明 |
|---|---|
| **学科标签** | 输入区可标注学科（颜色标识），影响 AI 上下文 |
| **学习目标** | 会话可设目标（"极限练习"），影响工具调用偏好 |
| **错题关联** | 会话可关联错题 ID（从 S4 跳转，AI 可读取错题上下文） |
| **L1 画像注入** | `before_agent_start` 钩子注入学习者画像（03-Architecture §2.3） |
| **L3 会话检索** | `turn_end` 钩子增量索引，历史会话可检索 |
| **TTS 随时可击发** | 任意 AI 回复可朗读（§5） |
| **工具调用可跳转** | AI 调用 `studybuddy_generate_note` 后，"查看"按钮跳转到笔记 Tab |

**关键约束**：
- **对话是基础功能，不是可选**——pi-studybuddy 作为"专属 studybuddy"，必须让学生在本机完成所有 AI 问答，不被迫使用别的 AI
- **AI 受约束辅助**（02-PRD §1.2）：AI 只负责受约束生成/抽取/批改/润色，不接管决策、不替学生改写事实
- **工具调用透明**：每次 AI 调用工具都可视化展示，学生知道 AI 做了什么
- **Simple Mode**（03-Architecture §2.5）：学生可在设置中切换简化模式，降低上下文消耗

**与 S1-S7 标签页的关系**：
- 对话 Tab 是"自由探索"——学生零碎提问，AI 自由回答 + 按需调用工具
- S1-S7 标签页是"结构化工具"——学生走闭环（上传→练习→错题→冲刺），规则驱动
- 两者数据贯通：对话中 AI 生成的笔记 → 笔记 Tab 可见；练习 Tab 的错题 → 对话中可 @引用讨论
- 会话列表（左侧栏 §3.3）统一管理所有对话，支持学科+目标+内容模糊搜索

### 4.3 首页 Tab：每日学习简报

**依据**：07-Workflow §2.2 第 5 步 + 02-PRD §7.4（规则聚合非 AI）

```
┌─────────────────────────────────────────────────────────────────┐
│  2026-08-07 周五  |  高等数学  |  距期中考试还有 12 天            │
├─────────────────────────────────────────────────────────────────┤
│  📌 明日准备                                                     │
│  • 高数 8:00-9:30 第3章 导数（课表）                               │
│  • 英语 10:00-11:40 Unit 5                                       │
├─────────────────────────────────────────────────────────────────┤
│  ⏰ 待办（到期/未完成）                                           │
│  • [ ] 重做错题 #极限-001（来源：S4，优先级高）                     │
│  • [ ] 复习"导数定义"知识模块（来源：S2，今日目标）                   │
├─────────────────────────────────────────────────────────────────┤
│  🔔 待质检                                                       │
│  • "高数第2章笔记.pdf" AI 生成失败，保留原文待重新生成 [重试]         │
├─────────────────────────────────────────────────────────────────┤
│  📖 错题复习建议                                                  │
│  • 极限计算 3 道错题（距上次复习 3 天，艾宾浩斯提醒） [去复习]        │
├─────────────────────────────────────────────────────────────────┤
│  ➡️ 下一步建议                                                    │
│  • 今日数据结构尚未学习，建议 14:00-15:30                          │
│                                          [▶ 朗读今日简报]         │
└─────────────────────────────────────────────────────────────────┘
```

**关键约束**：
- **规则聚合非 AI**（02-PRD §7.4）：从 `study_events` + `study_tasks` + `schedule_entries` + `mistakes` 聚合
- 每日简报可 TTS 朗读（朗读按钮在右下角）
- 待质检项可一键重试（`materials.retryAiGeneration`）

### 4.4 资料 Tab：上传与转换

**依据**：07-Workflow §2.3

```
┌─────────────────────────────────────────────────────────────────┐
│  高等数学 > 资料                              [📤 上传资料]       │
├─────────────────────────────────────────────────────────────────┤
│  文件名              类型   大小    状态         操作              │
│  ───────────────────────────────────────────────────────────── │
│  第2章笔记.pdf       PDF   2.3MB   ✅ 已生成笔记  [查看] [朗读]   │
│  导数练习.doc        DOC   1.1MB   🔄 转换中...   ─              │
│  课堂截图.png        PNG   800KB   ✅ 已生成笔记  [查看] [朗读]   │
│  第1章.ppt          PPT   5.2MB   ⚠ 转换失败    [重试]          │
└─────────────────────────────────────────────────────────────────┘
```

**上传交互**：
- 点击"上传资料" → 文件选择对话框（支持拖拽，借鉴 `useDragDrop.ts`）
- 上传后显示转换进度（`Streams["jobs.progress"]`）
- 状态标识：`pending` ⏳ / `converting` 🔄 / `converted` ✅ / `note_generating` ✍️ / `completed` ✅ / `conversion_failed` ⚠ / `pending_quality_check` 🔔

**关键约束**：
- 服务端 MIME 验证（不信浏览器，08-Test §5.1 断言）
- `doc/ppt/xls` 走 WPS COM 桥转换（状态显示"DOC→DOCX 转换中"）
- zip/7z/rar 拒绝解析（02-PRD §6 澄清）

### 4.5 笔记 Tab：预览与导图

**依据**：07-Workflow §2.3 第 3/6 步

```
┌──────────────────────────────────────┬──────────────────────────┐
│  📝 第2章笔记（Markdown）             │  🧠 思维导图（Markmap）   │
│  ┌────────────────────────────────┐  │  ┌────────────────────┐  │
│  │ # 导数与微分                    │  │  │                    │  │
│  │                                  │  │  │   导数与微分       │  │
│  │ ## 导数定义                      │  │  │   ├ 导数定义        │  │
│  │ $f'(x) = \lim_{\Delta x \to 0}$ │  │  │   ├ 导数几何意义    │  │
│  │                                  │  │  │   └ 求导法则        │  │
│  │ ## 知识模块                       │  │  │                    │  │
│  │ - [ ] 导数定义 [未开始]           │  │  └────────────────────┘  │
│  │ - [x] 极限复习 [已掌握]          │  │                          │
│  │                                  │  │  [▶ 朗读此笔记]          │
│  └────────────────────────────────┘  │                          │
│  [▶ 朗读] [✏️ 编辑] [📋 知识模块]      │                          │
└──────────────────────────────────────┴──────────────────────────┘
```

**关键约束**：
- Markdown 渲染：react-markdown + KaTeX 公式 + Mermaid 图表（pi-desktop 直接复用）
- 知识模块**必须带 source_evidence 回链**（点击模块可跳回原文位置，08-Test §7.3 断言）
- 学习状态流转：`not_started → learning → mastered → needs_review`（07-Workflow §2.3 第 7 步）
- 朗读按钮内嵌在笔记标题栏

### 4.6 练习 Tab：限时作答

**依据**：07-Workflow §2.4

```
┌─────────────────────────────────────────────────────────────────┐
│  限时练习  |  模块：导数定义  |  ⏱ 12:34 剩余  |  5/10 题       │
├─────────────────────────────────────────────────────────────────┤
│  Q5. 设 $f(x) = x^2$，则 $f'(2) = $？                            │
│                                                                   │
│  A. 2      B. 4      C. 8      D. 16                              │
│                                                                   │
│  你的答案：[ B ]                                                   │
│                                                                   │
│  [上一题]  [下一题]  [提交]                                       │
└─────────────────────────────────────────────────────────────────┘
```

**关键约束（防泄露铁律）**：
- **作答前 DTO 不含 correct_answer / acceptable_answers / explanation**（08-Test §7.2 断言）
- 计时器由前端管理（`Streams["practice.timer"]`）
- 超时可提交但标记超时
- 提交后规则批改（非 AI），展示结果：

```
┌─────────────────────────────────────────────────────────────────┐
│  练习结果  |  正确率 80% (8/10)  |  用时 15:22                     │
├─────────────────────────────────────────────────────────────────┤
│  Q5. ✅ 正确  你的答案：B  正确答案：B  解析：$f'(x)=2x$...        │
│  Q6. ❌ 错误  你的答案：A  正确答案：C  解析：...   [加入错题]      │
│                                            [▶ 朗读错题解析]      │
└─────────────────────────────────────────────────────────────────┘
```

### 4.7 错题 Tab：改错与薄弱点

**依据**：07-Workflow §2.5

```
┌──────────────────────────────────┬──────────────────────────────┐
│  错题列表                          │  错题详情                     │
│  ──────────────────────────────  │  ──────────────────────────  │
│  🔴 极限-001  导数定义  需复习      │  题目：...                    │
│  🟡 极限-002  求导法则  需复习      │  你的答案：A（错误）           │
│  🟢 极限-003  导数定义  已掌握      │  正确答案：C                  │
│                                    │  解析：...                    │
│  [筛选：全部/需复习/已掌握]        │                               │
│                                    │  AI 错因建议（仅供参考）：     │
│                                    │  • 可能是概念不清（不确定）    │
│                                    │                               │
│                                    │  请确认错因：                  │
│                                    │  ○ 概念不清                    │
│                                    │  ○ 看错题                      │
│                                    │  ○ 公式错误                    │
│                                    │  ○ 步骤遗漏                    │
│                                    │  ○ 时间压力                    │
│                                    │  ○ 其他                        │
│                                    │  [确认错因]  [重做]  [▶ 朗读] │
└──────────────────────────────────┴──────────────────────────────┘
```

**关键约束**：
- 错因六分类（07-Workflow §2.5 第 4 步）
- AI 建议带"不确定"标记，学生必须确认（08-Test §7.3 断言）
- 重做正确 → 掌握证据增加；重做错误 → 保持 `needs_review`
- `evidence_count≥2` 才形成薄弱点（单次错误不形成）
- "已掌握"非终态，可回退（状态机 §8.6）

### 4.8 冲刺 Tab：模拟考/速背/计划

**依据**：07-Workflow §2.6

```
┌─────────────────────────────────────────────────────────────────┐
│  期末冲刺  |  距期中考试 12 天  |  考试已确认 ✅                   │
├─────────────────────────────────────────────────────────────────┤
│  [模拟考]  [速背卡]  [冲刺计划]                                   │
├─────────────────────────────────────────────────────────────────┤
│  🃏 速背卡（确定性只读，不依赖 AI）                                │
│  ─────────────────────────────────────────────────────────────  │
│  卡片 3/15                                                       │
│                                                                   │
│  知识点：导数的几何意义                                           │
│                                                                   │
│  函数 $f(x)$ 在点 $x_0$ 处的导数 $f'(x_0)$ 是曲线在该点的切线斜率。 │
│                                                                   │
│  切线方程：$y - f(x_0) = f'(x_0)(x - x_0)$                       │
│                                                                   │
│  [上一张]  [下一张]  [▶ 朗读此卡]  [标记已掌握]                   │
└─────────────────────────────────────────────────────────────────┘
```

**关键约束**：
- 速背卡 + 冲刺计划是**确定性只读 DTO**，不持久化、不依赖 AI（08-Test §7.4 断言）
- 模拟考必须 `confirmed` 考试才能生成（触发器拦截，08-Test §3.2.3）
- 朗读按钮在每张速背卡上（考前每日 TTS 朗读）
- 模拟考结果展示总分/正确率/耗时/逐题/模块覆盖和弱项

### 4.9 报告 Tab：家长报告（学生侧）

**依据**：07-Workflow §3

```
┌─────────────────────────────────────────────────────────────────┐
│  家长报告  |  本周报告  |  2026-08-01 ~ 2026-08-07               │
├─────────────────────────────────────────────────────────────────┤
│  [生成报告]  [查看历史]                                          │
├─────────────────────────────────────────────────────────────────┤
│  📊 学习节奏                                                      │
│  本周学习 5 天，累计 12.5 小时（目标 14 小时）                     │
│  高数 6h / 英语 4h / 数据结构 2.5h                                │
├─────────────────────────────────────────────────────────────────┤
│  📁 资料整理                                                      │
│  上传 8 份资料，已生成笔记 7 份（1 份质检中）                     │
├─────────────────────────────────────────────────────────────────┤
│  ✏️ 练习表现                                                      │
│  完成 5 次练习，平均正确率 78%（↑5% vs 上周）                     │
├─────────────────────────────────────────────────────────────────┤
│  ❌ 错题情况                                                      │
│  新增错题 12 道，已掌握 8 道，待复习 4 道                         │
│  错因分布：概念不清 5 / 公式错误 4 / 看错题 3                     │
├─────────────────────────────────────────────────────────────────┤
│  🔔 考试提醒                                                      │
│  期中考试 8月19日（剩 12 天）                                     │
├─────────────────────────────────────────────────────────────────┤
│  ✅ 数据质量                                                      │
│  数据完整，无遗漏                                                 │
├─────────────────────────────────────────────────────────────────┤
│  [冻结并投递]                                                     │
│  投递渠道：本地导出 ✓  /  邮件 ✗（重试中）  /  飞书 ─（未配置）   │
└─────────────────────────────────────────────────────────────────┘
```

**关键约束（隐私铁律）**：
- 报告内容**只有脱敏聚合**：计数/状态/趋势/短标题/日期窗口（02-PRD §5.2）
- **绝不包含**：原文/题干/答案/作答/错因正文/完整 UUID/真实渠道地址
- 冻结时 `assertNoSensitiveLeak` UUID 泄漏检测（08-Test §5.4 冒烟）
- 投递渠道独立失败隔离，状态可视化（sent ✅ / failed ✗ / 未配置 ─）
- **家长不进系统**：学生侧生成投递，家长只接收

### 4.10 采集 Tab：课堂录音转写

**依据**：07-Workflow §2.7

```
┌─────────────────────────────────────────────────────────────────┐
│  课堂采集  |  高等数学                                           │
├─────────────────────────────────────────────────────────────────┤
│  ⚠ 合规确认                                                      │
│  ☑ 我已获老师和相关同学允许，仅用于本机学习整理                    │
├─────────────────────────────────────────────────────────────────┤
│  选择音频文件（PCM WAV: 16kHz/单声道/16-bit）                     │
│  [选择文件...]  或拖拽到此处                                      │
├─────────────────────────────────────────────────────────────────┤
│  转写结果（可编辑）                                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 今天我们讲导数的定义。导数是函数在某点的变化率...           │  │
│  │ ...                                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  [保存为 S2 笔记输入]  [▶ 朗读转写文本]                          │
└─────────────────────────────────────────────────────────────────┘
```

**关键约束**：
- **受控 PCM WAV 单一输入**（服务端重新验证文件头，08-Test §3.3.2）
- **许可确认强制**（未勾选不可上传）
- 原始音频只暂存 tmp，`finally` 清理（不进日志/StudyEvent/S6）
- 保存后创建 `file_type='text'` material，学生在 S2 自行 `generateNote`

---

## 5. TTS 朗读 UI（跨子系统随时可击发）

### 5.1 全局朗读控制条

**依据**：02-PRD §3.9 + 07-Workflow §4

朗读控制条**常驻**在主内容区顶部（见 §2.1 布局图）：

```
┌─────────────────────────────────────────────────────────────────┐
│  🔊 TTS  |  引擎：SAPI（离线）▼  |  语速：1.0x ━●━━━  |  停止    │
└─────────────────────────────────────────────────────────────────┘
```

**控制项**：
- **引擎切换**：SAPI（默认离线）/ edge-tts（音质好需网络）
- **语速调节**：0.5x ~ 2.0x
- **播放控制**：播放 / 暂停 / 停止
- **状态显示**：当前朗读内容标题 + 进度

**API**：`tts.speak` / `tts.control` / `tts.switchEngine` / `tts.getStatus`（06-API）

### 5.2 内嵌朗读按钮

**每个有文字内容的区域都有独立的朗读入口**：

| 位置 | 朗读内容 | 触发 |
|---|---|---|
| 首页每日简报 | 简报全文 | "朗读今日简报"按钮 |
| S2 笔记预览 | Markdown 笔记正文 | 笔记标题栏"朗读"按钮 |
| S2 知识模块 | 模块内容 | 模块卡片"朗读"按钮 |
| S3 练习结果 | 错题解析 | "朗读错题解析"按钮 |
| S4 错题详情 | 错题解析/复盘笔记 | "朗读"按钮 |
| S5 速背卡 | 速背卡内容 | "朗读此卡"按钮（考前每日） |
| S5 冲刺计划 | 计划内容 | "朗读计划"按钮 |
| S7 转写文本 | 转写结果 | "朗读转写文本"按钮 |
| 任意 Markdown | Markdown 内容 | 内容区"朗读"按钮 |

### 5.3 朗读状态反馈

```
朗读中 → 控制条高亮，进度条显示当前位置
        → 当前朗读段落高亮（类似阅读器朗读模式）
暂停   → 控制条暂停状态，可继续
完成   → 控制条归位，可选"标记已复习"
```

### 5.4 标记已复习

**依据**：07-Workflow §4.3

朗读完成后（或学生主动），可标记"已复习"：
- 点击"标记已复习" → 写 `study_events`（`event_type=practice_reviewed`）
- 朗读本身不持久化，仅"已复习"标记写 StudyEvent
- **API**：`events.markReviewed({refType, refId})`

### 5.5 引擎降级

```
edge-tts 朗读失败 → 自动降级 SAPI → 控制条提示"已切换到 SAPI"
SAPI 不可用（非 Windows）→ 提示"系统 TTS 不可用，请安装 edge-tts"
```

---

## 6. 备份恢复 UI

**依据**：07-Workflow §5 + 02-PRD §3.10

### 6.1 备份入口

```
┌─────────────────────────────────────────────────────────────────┐
│  备份与恢复                                                       │
├─────────────────────────────────────────────────────────────────┤
│  手动备份                                                         │
│  [备份此课程]  [备份全部课程]                                      │
│  目标目录：H:\backups\studybuddy\  [更改...]                      │
├─────────────────────────────────────────────────────────────────┤
│  定期调度                                                         │
│  ☑ 自动备份（每周一 00:00）                                       │
│  频率：○ 每周一  ○ 每月一日                                       │
├─────────────────────────────────────────────────────────────────┤
│  备份历史                                                         │
│  时间         课程        类型       大小    状态    操作          │
│  08-07 00:00  高等数学    scheduled 12MB   ✅完成  [恢复] [删除]  │
│  08-06 18:30  全部课程    manual     45MB   ✅完成  [恢复]        │
│  08-05 00:00  大学英语    scheduled  8MB    ✅完成  [恢复] [删除]  │
├─────────────────────────────────────────────────────────────────┤
│  从备份恢复                                                       │
│  [选择 zip 文件...]                                               │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 恢复交互

选择 zip 文件后：

```
1. 校验 content_hash → 通过 / 失败（损坏提示）
2. 校验 schema_version → 兼容 / 不兼容（版本提示）
3. 检查同名课程冲突：
   - 无冲突 → 直接导入
   - 有冲突 → 弹窗：
     ┌─────────────────────────────────────────┐
     │  发现同名课程"高等数学"                   │
     │  ○ 覆盖现有课程数据                        │
     │  ○ 新建课程（保留两份）                    │
     │  [确认]  [取消]                          │
     └─────────────────────────────────────────┘
4. 恢复完成 → integrity_check → 显示结果：
   "已恢复课程：高等数学 | 导入 15 张表 | 恢复 23 个文件 | 完整性检查通过"
```

### 6.3 归档触发备份

学期归档时（`semesters.transition` → `archived`）：
- 自动弹出"归档前备份"进度条
- 所有课程备份成功后才允许归档
- 归档完成后再触发一次"归档后备份"

---

## 7. 会话管理 UI（业务化，对话 Tab 的承载）

**借鉴**：pi-desktop `SessionSidebar.tsx` + `ChatWindow.tsx`（03-Architecture §6.7）

> **会话即"💬 对话 Tab"的内容**——左侧栏选中一个会话，主内容区"💬 对话"标签页加载该会话。这是 pi-studybuddy 的基础能力，不是可选。

### 7.1 学习会话语义

pi-desktop 的会话是 coding agent 对话，pi-studybuddy 业务化为**学习对话**：

| 维度 | pi-desktop | pi-studybuddy |
|---|---|---|
| 会话语义 | coding 对话 | 学习对话（提问/练习/笔记整理/零碎问答） |
| 承载 | 会话窗口 | **"💬 对话"标签页**（默认打开） |
| 标签 | 无 | 学科标签（颜色标识） |
| 学习目标 | 无 | 目标标题（如"极限练习"） |
| 关联 | 无 | 可关联错题 ID / 资料 ID |
| 流式回复 | `Streams["agent.events"]` | 直接复用 |
| 工具调用视图 | `countToolCallBlocks` | 直接复用 + 学习工具图标 |
| 上下文压缩 | `onContextUsageChange` | 直接复用 |

### 7.2 会话交互

```
┌─────────────────────────────────────────────────────────────────┐
│  💬 学习对话  |  📐 高数  |  目标：极限练习  |  关联错题：#极限-001│
├─────────────────────────────────────────────────────────────────┤
│  学生：帮我理解极限的 $\epsilon-\delta$ 定义                       │
│                                                                   │
│  🤖 AI：极限的 $\epsilon-\delta$ 定义是...                         │
│  [工具调用：studybuddy_tts_speak]  🔊 已朗读                      │
│                                                                   │
│  学生：@第2章笔记.pdf 第3页那个例子                                │
│                                                                   │
│  🤖 AI：根据笔记第3页...                                           │
├─────────────────────────────────────────────────────────────────┤
│  [📎 @文件]  [📐 学科]  [输入消息...]                    [发送]   │
└─────────────────────────────────────────────────────────────────┘
```

**@文件引用**：借鉴 pi-desktop `allowed-roots.ts` + `session-file-references.ts`
- 输入 `@` → 弹出文件选择器（限当前课程资料）
- 选中的文件作为上下文传入对话

---

## 8. 文件体验 UI

**借鉴**：pi-desktop `FileExplorer.tsx` + `FileViewer.tsx`（03-Architecture §6.6）

### 8.1 文件浏览器

```
┌──────────────────────────────────┐
│  📁 当前课程：高等数学             │
│  📁 storage/                      │
│    📄 第2章笔记.pdf               │
│    📄 导数练习.doc                │
│    🖼 课堂截图.png                │
│    📄 第1章.docx                  │
│  [刷新]  [在外部打开]              │
└──────────────────────────────────┘
```

### 8.2 文件预览

**支持格式**（03-Architecture §6.6）：
- **Markdown**：react-markdown + KaTeX + Mermaid + SyntaxHighlighter
- **docx**：mammoth 转 HTML（DOCX_PREVIEW_MAX_BYTES 限制）
- **图片**：直接预览
- **PDF**：内嵌预览

**@引用**：文件可拖入对话作为上下文

---

## 9. 技能与模型管理 UI

### 9.1 学习技能包管理（替换 pi-desktop SkillsConfig）

**依据**：03-Architecture §5.1-§5.3 + §6.8（Skills.sh 替换为学习技能包）

```
┌─────────────────────────────────────────────────────────────────┐
│  学习技能包                                                       │
├─────────────────────────────────────────────────────────────────┤
│  已安装技能                                                       │
│  🔊 studybuddy-tts          v1.0  依赖：SAPI                     │
│  📝 studybuddy-ocr-schedule  v1.0  依赖：OCR venv                 │
│  📄 studybuddy-wps-convert   v1.0  依赖：WPS COM                  │
│  🎤 studybuddy-whisper       v1.0  依赖：whisper.cpp              │
│  📺 youtube-transcript       v1.0  依赖：网络（pi-skills 引入）   │
│  🔍 brave-search             v1.0  依赖：网络 + API key           │
├─────────────────────────────────────────────────────────────────┤
│  技能健康检查                                                     │
│  ✅ SAPI 可用          ✅ OCR venv 可用                           │
│  ✅ WPS COM 可用       ⚠ whisper.cpp 未配置 [配置路径]            │
│  ✅ 网络可用           ✅ brave API key 已配置                     │
└─────────────────────────────────────────────────────────────────┘
```

**关键约束**：
- **不让学生接触 pi 插件机制**（Plugins 省略）
- 技能管理面向"学科/章节/题型模板"语义，非开发者
- 启动校验：技能完整性 + 依赖可用性（`skills.manifest.json`，03-Architecture §5.5）

### 9.2 模型供应商配置

**借鉴**：pi-desktop `ModelsConfig.tsx`

```
┌─────────────────────────────────────────────────────────────────┐
│  模型供应商                                                       │
├─────────────────────────────────────────────────────────────────┤
│  当前默认：ZAI/GLM-4.5                                           │
│                                                                   │
│  已配置供应商                                                     │
│  ○ ZAI / GLM-4.5          ✅ API key 已配置（credential-vault）  │
│  ○ Qwen / Qwen-Max        ✅ API key 已配置                      │
│  ○ Ollama / qwen2.5:7b     ✅ 本地推理可用                        │
│  ○ Xiaomi / MiMo-7B       ─ 未配置 [配置]                        │
│                                                                   │
│  [添加供应商]  [测试连接]                                         │
└─────────────────────────────────────────────────────────────────┘
```

**关键约束**：
- API key 走 credential-vault（DPAPI 加密，键名 `modelProvider:xxx`）
- 支持本地推理（Ollama）或学生自配供应商
- 模型选择持久化到 `<dataRoot>/config/models.json`（`__studybuddy_managed` 标记）

<!-- supersedes: v0.1.2 原写 "~/.pi/agent/models.json"（§4.2 多模型切换 + §9.2 模型选择持久化），T-M3-005 裁决 1 改业务数据根 <dataRoot>/config/models.json（AGENTS.md §9.5 物理隔离，pi-studybuddy 不侵入 ~/.pi） -->

---

## 10. 设置 UI

### 10.1 设置页结构

```
┌──────────────────────────────────┐
│  ⚙ 设置                           │
├──────────────────────────────────┤
│  通用                             │
│  ├ 学习偏好（每日目标时长/可用时间）│
│  ├ TTS（默认引擎/语速/音色）       │
│  └ 备份（默认目录/调度频率）       │
│  安全                             │
│  ├ 密钥管理（credential-vault）   │
│  ├ 日志（脱敏设置/查看日志）       │
│  └ 数据根（路径/磁盘占用）         │
│  开发者                           │
│  ├ 工具链（Node/Python/uv/Git）   │
│  ├ 实验性功能                      │
│  └ 调试日志                        │
└──────────────────────────────────┘
```

### 10.2 工具链发现（借鉴 pi-desktop ToolchainsConfig）

**依据**：03-Architecture §6.5

```
┌─────────────────────────────────────────────────────────────────┐
│  工具链健康检查                                                   │
├─────────────────────────────────────────────────────────────────┤
│  Node.js        v24.10.0   ✅ healthy   /usr/bin/node           │
│  Python         v3.10.8    ✅ healthy   H:\AIStudyBuddy\venv\   │
│  uv             v0.5.0     ✅ healthy   ~/.local/bin/uv         │
│  Git             v2.45.0    ✅ healthy   /usr/bin/git            │
│  ripgrep        v14.0.0    ✅ bundled   (内置)                   │
│  fd             v10.1.0    ✅ bundled   (内置)                   │
│  WPS Office     2026       ✅ detected   (COM 可用)              │
│  whisper.cpp    ─          ⚠ 未配置    [配置路径]                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 11. 安全与隐私 UI 边界

### 11.1 不展示的内容（铁律）

**依据**：02-PRD §5.2 + 08-Test §7.2

UI 层**绝不展示**：
- 完整 UUID（只显示短 ID 或 `极限-001` 人类可读 ID）
- API Key（只显示"已配置/未配置"）
- 真实渠道地址（邮箱/Webhook URL，只显示别名"妈妈邮箱"）
- 内部错误栈（只显示中文可操作消息）
- 文件绝对路径（只显示相对路径或文件名）
- SQL 语句
- 学生隐私正文（家长报告中）

### 11.2 安全交互

- **敏感操作二次确认**：归档学期 / 恢复覆盖 / 删除课程 → 弹窗确认
- **许可确认强制**：S7 课堂采集必须勾选合规确认
- **密钥输入**：API key 输入框 `type="password"`，提交后走 credential-vault，不回显
- **错误消息**：统一 6 错误码中文消息，不暴露内部

### 11.3 桌面通知与消息中心

**依据**：03-Architecture §7.4（推送目标改为桌面通知 + 应用内消息中心）

```
┌──────────────────────────────────┐
│  🔔 消息中心                      │
├──────────────────────────────────┤
│  📋 备份完成  08-07 00:00         │
│  高等数学已自动备份（scheduled）   │
│  ──────────────────────────────  │
│  📋 错题复习提醒  08-06 18:00     │
│  3道错题距上次复习已3天            │
│  ──────────────────────────────  │
│  📋 报告投递失败  08-06 20:00     │
│  邮件投递失败（重试2/3）           │
└──────────────────────────────────┘
```

**Streams 推送**：`schedule.reminder` / `backup.progress` / `delivery.status`

---

## 12. Streams 推送与 UI 状态映射

**依据**：06-API §4（9 个 Streams 主题）

| Stream | UI 更新位置 | 触发 |
|---|---|---|
| `agent.events` | 会话窗口流式回复 + 上下文压缩指示器 | AI 回复 / 上下文压缩 |
| `jobs.progress` | 资料 Tab 状态列 + 笔记生成进度 | 转换/生成 Job 状态变更 |
| `practice.timer` | 练习 Tab 计时器 | 限时练习倒计时 |
| `tts.state` | 朗读控制条 | 朗读播放/暂停/停止 |
| `backup.progress` | 备份进度条 + 消息中心 | 备份进度 |
| `delivery.status` | 报告 Tab 投递状态 + 消息中心 | 投递成功/失败 |
| `files.changed` | 文件浏览器刷新 | 文件变更 |
| `toolchains.changed` | 设置页工具链健康检查 | 窗口 focus 重扫后 |
| `schedule.reminder` | 消息中心 + 桌面通知 | 调度提醒 |

---

## 13. 响应式与可访问性

### 13.1 响应式（借鉴 pi-desktop `useIsMobile.ts`）

- **桌面（>1024px）**：三栏布局（左导航 + 主内容 + 右面板）
- **平板（768-1024px）**：两栏（左导航折叠为图标 + 主内容），右面板改为抽屉
- **窄屏（<768px）**：单栏 + 抽屉式导航（虽然 v0.1 只 Windows 桌面，但保留响应式基础）

### 13.2 主题（借鉴 pi-desktop `useTheme.ts`）

- **浅色/深色主题**：跟随系统或手动切换
- **学习场景配色**：学科标签颜色（数学=蓝/英语=红/物理=绿/化学=紫/...）
- **状态颜色**：success=绿 / warning=黄 / error=红 / info=蓝

### 13.3 键盘快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl+N` | 新建会话 |
| `Ctrl+O` | 打开文件 |
| `Ctrl+S` | 保存当前笔记 |
| `Ctrl+P` | 打印 |
| `Space` | TTS 播放/暂停（焦点在朗读内容时） |
| `Esc` | TTS 停止 |
| `Ctrl+,` | 打开设置 |
| `Ctrl+Shift+B` | 备份当前课程 |

---

## 14. UI 测试断言映射

**依据**：08-Test §6（E2E）+ §7（关键断言矩阵）

### 14.1 E2E UI 断言

| E2E 用例 | UI 断言 |
|---|---|
| E2E-01 学期初始化 | 课表 OCR 预览显示 → 确认后首页 ready |
| E2E-02 资料笔记 | 上传后状态转换可见 → 笔记预览含 KaTeX 公式 → 导图渲染 |
| E2E-03 练习→错题 | 作答前 UI 不显示正确答案 → 批改后结果可见 → 错题 Tab 自动更新 |
| E2E-04 期末冲刺 | 速背卡可翻页 → 每卡有朗读按钮 → 冲刺计划展示 7 天 |
| E2E-05 课堂采集 | 许可确认未勾选时上传按钮禁用 → 转写可编辑 |
| E2E-06 家长报告 | 报告界面无原文/题干/答案/UUID → 投递状态可视化 |
| E2E-07 TTS | S2 笔记/S4 错题/S5 速背卡均有朗读按钮 → 朗读控制条状态更新 |
| E2E-08 备份恢复 | 备份进度条 → 恢复冲突弹窗 → integrity_check 结果显示 |
| E2E-09 调度备份 | 调度配置后消息中心显示备份完成通知 |
| E2E-10 对话默认主入口 | 应用启动 → 默认打开"💬 对话"标签页 → 看到"你好，今天想学点什么？" → 发送消息 → AI 流式回复（ChatWindow 滚动）→ 会话标题栏显示学科标签（L1 画像注入） |
| E2E-11 对话 AI 调用工具 + 跳转 | 学生发"出题" → AI 回复含工具调用视图（studybuddy_generate_questions 卡片）→ "已生成 5 题 [去练习]"按钮 → 点击跳转练习 Tab（sessionId 传递） |
| E2E-12 对话 @文件引用 | 输入 @ → 弹出文件选择器（限当前课程资料）→ 选文件 → 输入区显示文件引用 → AI 引用文件内容回复 |
| E2E-13 对话 TTS 朗读 + L3 检索 | AI 回复含内嵌朗读按钮 → 朗读控制条状态更新 → 历史会话列表模糊搜索可查到本次会话 |

### 14.2 隐私边界 UI 断言（08-Test §7.2）

- [ ] 报告界面序列化扫描不含完整 UUID（正则检测）
- [ ] 作答前 UI 不含 `correct_answer` / `acceptable_answers` / `explanation` 字段
- [ ] API key 输入框 `type="password"`，不回显
- [ ] 错误消息不含内部栈/SQL/路径/UUID
- [ ] 桌面通知不含原文/题干/答案

---

## 15. 版本历史

| 版本 | 日期 | 变更 |
| v0.1.4 | 2026-08-09 | 交叉审查修订：修正早期“打开即进入 S1 首页”表述，与实现 `DEFAULT_TAB_ID=chat`、PRD/Workflow/Test 的对话默认主入口统一。 |
| v0.1.3 | 2026-08-08 | §4.2 多模型切换 + §9.2 模型选择持久化落点修订：`~/.pi/agent/models.json` → `<dataRoot>/config/models.json`（T-M3-005 裁决 1，AGENTS.md §9.5 物理隔离；supersedes 注记见 §9.2 后）。上游 06-API v0.1.5 同步 |
| v0.1.2 | 2026-08-07 | 审查定案：§4 子章节编号修正（4.3 重号→4.4-4.10 连续）；§12 Streams 与 06-API §4 对齐（material.status→jobs.progress，context.usage 归入 agent.events，补 toolchains.changed）；§4.4 Stream 引用同步；§14.1 补充 E2E-10~13 对话 Tab UI 断言；上游版本号同步。审查结论：完整性/上游一致性/铁律落实/E2E 覆盖四项通过 |
| v0.1.1 | 2026-08-07 | 按用户反馈修订：把"💬 对话"提升为默认第一标签页（§4.2 新增），ChatInput 升级为核心组件承载 pi 原生 AI 对话能力（§2.2 修订）；明确 pi 天生自带对话能力是"专属 studybuddy"的根基，不废弃；学生可在对话里零碎问答 + AI 自主调用 S1-S7 工具，避免被迫使用别的 AI；会话管理 §7 明确"会话即对话 Tab 的承载"；标签页结构改为"对话（默认）+ 学习工作台"双层并存 |
| v0.1.0 | 2026-08-07 | 初始草案：整体布局（三栏 AppShell + 标题栏 + 状态栏）；左侧栏（学期/课程/会话三级导航）；学习工作台 8 标签页（首页/资料/笔记/练习/错题/冲刺/报告/采集，对应 S1-S7）；TTS 跨子系统随时可击发 UI（全局控制条 + 内嵌朗读按钮 + 标记已复习）；备份恢复 UI（手动/定期/恢复/归档触发）；会话管理业务化（学科标签/学习目标/错题关联）；文件体验（FileExplorer + Markdown/KaTeX/Mermaid 预览）；技能与模型管理（学习技能包替换 Skills.sh + 供应商配置）；设置页（通用/安全/开发者）；安全隐私 UI 边界（不展示 UUID/API key/真实地址/错误栈）；Streams 推送与 UI 状态映射（9 主题）；响应式与可访问性（主题/快捷键）；UI 测试断言映射（E2E + 隐私边界）。输入：pi-desktop renderer 目录结构 + 03-Architecture §6 + 07-Workflow §2-§5 + 08-Test §6-§7 |
