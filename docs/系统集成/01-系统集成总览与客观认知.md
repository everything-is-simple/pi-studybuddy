# 系统集成总览与客观认知

**版本**：v0.1.5
**日期**：2026-08-19
**状态**：✅ 已审查批准；T-M5-011 执行中
**权威性**：系统集成四份文件之一；后续系统集成动作必须以本文件、`02-系统问题总账与整改清单.md`、`03-系统集成验收与交付路径.md`、`04-系统集成固定工作流与阶段门禁.md` 为依据。

## 1. 文件目的

本文件记录 pi-studybuddy 当前系统的客观认识，不把设计声明、源码存在、单元测试通过、Job 登记或历史任务 done 等同于产品可用。

系统当前的真实目标是：在当前开发机先完成从空数据到学习闭环的真实运行，再将同一套已验证的运行资源、默认配置和启动拓扑复制到使用机，最后重建并验收安装包。

## 2. 当前阶段进度

- 2026-08-19，`T-M5-011` 已在全新隔离根 `H:\\pi-studybuddy-tmp\\runs\\T-M5-011\\startup-empty\\` 完成 S1 开发机真实 Electron 路径：可见 UI 创建学习计划、学期、课程、考试确认、课表和任务完成；关闭后第二次启动可见 UI 回读通过。
- 证据等级：E5。`global.db` 与学期 `sem.db` 的 `PRAGMA integrity_check` 均为 `ok`，页面 DOM 未发现完整 UUID、绝对路径或错误栈。
- 随后执行 `T-M5-011-P0-002-model-gate`：真实 Electron 生产 runtime 在全新隔离根验证空配置 `agent.send` 返回固定 `MODEL_NOT_CONFIGURED`，双启动边界成立，未观察 fixture/假成功，空配置范围为 E3。
- 用户扩展授权后执行 `P0-002-provider-fallback-delivery`：隔离 DPAPI vault 配置 GPT-5.6 Terra 四家有序路由（Vokly 主路由，Pixel/鲨鱼辣椒/小鸡农场 fallback）；第二个 Electron 进程解密与路由回读通过；真实 pi 路径验证小鸡失败后切换一次 Vokly 并收到非空 token。QQ SMTP 和飞书各一次最小投递成功。该结果为生产集成 E3 + 隔离配置重启回读 E5，不是人工设置页 E5，也未写真实业务根。
- 此事实只证明 S1 数据生命周期、P0-002 空配置边界和隔离 fallback/渠道连通性；用户已批准复制已验证配置到真实业务根，并指定先提交、推送本阶段检查点。真实业务根默认配置和重启回读尚未执行，S2-S7、运行资源固化、使用机复制和安装包验收仍未完成，`T-M5-011` 保持执行中。

## 3. 系统客观状态

| 层次 | 当前认识 | 完成标准 |
|---|---|---|
| 设计层 | 01-09 已定义 Electron、S1-S7、数据、API、工作流和 UI | 设计条款被真实主路径验证 |
| 源码层 | 壳层、handler、Adapter、UI 和测试资产已大量存在 | 代码被生产入口接通并服务真实业务数据 |
| 集成层 | 部分链路已接通，部分仍停留在 Job、mock、fixture 或局部 Adapter | 用户动作从 UI 到数据库再到后续业务消费连续成功 |
| 运行资源层 | Electron 自带 Node；Python/OCR/whisper/WPS 仍需固定拓扑和健康检查 | 开发机启动即可发现并使用所需资源 |
| 数据层 | 全局库、学期库和业务表已存在 | 真实用户动作写入后可重启回读、备份和恢复 |
| 交付层 | 当前历史 setup 证据不能代表最终开箱即用 | 使用机复制和安装包安装后可完成主路径 |

## 4. 证据等级

| 等级 | 证据 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| E0 | 设计文档声明 | 应该具备的能力 | 代码或运行可用 |
| E1 | 源码、API、handler 存在 | 局部实现存在 | 生产接通、用户成功 |
| E2 | 单元/Adapter 测试 | 局部逻辑正确 | 系统集成、安装后可用 |
| E3 | 集成/Electron 自动化 | 受控链路可回归 | 空数据真实主路径、资源交付 |
| E4 | 开发机可见 UI 主路径 | 当前机器用户动作成功 | 重启持久化、使用机可用 |
| E5 | 开发机重启回读 | 当前机器数据生命周期成立 | 使用机和安装包可用 |
| E6 | 使用机真实主路径 | 复制拓扑可用 | 最终安装包发行质量 |
| E7 | 安装包安装后主路径 | 可支持最终发行判断 | 不代表所有未声明格式或外部组件均可用 |

系统集成中的“通过”必须标注达到的证据等级。

## 5. 系统集成主链

```text
空数据启动
→ 默认配置/默认模型
→ 创建学期、课程、考试、任务
→ 导入资料
→ 文件存储与格式转换
→ normalized_texts
→ structured_notes
→ knowledge_modules
→ S3 练习
→ S4 错题/重做
→ S5 冲刺
→ S6 报告
→ S7 课堂转写
→ TTS/已复习
→ 备份恢复
→ 重启逐项回读
→ 固化运行资源
→ 复制到使用机
→ 重建并验收安装包
```

任何一步失败，都回到当前开发机修复；不得以后一阶段的测试、安装或文档替代前一阶段的真实结果。

## 6. 能力边界

核心格式必须不依赖 WPS：PDF、DOCX、PPTX、XLSX、TXT、Markdown、PNG、JPG/JPEG、BMP、GIF、WEBP。

旧 Office 格式 DOC、PPT、XLS 作为受控扩展，经已安装且合法可用的 WPS COM 转换为新格式后进入同一文本管道。未完成 WPS 生产注入、真实转换和主链验证前，不得宣称旧 Office 闭环完成。

ODT、ODS、ODP、RTF、EPUB、TIFF 本轮不做，不阻塞核心交付。

## 7. 运行和用户原则

- 孩子不负责安装 Node.js、pnpm、Python、Git 或手工配置 OCR/whisper。
- 设置页只保留首次必要的 AI API Key、QQ SMTP 授权码/收件邮箱和飞书 Webhook。
- 默认模型、Windows SAPI、每周备份、数据目录和学习参数由系统提供默认值。
- 真实秘密、学生数据和生产凭据不得进入 Git、安装包、文档或日志。
- 外部组件缺失必须明确失败、可重试，不得把 Job 登记显示成成功。

## 8. 与另外三份文件的关系

- `02-系统问题总账与整改清单.md`：记录每个断点、根因、责任边界、当前状态和整改顺序。
- `03-系统集成验收与交付路径.md`：规定真实主路径、证据、重启回读、使用机复制和安装包完成判据。
- `04-系统集成固定工作流与阶段门禁.md`：规定每一个系统集成阶段必须执行的固定工作流、阶段证据、回退和停止授权。
- 本文件：规定判断系统时采用的客观模型和边界。

后续任何系统集成计划、代码修改、测试记录和交付判断，必须引用这四份文件中的条款，不得建立平行口径。

## 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1.5 | 2026-08-19 | 用户批准把隔离验证配置复制到真实业务根，并指定先提交、推送本阶段成果到远端；本版本仅登记授权与执行顺序，真实业务根尚未写入，P0-002 仍 partial，不进入 S2。 |
| v0.1.4 | 2026-08-19 | 用户扩展授权后，隔离根完成 GPT-5.6 Terra 四家 fallback、DPAPI 第二进程回读、真实小鸡失败→Vokly 成功切换，以及 QQ SMTP/飞书各一次最小投递；生产集成 E3、隔离配置回读 E5。真实业务根和人工设置页 E5 未完成，P0-002 保持 partial，不进入 S2。 |
| v0.1.3 | 2026-08-19 | 回填 T-M5-011 P0-002：真实 Electron 生产空配置边界 E3（固定 MODEL_NOT_CONFIGURED、无 fixture/假成功、双启动）通过；真实凭据发送因授权不足 blocked；未达到人工 UI E4/E5，不进入 S2。依据：系统集成 04 步骤 8-12、02 问题总账和 03 验收顺序。 |
| v0.1.2 | 2026-08-19 | 系统集成第四份文件 `04-系统集成固定工作流与阶段门禁.md` 建立并纳入唯一权威链；后续每个阶段必须按其固定工作流执行，直至 E7。依据：用户恢复固定工作流的明确裁决。 |
| v0.1.1 | 2026-08-19 | 回填 T-M5-011 开发机 S1 阶段 E5 事实：空隔离根真实 Electron 可见 UI 创建学习计划、学期、课程、考试确认、课表和任务完成，并完成重启回读；只证明 S1 数据生命周期，整体任务仍执行中。依据：真实 UAT 证据 + 系统集成 02/03 + 用户要求三份文件同步。 |
| v0.1.0 | 2026-08-15 | 初始审查批准版本。 |
