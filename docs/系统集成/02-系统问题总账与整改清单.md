# 系统集成 / 02 系统问题总账与整改清单

**版本**：v0.1.0
**日期**：2026-08-15
**状态**：✅ 已审查批准；T-M5-011 执行中；这是当前整改入口，不替代 `04-Todo` 的历史任务证据。
**目标**：先在当前开发机把系统按真实用户路径跑通，再将同一套运行资源和默认配置复制到使用机。流程不再把开发机、使用机、组件阶段拆成互相替代的复杂门槛。

## 1. 当前结论

当前仓库已经完成大量骨架、handler、Adapter、UI 接线和自动化测试，但尚未形成“空数据安装后即可使用”的稳定系统。历史任务的 `done` 只表示该任务定义的代码和证据完成；本表以“用户能否真实完成学习动作”为准重新登记问题。

当前 `04-Todo` 统计：72 个任务，68 done、3 pending、1 blocked。未完成任务为：

| 任务 | 当前状态 | 未完成内容 |
|---|---|---|
| T-M5-005 | blocked | 报告投递重启回读、真实备份恢复重启回读、TTS 已复习持久化原生回读 |
| T-M5-006 | pending | 运行依赖自包含、OCR/whisper/WPS 生产资源和开发机完整装配 |
| T-M5-007 | pending | 干净 Windows 全功能 UAT |
| T-M5-008 | pending | setup/portable 最终发行验收 |

## 2. P0 必须解决的问题

### T-M5-011 开发机 S1 首次启动阶段证据（2026-08-19）

在全新隔离根 `H:\\pi-studybuddy-tmp\\runs\\T-M5-011\\startup-empty\\` 中，以真实 Electron 生产入口启动应用，并仅通过可见 UI 完成：创建学习计划、学期、课程、考试、考试确认、手工课表、任务创建和任务完成。关闭后第二次启动，通过可见 UI 展开学期和课程并读取考试与任务。

- 证据等级：E5（开发机可见 UI 主路径 + 开发机重启回读）。
- 结果：P0-001、P1-001 在本阶段的 S1 范围通过；页面状态与真实 SQLite 状态一致。
- 数据摘要：`global.db` 与学期 `sem.db` 的 `PRAGMA integrity_check` 均为 `ok`；`semesters.ready=1`；考试为 `confirmed`；任务为 `completed`。
- 隐私摘要：两次 UI DOM 均未发现完整 UUID、绝对路径或错误栈。
- 默认模型：空根生成 `config/pi-models.json` 默认目录；未配置 `models.json`/凭据时模型状态保持未配置，生产发送不得伪装成功，符合 `MODEL_NOT_CONFIGURED` 边界。真实模型发送未在本阶段连接外部服务。
- 运行器备注：首次证据运行器曾出现两个临时脚本选择器/闭包问题，均在隔离运行根内修复，不是生产代码断点，不进入仓库。


| ID | 问题 | 事实/证据 | 解决顺序 |
|---|---|---|---|
| P0-001 | 空数据首次启动必须能创建学习计划 | T-M5-001 发现入口缺失；T-M5-002 已部分修复；T-M5-011 在当前开发机空隔离根以真实 UI 创建学期/课程并重启回读通过（E5），S1 范围不再阻塞 | 1 |
| P0-002 | 默认模型必须可用，不能停在未配置或 fixture | T-M4-023/T-M4-024 已修部分生产路径；T-M5-011 空根确认默认 provider 目录可生成，未配置模型/凭据时生产状态明确为未配置且发送不得回退 fixture；真实凭据发送仍待受控开发机验证 | 1 |
| P0-003 | S2 必须完成“导入→转换→normalized_text→生成笔记→知识模块” | T-M1-002 曾只登记 Job；T-M4-025 只补真实 TextExtractor；generateNote 与模块进入练习仍需真实主路径验证 | 2 |
| P0-004 | `txt/md`、图片和旧 Office 路由不能只显示允许上传 | `materials.ts` 已允许多类格式；生产 OCR/WPS 尚未完整注入 | 2、5 |
| P0-005 | 生产不能依赖 fixture、固定会话、固定错题或静默失败 | T-M5-001/T-M5-003 已发现并部分修复；需按当前 master 复跑全路径 | 1、3 |
| P0-006 | 开发机必须具备一套固定可复制运行资源 | 当前 toolchain installer 只发现/验证，旧部署脚本要求用户安装 Node/Python/npm/WPS | 1、5 |
| P0-007 | 安装包不能把缺少 Python/OCR/whisper/WPS 的半成品称为可用 | T-M5-001 依赖审计已登记未随包；M5-006 尚未执行 | 5、6 |

## 3. P1 业务闭环问题

| ID | 问题 | 事实/证据 | 解决顺序 |
|---|---|---|---|
| P1-001 | S1 学期、课程、考试、任务完成后首页必须显示真实状态并重启回读 | T-M5-011 在空隔离根真实 Electron UI 创建学期/课程/考试/课表/任务并完成任务；第二次启动可见 UI 回读，SQLite integrity_check=ok（E5） | 1 |
| P1-002 | S2 资料转换成功后笔记生成必须实际写入 structured_notes | T-M1-007 明确 AI 笔记不在范围；当前需补完整生产链 | 2 |
| P1-003 | S2 生成的知识模块必须能进入 S3 练习 | T-M5-004 曾补最小 modules.create；需验证当前生产数据链 | 2、3 |
| P1-004 | S3 作答→S4 错题→重做→薄弱点必须使用真实业务数据 | handler 和 E2E 存在，需开发机空数据真实走通 | 3 |
| P1-005 | S5 模拟考、速背卡、冲刺计划必须从真实课程/模块进入 | 空课程 FK 缺陷曾修复；需完整复跑 | 3 |
| P1-006 | S6 报告真实生成、冻结、投递和重启状态回读 | T-M5-010 交付 mock 投递状态；T-M5-005 原生 UAT仍未完成 | 4 |
| P1-007 | S7 WAV→whisper→可编辑文本→S2 资料→重启回读 | S7 handler/Adapter 存在；随包运行时与生产资源仍未完成 | 4、5 |
| P1-008 | TTS SAPI 朗读与“已复习”必须真实持久化 | T-M2/T-M4 已有状态和接线；原生回读仍是 T-M5-005 阻塞项 | 4 |
| P1-009 | 备份→恢复→业务数据完整性→重启回读 | T-M5-010 已有隔离原生 UAT；需在开发机当前基线复跑并纳入总路径 | 4 |

## 4. P1 工程与运行问题

| ID | 问题 | 事实/证据 | 解决顺序 |
|---|---|---|---|
| P1-010 | 文档状态互相矛盾 | `04-Todo` 已写 T-M5-010 done；`AGENTS.md`、`00`、`README`仍有旧计划/旧版本/旧阶段描述 | 立即治理修复 |
| P1-011 | README 仍要求用户安装 Node/Python/WPS/Git | 与“安装后可用”目标冲突 | 立即治理修复 |
| P1-012 | 旧版安装/部署文档和当前 Electron 拓扑混杂 | 上一版 bootstrap 脚本要求 npm ci、Python、OCR venv；当前产品未形成单一启动入口 | 1、5 |
| P1-013 | toolchain installer 名称含 install，但实际不安装 | 当前实现固定返回 `INSTALLER_UNAVAILABLE` | 1、5 |
| P1-014 | 设置页仍混入开发诊断和工具链内容 | T-M5-010 已修部分真实性；产品入口仍需收敛为必要配置和默认值 | 1 |
| P1-015 | 测试通过与用户功能完成的口径混淆 | 大量测试使用 mock、fixture、Job 登记；总账必须以真实动作结果为准 | 全程 |

## 5. P2 格式与能力边界

| ID | 问题 | 处理 |
|---|---|---|
| P2-001 | PDF/DOCX/PPTX/XLSX/TXT/MD | 作为核心格式，开发机优先跑通真实转换和笔记生成 |
| P2-002 | PNG/JPG/JPEG/BMP/GIF/WEBP | 作为图片格式，开发机接入 RapidOCR 后跑通导入→文本→笔记 |
| P2-003 | DOC/PPT/XLS | 通过 WPS COM 转 DOCX/PPTX/XLSX，再进入统一文本提取和笔记流程；WPS 未完成开发机真实主路径前不得宣称完成 |
| P2-004 | ODT/ODS/ODP/RTF/EPUB/TIFF | 本轮明确不做，不阻塞核心交付 |

## 6. WPS 当前问题

WPS 桥本身已有 `pywin32 + KWPS/KET/KWPP.Application`、子进程隔离、JSON 协议和开发机单件证据；但仍缺：生产 `S2Context` 注入、固定 WPS/Python 路径、转换后笔记主路径、超时/残留进程处理的开发机系统验证。WPS 不是核心格式引擎，只有旧 Office 文件依赖它。

整改要求：先让核心格式无 WPS 完整跑通；随后在当前开发机实际选择 DOC/PPT/XLS，完成 WPS 转换、normalized_text、笔记生成和重启回读；最后复制相同 WPS/桥配置到使用机。不能用 mock 或“Job 已登记”替代。

## 7. DeepSeek 生成系统的主要缺陷归因

这里的批评针对当前代码和交付方法，不是对模型人格化评价。

1. **擅长局部实现，不擅长系统闭环**：能快速生成 handler、DTO、Adapter 和测试，但常把“方法存在”“Job 写入”“组件接口完成”误当成用户动作完成。
2. **过度遵从局部任务边界**：每个任务都按计划完成，结果跨任务链路断开，例如转换任务完成了 `normalized_texts`，笔记生成仍只是登记 Job，生产注入后来才补。
3. **测试替代真实运行**：大量 mock、fixture、合成夹具和静态渲染测试让质量门变绿，但早期真实安装审计仍发现 fixture、固定 ID、空数据入口缺失和依赖未随包。
4. **容易制造“完成叙事”**：版本历史、任务统计和实施记录非常完整，反而掩盖“安装后孩子能否完成任务”这个最终问题。
5. **设计过度工程化**：五阶段治理、多个里程碑、几十个任务、独立审查、复杂计划和多套追溯资产，增加了管理精度，却没有优先保证最短主路径能运行。
6. **外部依赖边界判断滞后**：WPS、OCR、whisper、真实模型、SMTP/飞书等被分别做成 Adapter 或 mock，但没有早期统一解决“运行资源从哪里来、谁负责安装、怎么在当前机器跑通”。
7. **默认用户视角不足**：产品定位写的是孩子，但实现入口长期偏向开发者配置、工具链健康、模型目录和诊断状态；README 甚至仍要求用户安装开发环境。
8. **状态一致性维护弱**：代码、计划、04-Todo、00 索引、AGENTS、README 多次出现版本和当前任务描述不同步，说明它更重视追加记录，不擅长回收旧事实。
9. **容易追求覆盖率和数量**：测试文件数、测试用例数、contract 数成为显眼成果，但没有等价强调“从空数据导入真实资料到笔记再到练习”的单条验收结果。
10. **缺少发布闭环意识**：setup 能安装、Electron 能启动不等于产品可用；随包资源、许可、默认配置、升级、恢复和目标机复制被推迟到后期。

## 8. 唯一整改路径

```text
当前开发机清空隔离数据根
→ 启动应用
→ 默认配置/模型可用
→ 创建学期/课程/考试/任务
→ 导入 PDF/DOCX/PPTX/XLSX/TXT/MD/图片
→ 转换并写 normalized_texts
→ 生成笔记/知识模块
→ 练习/错题/重做/冲刺
→ 报告/备份/TTS/课堂转写
→ 重启逐项回读
→ 固化运行资源、默认配置和启动方式
→ 用同一拓扑复制到使用机
→ 重新构建最终安装包
```

任何一步失败都回到当前开发机修复，不进入下一步。`04-Todo` 的历史 done 状态不删除，但以本表的真实闭环状态作为当前交付判断。
