# 当前任务

**状态**：✅ T-M3-007 已完成（04-Todo v0.1.63 登记 done，待 git 收尾）
**日期**：2026-08-08
**里程碑**：M3 对话与打磨（done 7/8：T-M3-001/002/003/004/005/006/007）
**任务（已交付）**：T-M3-007 E2E-10~13 对话承载 E2E
**计划文件**：[.plan/T-M3-007-e2e-chat.md](./T-M3-007-e2e-chat.md)（✅ 已批准，实施完成，保留为验收证据）
**上一任务**：T-M3-006（✅ 已完成，[实施记录](../.record/T-M3-006-实施记录.md)）

> T-M3-007 已实施完成（2026-08-08）：E2E-10~13 对话承载 E2E 落地——test-main.js 装配 createAgentHandlers/createSessionHandlers/createModelHandlers/createFileHandlers（files.read @引用白名单）+ test.turnEndIndex 直调生产 indexTurnEndChunks + eventForwardServer shim（IPC {"type":"event","topic","payload"}）+ RpcDriver waitForEvent 订阅辅助 + electron-launcher E2E_RUN_DIR→runs\T-M3-007\ + reuseDataRoot 二次 launch（L3 跨进程持久化）+ 4 E2E 文件（E10 默认主入口+流式事件序列+防泄露 / E11 触发词事件对+toolJumpTarget 映射+sessionMeta 写回 / E12 @引用 allowed-roots+越权拒绝+TTS+标记已复习 / E13 turn_end 幂等索引+跨进程持久化+search 无 UUID）+ 三处承载层根因修复（global.sql.ts 幂等建表 / indexer.ts 建父目录 / turn-end.ts 幂等计数）。质量门全通过：type-check + 966 单元/集成测试 + 110 E2E 测试（+27）+ build + smoke 6/6 + verify 全绿 + 文档治理 + 契约覆盖 127 handlers + 安全不变量 6/6 + UUID 泄漏 7/7。04-Todo v0.1.63 + 00-索引 v0.1.69 + AGENTS.md v0.1.49 同步。M3 done 7/8。

> T-M3-002 已实施完成（testing，2026-08-08）：五大承载点全部落地——AgentEvent payload 结构化（tool_call/tool_result 脱敏载荷）+ 工具调用卡片视图（toolCallId 配对 + ✅/⚠️ 状态）+ 上下文压缩提示条 + @文件引用选择器（materials.list 数据源 + allowed-roots 白名单门禁 + 1MB 截断 + 渲染前 UUID 二次脱敏）+ 多模型切换（models.list 受控 fixture handler，不读真实 ~/.pi/agent）+ 流式块渲染增强。06-API v0.1.3 同步（§4 payload 结构化 + §3.2 files.read 落地注解 + §3.1.1 agent.send 扩展注解）。质量门全通过：type-check + 856 单元/集成测试（+33）+ 83 E2E + build + smoke 6/6 + verify 全绿 + 文档治理 + 契约覆盖 + 安全不变量 6/6 + UUID 泄漏 7/7。04-Todo v0.1.53 + 00-索引 v0.1.59 + AGENTS.md v0.1.39 同步。M3 done 2/8。

> T-M3-003 已实施完成（testing，2026-08-08）：学习场景业务化五大承载点全部落地——L3 承载层（src/data/l3/ bigram.ts 分词器 CJK bigram + ASCII 整词 + 完整 UUID 剥离 + indexer.ts 幂等建表/写入 + search.ts OR-combined MATCH）+ L1 画像写回（src/data/l1-profile.ts 原子写）+ context-pack 扩展（学科/目标/错题段注入，错题只含错因摘要白名单）+ 会话级元数据（SessionSummary subject/goal/mistakeIds + session-store updateMeta，不新增 RPC 方法）+ sessions.search handler（L3 检索 + 内存仓库映射）+ agent.send sessionMeta 扩展（[学习上下文] token 同步注入 + 元数据写回）+ ChatTab 学习场景元数据条 UI（📐 学科/目标/错题 #chip）。06-API v0.1.4 同步（§3.1 sessions.search 落地 + SessionSummary 扩展 + §3.1.1 agent.send 扩展注解）。质量门全通过：type-check + 892 单元/集成测试（+36）+ 83 E2E + build + smoke 6/6 + verify 全绿 + 文档治理 + 契约覆盖 127 handlers + 安全不变量 6/6 + UUID 泄漏 7/7。04-Todo v0.1.55 + 00-索引 v0.1.61 + AGENTS.md v0.1.41 同步。M3 done 3/8。

## 下一步

- T-M3-007 已实施完成（04-Todo v0.1.63 登记 done），等待用户授权 git 收尾（显式 git add + commit + ff-only 合并 + 推送 origin/master）
- 下一任务候选：T-M3-008（E2E-01~13 全链回归 + 安全不变量最终校验，M3 收官，待 T-M3-007 收尾后用户选择）

## 单一执行任务门禁（AGENTS.md §4.4）

`.plan/` 同一时刻只允许存在一个**正在执行**的详细任务计划。当前任务 T-M3-007 已实施完成待收尾；新任务计划待用户选择 T-M3-008 后创建。

## 历史

- 2026-08-08（T-M3-007 实施完成待提交）：E2E-10~13 对话承载 E2E 落地——test-main.js 装配 createAgentHandlers/createSessionHandlers/createModelHandlers/createFileHandlers（files.read @引用白名单）+ test.turnEndIndex 直调生产 indexTurnEndChunks + eventForwardServer shim + RpcDriver waitForEvent + electron-launcher reuseDataRoot 二次 launch（L3 跨进程持久化）+ 4 E2E 文件（E10~E13）+ 三处承载层根因修复（global.sql.ts 幂等建表 / indexer.ts 建父目录 / turn-end.ts 幂等计数）。110 E2E（+27）+ 966 单元/集成全绿，verify 全绿 + 文档治理 + 契约覆盖 + 安全 6/6 + UUID 7/7。04-Todo v0.1.63 + 00-索引 v0.1.69 + AGENTS.md v0.1.49 同步。M3 done 7/8（T-M3-008 剩余）。
- 2026-08-08（T-M3-005 候选计划登记）：用户批准方案 (B) 落盘开工 Prompt + 提交四裁决问题。已落盘 Prompt 到 H:\pi-studybuddy-tmp\prompts\T-M3-005-start-prompt.md（仓库外，不污染 .plan/，不进 Git）。四裁决定案（均选推荐）：① 落点=业务数据根 config/models.json（managed 标记 + 原子写 + 测试隔离，同步修订 4 文档加 supersedes）② turn_end 源=assistant + tool 不读 ~/.pi（复合键 sessionId:turnIndex:role:seq 幂等 + max(last_offset) 增量）③ handler 范围=modelsConfig.get/set 两个（test/addProvider/probe 留后续）④ 写文件分工=各自写同一文件共用 model-config 模块（单写进程无并发）。已起草 .plan/T-M3-005-model-select-turn-end.md（草案，待用户批准开工，未登记 in_progress，不涉及业务施工）。核心冲突：03-Arch §2.3 / 08-Test §4.2 / 06-API §3.13 / 09-UI §9.2 四文档写 ~/.pi/agent/models.json vs AGENTS.md §9.5 物理隔离——裁决 1 改业务数据根解决。
- 2026-08-08（T-M3-004 实施完成待提交）：四大承载点落地——工具→Tab 映射纯函数（src/renderer/tool-tab-map.ts，35 工具全覆盖 + tts/backup 域无目标）+ 受控发射触发词扩展（3→10 组按域分组覆盖 35 工具域）+ ChatTab 跳转承载（onNavigateTab prop + done 卡片 [去<Tab名>] 按钮 data-tab 属性）+ AppShell 接线（renderTab 参数注入 setActiveTabId）。07-WF v0.1.2 §2.8 衔接段映射表条款（裁决 1b 升格权威）。33 新增测试（14+11+8），925 单元/集成 + 83 E2E 全绿，verify 全绿 + 文档治理 + 契约覆盖 127 + 安全 6/6 + UUID 7/7。07-WF v0.1.2 + 00-索引 v0.1.63 + AGENTS.md v0.1.43 同步。M3 done 3/8（T-M3-004 待提交后 4/8）。用户批准五裁决（① 工具→Tab 映射表 35 工具全覆盖：S3→practice/S2 笔记→notes/S2 资料→materials/S2 update_learn_status→notes/S4→mistakes/S5→cram/S6→report/S7→capture/S1+ocr→home/TTS 无跳转；1a backup_* 无目标 Tab（TabBar 仅 9 Tab 无 backup）不渲染跳转按钮留 T-M3-006；1b 映射表条款补 07-WF §2.8 ② 触发词按域分组（每域 1-2 触发词 + 既有 3 触发词无回归）③ 跳转按钮统一 [去<Tab名>] ④ 跳转 context { tabId, sessionId?, courseId? } 脱敏 ⑤ 受控发射扩展测试确定性）。04-Todo v0.1.56 登记 T-M3-004 in_progress + §9 统计 M3 pending 5→4 + in_progress 0→1。00-索引 v0.1.62 + AGENTS.md v0.1.42 同步。M3 done 3/8。步骤 1 核实完成——① L3 边界：承载层（bigram 分词/写入/检索/sessions.search handler）归 T-M3-003，turn_end 钩子接线归 T-M3-005（05-ERD §4.3 触发点 vs 承载能力为不同交付物，无 supersedes）② sessions.search 落点 L3 检索库；rename/export 留 T-M3-006 ③ L1 写回：preferred_subjects/goals 现成字段，version 1.0 不变，原子写 ④ 错题关联：会话级元数据（session-store Session 扩展），不新增表 ⑤ bigram：CJK bigram + ASCII 整词小写，完整 UUID 不索引。用户批准五裁决。已起草 .plan/T-M3-003-study-context.md（待批准开工，未登记 in_progress）。
- 2026-08-08（T-M3-002 实施完成待提交）：五大承载点落地：AgentEvent payload 结构化（tool_call/tool_result 脱敏载荷，字段子集对齐 pi 底座 ToolCallEvent/ToolResultEvent）+ allowed-roots.ts 白名单纯函数（realpath 防符号链接逃逸）+ files.read handler（现成契约 + 1MB 截断 + 相对 storageKey 解析）+ agent-host 受控发射扩展（触发词「出题/笔记/朗读」→ tool_call/tool_result 事件对）+ models.list handler（受控 fixture，不读真实 ~/.pi/agent）+ ChatTab 工具卡片/压缩提示条/模型选择器/@选择器（渲染前 UUID 二次脱敏）。06-API v0.1.3 同步（§4 payload 结构化 + §3.2 files.read 落地注解 + §3.1.1 agent.send 扩展注解）。856 单元/集成 + 83 E2E 测试全绿，verify 全绿。文档登记同步 04-Todo v0.1.53 + 00-索引 v0.1.59 + AGENTS.md v0.1.39。M3 done 2/8。
- 2026-08-08（T-M3-001 实施完成待提交）：ChatTab 组件替换 EmptyState（欢迎语「🤖 你好，今天想学点什么？」/消息输入/消息列表/会话列表）+ agent.send 新契约（renderer→agent-host→Streams["agent.events"] 受控序列 message_start→token×6→context_compressed）+ sessions.* 最小 handler（list/get/delete/context，内存仓库，不读真实 ~/.pi/agent）+ AppShell chat 分支接线 + 06-API v0.1.2 新增 agent.send。质量门全通过：type-check + 823 单元/集成测试（+21）+ 83 E2E + build + smoke 6/6 + verify 全绿 + 文档治理 + 契约覆盖 + 安全不变量 6/6。04-Todo v0.1.51 + 00-索引 v0.1.57 + AGENTS.md v0.1.37 同步。M3 done 1/8。
- 2026-08-08（T-M3-001 候选计划登记）：用户要求为 M3 首项任务准备充足上下文的开工 Prompt。已核实工程现状（chat 为 DEFAULT_TAB_ID 但渲染 EmptyState；AgentEvent 5 种 kind；Streams 9 主题；sessions.* 契约已定义但 agent-host handlers 无实现；rpc-client 已具备 subscribe；35 工具 + 4 钩子就绪）。起草 .plan/T-M3-001-chat-tab.md（待批准，不涉及业务施工）。
- 2026-08-08（T-M2-007 实施完成待提交）：whisper.cpp 真实 Adapter 实施完成，替换 mock。`createStudyBuddyExtension` 增加可选 whisper 配置（StudyBuddyExtensionOptions.whisperCliPath/whisperModelPath，优先级调用参数 > 环境变量 > 空默认 mock），有 cliPath+modelPath 装配 `createRealWhisperAdapter` 接入 S7Context，无则默认 mock（08-Test §5.4）。真实转写单件测试（合成 3s 正弦波 PCM WAV + 真实 whisper-cli -nt，探测 whisper-cli+ggml-base.bin 存在才跑，只断言 text 非空+无泄漏字段）+ 装配测试（带 whisper 路径 setup 不抛错+工具数仍 35）。802 单元/集成 + 83 E2E 测试全绿，verify 全绿。文档登记同步 04-Todo v0.1.49 + 00-索引 v0.1.55 + AGENTS.md v0.1.35。M2 全部 9 任务完成。
- 2026-08-08（T-M2-007 开工登记）：用户批准 T-M2-007 whisper.cpp 真实 Adapter 开工。计划 .plan/T-M2-007-whisper-adapter.md 已创建。前置依赖 T-M2-003 done（WhisperCppAdapter 三态 + wav-validator 已实现）。whisper.cpp CLI v1.9.1 真实验证通过（-nt 模式 stdout 即纯文本）。配置：调用参数 > 环境变量 > 空（默认 mock）。文档同步 04-Todo v0.1.48 + 00-索引 v0.1.54 + AGENTS.md v0.1.34。
- 2026-08-08（T-M2-006 实施完成待提交）：UUID 泄漏检测独立校验脚本 scripts/check-uuid-leak.mjs 实施完成，7 条静态断言（UUID-01~07）审计 leak-detector/reports/errors/types 布线，仿 check-desktop-security.mjs 硬断言范式；--src 覆盖源根 + readSource 优雅降级；tests/e2e/check-uuid-leak.script.test.ts 脚本冒烟 3 用例。799 单元/集成 + 83 E2E 测试全绿，type-check + build + smoke 6/6 + 文档治理通过。文档登记同步 04-Todo v0.1.47 + 00-索引 v0.1.53 + AGENTS.md v0.1.33。步骤 16 提交已完成（master 5fe2aed + origin/master 推送成功）。
- 2026-08-08（T-M1-008 实施完成待提交）：跨切钩子实施完成，4 个 pi.on 生命周期钩子（before_agent_start 多源上下文注入 / session_start 初始化学期库 / tool_call workspace-path-guard 拦截 / tool_result 集中错误日志）。799 单元/集成 + 80 E2E 测试全绿，verify 全绿。M1 全部 10 任务完成。
- 2026-08-08（T-M1-010 实施完成待提交）：E2E-01~03 实施完成，3 E2E 文件 36 测试 + child_process.fork + Node.js IPC + 防泄露铁律 + 幂等归档 + 薄弱点 evidence_count≥2 + 规则批改可证伪 + M1 退出门槛全通过。722 单元/集成 + 36 E2E 测试全绿，verify full 阶段全通过。
- 2026-08-08（T-M1-010 计划创建）：T-M1-010 E2E-01~03 计划创建，用户批准 vitest + Electron 方案。08-Test v0.1.2 同步框架选择变更。
- 2026-08-08（T-M2-008 正式完成）：09-UI S5-S7+TTS+备份恢复 UI 已正式完成（§8.4 三者齐全），master c94650c + origin/master 推送成功。722 测试全绿（54 test files）。M2 done 6/9。
- 2026-08-08（T-M2-005 正式完成）：备份恢复已正式完成（§8.4 三者齐全），master be2401a + origin/master 推送成功。598 测试全绿（43 test files）。M2 业务 Adapter 层全部完成。
- 2026-08-08（T-M2-004 实施完成待提交）：TTS skill 实施完成，4 RPC handler + 3 studybuddy_* 工具 + TtsContext 双引擎可注入 + SAPI 默认 + edge-tts 失败降级 + 朗读状态机 + Streams["tts.state"] 推送 + 无独立 TTS 表 + 不写 StudyEvent + 全 mock。562 测试全绿（38 test files）。
- 2026-08-07（T-M2-003 正式完成）：master 0cc3915 + origin/master 推送成功（§8.4 三者齐全）。S7 课堂采集 2 RPC handler + 2 工具 + WhisperCppAdapter 可注入 + PCM WAV 字节级验证 + handoff 到 S2。509 测试全绿（34 test files）。
- 2026-08-07（T-M2-002 实施完成）：S6 家长报告 11 RPC handler + 3 工具 + 规则生成 + AI 仅润色降级 + UUID 泄漏检测 + 渠道独立失败隔离。455 测试全绿。
- 2026-08-07（T-M2-001 实施完成）：S5 期末冲刺 8 RPC handler + 2 工具 + MockExamGenerator 可注入 + source_hash 防重复。420 测试全绿。
- 2026-08-07（T-M1-004 完成 + 推送）：S4 错题/薄弱点 10 RPC handler + 4 工具。383 测试全绿。commit 9118c60。
- 2026-08-07（T-M1-003 完成 + 推送）：S3 限时练习 5 RPC handler + 3 工具 + QuestionGenerator 可注入 + grader 三策略。336 测试全绿。commit f864e4c。
- 2026-08-07（T-M1-002 完成 + 推送）：S2 资料/笔记/知识模块 17 RPC handler + 6 工具。295 测试全绿。commit 939f6fe。
- 2026-08-07（T-M1-001 完成 + 推送）：S1 学习节奏 25 RPC handler + 6 工具。237 测试全绿。commit addec03。
- 2026-08-07（T-M0-009 完成 + 推送）：M0 系统冒烟完整（M0 收官）。commit 378aa7d。
- 2026-08-07（T-M0-008 完成 + 推送）：09-UI 三栏布局 + 标签页骨架。commit 034969c。
- 2026-08-07（T-M0-007 完成）：studybuddy-extension 空壳。
- 2026-08-07（T-M0-005 完成 + 提交）：file-watch。commit 47a2357。
- 2026-08-07（T-M0-004 完成）：toolchain 框架。
- 2026-08-07（T-M0-003 完成）：credential-vault。
- 2026-08-07（T-M0-006 完成）：数据层 schema。
- 2026-08-07（T-M0-002 完成）：contract 类型化契约面。
- 2026-08-07（T-M0-001 完成）：四进程骨架 + 自研 RPC + 最小 contract。
