# 当前任务

**状态**：▶️ 执行中（T-M0-003 credential-vault，safeStorage/DPAPI 密钥库）
**日期**：2026-08-07
**里程碑**：M0 骨架搭建

> 当前 `.plan/` 唯一执行中任务：T-M0-003 credential-vault。

## 单一执行任务门禁（AGENTS.md §4.4）

`.plan/` 同一时刻只允许存在一个**正在执行**的详细任务计划。

创建新计划的三项前置条件（必须同时满足）：
1. ✅ 上一项任务已完成（T-M0-006 已完成）
2. ✅ 用户已明确选择该任务并批准开工（T-M0-003 credential-vault）
3. ✅ 该任务即将进入实施

**前置门禁检查**：
- ✅ `.plan/` 无其他执行中任务
- ✅ T-M0-006 已实现（04-Todo 证据 + 实施记录）
- ✅ 本任务计划已获批（T-M0-003 关联文档：03-Arch §4.5 + 08-Test §5.6-§5.7(INV-04) + 06-API §3.15 + 01-TRD §9.2 + 05-ERD §2）

## 历史

- 2026-08-07（T-M0-003 执行中）：credential-vault（safeStorage/DPAPI 密钥库）。权威条款：03-Arch §4.5（`import { safeStorage } from "electron"` + `isEncryptionAvailable()` 校验 + `encryptString`/`decryptString` + 写文件 mode 0o600 原子写 temp+rename + 键格式 `/^modelProvider:[a-z0-9._-]{1,160}$/i` 与 `/^parentContact:[a-z0-9._-]{1,160}$/i`）、08-Test §5.6（往返一致/mode 0o600/键名校验）、08-Test §5.7 INV-04（`check(usesSafeStorage, "credential-vault 用 safeStorage")`）、06-API §3.15（credentials.set/get/delete/listKeys）、01-TRD §9.2（真实密钥仅本机 DPAPI 存储）。落地：`src/main/credential-vault.ts` + `tests/unit/credential-vault.test.ts` + 补全 check-desktop-security.mjs INV-04 占位。
- 2026-08-07（T-M0-006 完成）：数据层 schema——`global.db` 4 表 + 8 索引、`semester.db` 25 表 + 9 触发器、三层记忆（L1 画像 JSON / L2 知识库索引骨架 / L3 会话 FTS5），统一 PRAGMA（WAL/foreign_keys ON）建库后 integrity_check 通过。`node:sqlite` 经 `src/data/sqlite.ts` 用 `process.getBuiltinModule` 动态加载，规避 esbuild/vite-node transform 剥离 `node:` 前缀。单件 13 + 集成 4 测试全绿，`pnpm verify` 全绿（执行 7，跳过 2）。详见 [.record/T-M0-006-实施记录.md](../.record/T-M0-006-实施记录.md)。
- 2026-08-07（T-M0-002 完成）：contract 类型化契约面补全——`api.ts` 126 方法（06-API §3 全集）、`types.ts` DTO + Envelope + ErrorCode、`streams.ts` 9 主题（06-API §4）、`desktop.ts` PiBridge 8 桥面；`preload.ts` 同步转发新增桥面方法。类型契约测试 25 断言全绿（RED→GREEN），`pnpm verify` 全绿（执行 7，跳过 2）；INV-05（Host RPC 契约化）落地；contract-coverage missing→警告。详见 [.record/T-M0-002-实施记录.md](../.record/T-M0-002-实施记录.md)。
- 2026-08-07（T-M0-001 完成）：四进程骨架 + 自研 RPC + 最小 contract 实现并验证——12 测试全绿、build 通过、smoke 通过、安全不变量 3 条全绿。详见 [.record/T-M0-001-实施记录.md](../.record/T-M0-001-实施记录.md)。

## 下一步

完成 T-M0-003 credential-vault 后，等待用户选择 M0 下一任务并批准开工。

## 候选任务（仅名称登记，不预写计划）

| task-id | 标题 | 里程碑 | 状态 |
|---|---|---|---|
| T-M0-004 | toolchain 发现-探测-安装-绝对路径 | M0 | 📝 候选 |
| T-M0-005 | file-watch（fs.watch + 防抖） | M0 | 📝 候选 |
| T-M0-007 | studybuddy-extension 空壳 | M0 | 📝 候选 |
| T-M0-008 | 09-UI 三栏布局 + 标签页骨架 | M0 | 📝 候选 |
| T-M0-009 | M0 系统冒烟完整 | M0 | 📝 候选 |

> 其余 M0-M3 任务大纲详见 [docs/04-Todo §7](../docs/04-任务清单-Todo-List.md)