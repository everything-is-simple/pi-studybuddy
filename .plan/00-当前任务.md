# 当前任务

**任务 ID**：T-M4-014
**计划文件**：.plan/T-M4-014-s4-mistakes-rpc.md
**状态**：in_progress（本地实现、双维度独立审查与质量门已完成；Git 收口待用户单独授权）
**日期**：2026-08-11
**里程碑**：M4 业务接线 + 打包部署
**标题**：S4 错题 Tab RPC 接线（list + confirmErrorCause + redo + weakPoints）
**实施分支**：agent/T-M4-014-s4-mistakes-rpc
**集成分支**：master
**集成基线**：master=origin/master=7d93560

## 当前门禁

- [x] T-M4-013 已完成治理事实收口，`docs/04-Todo` 已登记 done，master=origin/master=7d93560。
- [x] 用户已明确选择本轮仅实施 T-M4-014。
- [x] 已创建唯一执行计划与隔离分支 `agent/T-M4-014-s4-mistakes-rpc`。
- [x] 已按 TDD 完成 RED：T-M4-014 integration 初次 5/5 失败。
- [x] 已完成 GREEN 与最小 REFACTOR：MistakesTab 接通既有 S4 RPC。
- [x] 已完成定向 unit/integration 与真实 Electron renderer E2E。
- [x] Node24/pnpm11 完整质量门 `pnpm verify -- --stage=full` 通过。
- [x] 已完成双维度独立审查，P0/P1 无遗留；审查结论已写入实施记录。
- [x] 已创建 `.record/T-M4-014-实施记录.md`（8 章节）。
- [x] 已同步 `docs/04-Todo` 与 `docs/00-文档索引-Index.md`，任务保持 in_progress。
- [ ] 用户明确授权 Git 收口后，才可执行 add/commit/merge/push；当前不得执行。

## 范围约束

本任务仅接通 S4 MistakesTab renderer/UI：

- 复用既有 `mistakes.list`、`mistakes.get`、`mistakes.suggestErrorCause`、`mistakes.confirmErrorCause`、`mistakes.redo`、`weakPoints.list`。
- 当前课程下加载错题与薄弱点；错题详情由学生在本 Tab 局部显式选择；不新增 AppShell 跨 Tab 模块状态。
- AI 建议固定标记“仅供参考”，学生确认后才调用确认 mutation。
- 维持课程/错题切换竞态保护、卸载保护、重复 mutation 防护、错误净化与 archived 只读双层边界。
- 不新增或修改 API contract、handler、schema、AppShell 全局状态；不启动 T-M4-015~021。

## 测试隔离

所有测试数据、日志与 Electron 运行产物必须位于：

`H:\pi-studybuddy-tmp\runs\T-M4-014\`

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`，不得连接真实 AI、SMTP、飞书、WPS COM、whisper.cpp 或其他外部服务。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获实施与治理证据同步授权，未获 Git 收口授权；不得混入本工作区既有用户 dirty 修改。
