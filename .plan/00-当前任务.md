# 当前任务

**任务 ID**：T-M4-025
**计划文件**：.plan/T-M4-025-s2-extractor-prod.md
**状态**：in_progress（计划 ✅ 已批准，进入 RED→GREEN 实施）
**日期**：2026-08-11
**里程碑**：M4 业务接线 + 打包部署（缺陷修复）
**标题**：生产 S2Context 注入真实 TextExtractor（materials.convert 生产可用）
**实施分支**：agent/T-M4-025-s2-extractor-prod（待计划批准后建立）
**集成分支**：master
**集成基线**：master=origin/master=329b83d（T-M4-020 收尾登记事实核验）

## 当前门禁

- [x] T-M4-020 已完成治理事实收口，`docs/04-Todo` v0.1.142 已登记 done，master=origin/master=329b83d 已核验。
- [x] 用户明确指令解决生产 S2Context extractor 缺口（2026-08-11"然后我们再解决：生产 S2Context 未注入 extractor…"；T-M4-020 回归审查发现登记）。
- [x] 已创建唯一执行计划 `.plan/T-M4-025-s2-extractor-prod.md`（状态 📝 待审查）。
- [x] `.plan/` 无其他执行中任务（单一执行任务门禁满足；T-M4-021 待本任务收口后单独做计划）。
- [x] master 分支干净。
- [x] 步骤 4：独立审查计划。
- [x] 步骤 5：用户批准计划（2026-08-11“批准计划 T-M4-025”）。
- [x] 步骤 6：建立隔离分支 `agent/T-M4-025-s2-extractor-prod` 并进入 RED → GREEN 实施。
- [x] 步骤 7~15：TDD（materials E2E converted RED → 装配修复 + pdfjs 环境补齐 → GREEN）/ type-check / build / test（全量 118 files/1130 tests 不回归）/ smoke 6/6 + 真实 Electron E2E 28 files/136 tests / 独立审查（pdfjs utilityProcess 三层根因）/ 治理同步（04-Todo v0.1.144 + 00-索引 v0.1.148）/ 文档治理 / diff 检查均完成。
- [x] 步骤 16：Git 收口（用户明确授权 2026-08-11；网络恢复后推送成功）：功能提交 `00b7579` 与治理登记提交 `22ecdde`（及中间事实 `5359d17`）已快进进入 master，Node24 master 完整 `verify --stage=full` 复验通过（unit/integration 118 files/1130 tests、真实 Electron E2E 28 files/136 tests），`git push origin master` 成功并核验 `master=origin/master=5359d17`（远端 refs/heads/master 一致），任务登记为 done（docs/04 v0.1.147）。

## 范围约束

- `src/agent-host/index.ts`：`createBusinessHandlers` 中 `new S2Context(dataRoot, undefined, createRealTextExtractor())`（第 3 构造参数注入真实进程内 TextExtractor：pdf-parse/jszip/mammoth 本地库）。
- 影响：生产 materials.convert 的 convert_pdf/docx/pptx/xlsx 真实提取；成功→converted+normalized_texts；失败→conversion_failed+Job failed（固定文案）。
- `tests/e2e/t-m4-020-materials-renderer.test.ts` 升级：受控 PDF 夹具落盘 storageKey → 点击转换 → 断言状态"已转换"（真实完成）。
- 不注入 WPS/OCR（08-Test §5.4 外部组件 mock 边界，保持"仅登记 Job"既有语义）。
- 不新增/改 RPC API/handler/schema（contract 127/127 不变）。
- 不启动 T-M4-021。

## 测试隔离

所有测试数据、PDF 夹具、日志与 Electron 运行产物必须位于：

`H:\pi-studybuddy-tmp\runs\T-M4-025\`

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`，不得连接真实外部服务。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获任务选择与开工登记授权，未获计划批准与 Git 收口授权；不得混入本工作区既有用户 dirty 修改（未跟踪 pi-session html）。
