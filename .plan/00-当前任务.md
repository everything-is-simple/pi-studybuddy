# 当前任务

**任务 ID**：T-M4-016
**计划文件**：.plan/T-M4-016-s6-report-rpc.md
**状态**：done（本地实施、双维度独立审查、质量门与 Git 收口完成；master 集成与 origin/master 核验通过）
**日期**：2026-08-11
**里程碑**：M4 业务接线 + 打包部署
**标题**：S6 报告 Tab RPC 接线（reports + deliveries + reportTargets）
**实施分支**：agent/T-M4-016-s6-report-rpc
**集成分支**：master
**集成基线**：master=origin/master=3dcf93f（T-M4-024 Git 收口事实核验）

## 当前门禁

- [x] T-M4-024 已完成治理事实收口，`docs/04-Todo` v0.1.124 已登记 done，master=origin/master=3dcf93f 已核验。
- [x] 用户已明确选择并批准开工 T-M4-016（2026-08-11“开始 T-M4-016”）。
- [x] 已创建唯一执行计划 `.plan/T-M4-016-s6-report-rpc.md`；隔离分支 `agent/T-M4-016-s6-report-rpc` 待切换。
- [x] 步骤 4：独立审查计划（计划由实施者审阅后提交用户批准）。
- [x] 步骤 5：用户批准计划（2026-08-11“批准计划”）后进入 RED → GREEN → REFACTOR 实施。
- [x] RED：新增 T-M4-016 integration 失败用例（S6-RED-01~08/08b + host-boundaries 2），初次 9/9 失败（ReportTab 未接通 RPC / S6 host 缺 archived 防线），RED 证据已存 `H:\pi-studybuddy-tmp\runs\T-M4-016\red\red-initial-failure.log`。
- [x] GREEN/REFACTOR：ReportTab 接通既有 S6 RPC（生成/冻结/投递/重试/渠道状态可视化）；s6/lookup.ts 新增 `assertSemesterWritable` 并接入六写入口；定向 integration 11 tests + 既有 unit 回归 10 tests 全绿；type-check/build 通过。
- [x] 真实 Electron renderer E2E：t-m4-016-report-renderer 2 tests（主流程 + 归档只读）通过；运行根 `H:\pi-studybuddy-tmp\runs\T-M4-016\`。
- [x] Node24/pnpm11 完整质量门：unit/integration 115 files/1096 tests、真实 Electron E2E 21 files/126 tests、contract 127/127、security 6/6、smoke 6/6、UUID 7/7、docs 治理与 diff-check 通过。
- [x] 双维度独立审查，P0/P1 无遗留：审查者 A（功能/契约）确认 8 个既有 RPC 接线与六写入口防线；审查者 B（治理/安全）确认范围无越界、隐私净化/脱敏/质量门真实。
- [x] 创建 `.record/T-M4-016-实施记录.md`（8 章节）。
- [x] 同步 `docs/04-Todo`（v0.1.126）与 `docs/00-文档索引-Index.md`（v0.1.130），任务保持 in_progress。
- [x] Git 收口（2026-08-11“提交 推送 到远端 然后 合并”）：功能提交 `eb4becb` 与治理登记提交 `62fa21d` 已快进进入 master，Node24 master 完整 `verify --stage=full` 复验通过（unit/integration 115 files/1096 tests、真实 Electron E2E 21 files/126 tests），`git push origin master` 成功并核验 `master=origin/master=62fa21d`，任务登记为 done（docs/04 v0.1.127）。

## 范围约束

本任务仅接通 S6 报告 Tab renderer/UI：

- 复用既有 `reports.list/generate/freeze/get`、`deliveries.list/deliver/retry`、`reportTargets.list`（contract 保持 127/127，不新增 API/handler/schema）。
- 报告内容只有脱敏聚合（计数/状态/趋势/短标题/日期窗口）；绝不包含原文/题干/答案/作答/错因正文/完整 UUID/真实渠道地址。
- 冻结时 `assertNoSensitiveLeak` UUID 泄漏检测；投递渠道独立失败隔离，状态可视化（sent ✅ / failed ✗ / 未配置 ─）。
- `reportTargets.create/update/delete` 不纳入本轮接线（无 09-UI §4.9 表单 UI 依据，真实地址在 credential-vault）。
- 维持学期切换竞态保护、卸载保护、重复 mutation 防护、错误净化与 archived 只读双层边界（实施时核对 s6 handler host 侧防线，如缺失按 T-M4-015 先例补齐）。
- 不启动 T-M4-017~021。

## 测试隔离

所有测试数据、日志与 Electron 运行产物必须位于：

`H:\pi-studybuddy-tmp\runs\T-M4-016\`

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`，不得连接真实 AI、SMTP、飞书、WPS COM、whisper.cpp 或其他外部服务。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获实施与治理证据同步授权，未获 Git 收口授权；不得混入本工作区既有用户 dirty 修改（未跟踪 pi-session html）。
