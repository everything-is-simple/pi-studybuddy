# 当前任务

**状态**：✅ T-M2-005 备份恢复已正式完成（§8.4 三者齐全），无执行中任务
**日期**：2026-08-08
**里程碑**：M2 完整闭环（第 5 任务，M2 最后一个业务 Adapter）
**任务**：T-M2-005 备份恢复（zip 打包/解包 + content_hash + 恢复 + 调度）
**计划文件**：[.plan/T-M2-005-backup.md](./T-M2-005-backup.md)

> T-M2-005 备份恢复已正式完成（§8.4 三者齐全）：docs/04 v0.1.29 done + master cac82ce 复验通过 + origin/master 推送成功。598 测试全绿（43 test files），type-check + build + smoke 6/6 + 文档治理检查通过。M2 业务 Adapter 层全部完成（S5+S6+S7+TTS+备份恢复 共 5 任务）。
>
> T-M2-004 TTS skill 已正式完成：master 05a770d + origin/master 推送成功。

## 候选任务设计依据

- **04-Todo §6.4**（M2 范围含 TTS 跨子系统 + §7.3 M2 任务大纲 TTS 行）
- **07-WF §4**（TTS 朗读路径：随时可击发流程 4 步 + 场景化朗读 + 标记已复习 + 关键约束 4 条 + 错误处理 2 条）
- **06-API §3.10**（TTS 4 RPC 方法：tts.speak / tts.control / tts.switchEngine / tts.getStatus）
- **03-Arch §3.1**（TTS 3 工具：studybuddy_tts_speak / studybuddy_tts_control / studybuddy_tts_switch_engine + 无独立 TTS 表）
- **03-Arch §3.3**（外部桥 Adapter 契约：路径只来自配置、子进程调用、不猜路径不回退云端、路径/stdout/stderr/密钥不泄漏、固定错误码）
- **08-Test §3.5**（TTS skill 单测 3 断言 + §5.4 不连真实外部服务全 mock）
- **02-PRD §3.9**（TTS 跨子系统随时可击发核心特性）
- **09-UI §5**（TTS 朗读 UI：全局控制条 + 内嵌朗读按钮 + 状态反馈 + 标记已复习）

## 候选任务范围（仅名称 + 设计依据，不预写实施步骤，AGENTS.md §4.4）

**T-M2-004 TTS skill**

- **设计依据**（权威源，优先级 4）：
  - [07-Workflow §4](../docs/07-工作流-Workflow.md)：TTS 朗读路径（4 步流程 + 场景化朗读 + 标记已复习走 S1 StudyEvent practice_reviewed + 关键约束：无独立 TTS 表/封装为 skill/SAPI 默认离线/edge-tts 可选需网络 + 错误处理 2 条）
  - [06-API §3.10](../docs/06-API契约-API-Contracts.md)：4 RPC 方法（tts.speak / tts.control / tts.switchEngine / tts.getStatus）
  - [03-Arch §3.1](../docs/03-架构设计-Architecture-Design.md)：3 工具（studybuddy_tts_speak / studybuddy_tts_control / studybuddy_tts_switch_engine）+ 无独立 TTS 表（已复习走 S1 StudyEvent）
  - [03-Arch §3.3](../docs/03-架构设计-Architecture-Design.md)：外部桥 Adapter 契约（参照 whisper.cpp Adapter 范式：路径只来自配置、子进程调用、不泄漏、固定错误码）
  - [08-Test §3.5](../docs/08-测试验收-Test-Plan.md)：TTS skill 单测 3 断言（SAPI 默认返回 playbackId + edge-tts 失败降级 SAPI + 朗读不写 StudyEvent）+ §5.4 不连真实外部服务全 mock
  - [02-PRD §3.9](../docs/02-PRD-产品需求-Product-Requirements.md)：TTS 跨子系统随时可击发核心特性
  - [09-UI §5](../docs/09-使用者介面-UI-Design.md)：TTS 朗读 UI（本任务只做 RPC+工具，UI 在后续 09-UI 任务）
- **范围概述**（不含实施步骤）：
  - 4 RPC handler + 3 studybuddy_* 工具注册
  - TtsAdapter 可注入接口（mock/failing/real 三实现，参照 WhisperCppAdapter 范式）
  - SAPI COM 桥 Adapter（Windows 默认，powershell 子进程调用 System.Speech）
  - edge-tts 桥 Adapter（降级方案，子进程调用 edge-tts CLI）
  - 朗读状态机（idle → playing → paused → stopped）+ playbackId 生成 + 位置追踪
  - 跨子系统朗读 API（speak + control + switchEngine + getStatus）
  - Streams["tts.state"] 推送朗读状态（已在 streams.ts 就绪）
  - 错误降级（SAPI 失败 → INTERNAL_ERROR 固定文案；edge-tts 失败 → 自动降级 SAPI + fallbackUsed=true）
  - 安全：路径不泄漏 + stdout/stderr 不泄漏 + 不连真实外部服务（测试全 mock，08-Test §5.4）
- **不做什么**（07-WF §4.3 + 09-UI §5）：
  - 不做 TTS UI（全局控制条/内嵌按钮/状态反馈，留待 09-UI S5-S7+TTS 任务）
  - 不做"标记已复习"handler（走 S1 events.markReviewed，不在 TTS 范围）
  - 不持久化朗读内容（无独立 TTS 表，朗读是即时行为）
  - 不连真实 SAPI/edge-tts（测试全 mock）
- **治理阶段**：阶段 2-4（单件 → 集成 → 组装）
- **安全不变量**：不连真实 SAPI/edge-tts（mock）+ 路径不泄漏 + stdout/stderr 不泄漏 + 固定错误码

## 单一执行任务门禁（AGENTS.md §4.4）

`.plan/` 同一时刻只允许存在一个**正在执行**的详细任务计划。

创建新计划的三项前置条件（必须同时满足）：
1. ✅ T-M2-003 已正式完成（docs/04 v0.1.26 done + master 0cc3915 + origin/master 推送成功，§8.4 三者齐全）
2. ✅ 用户已明确批准 T-M2-004 TTS skill 开工（"按这个修正后的 prompt 继续执行 TTS 任务"）
3. ✅ 该任务即将进入实施（计划创建中，待用户审查批准计划后实施）

## 历史

- 2026-08-08（T-M2-005 正式完成）：备份恢复已正式完成（§8.4 三者齐全），master cac82ce + origin/master 推送成功。598 测试全绿（43 test files）。M2 业务 Adapter 层全部完成。
- 2026-08-08（T-M2-005 实施完成待提交）：备份恢复实施完成，7 RPC handler + 5 studybuddy_* 工具 + BackupContext + 手写最小 zip 格式（无外部依赖）+ content_hash=SHA-256 + schema_version 兼容 + 冲突处理 + PRAGMA integrity_check + zip 炸弹防护 + 符号链接逃逸防护 + backup_records 状态机 + Streams["backup.progress"] 推送 + 调度配置 CRUD + DTO 对齐 05-ERD §2.3/§2.4 + extension 接入 29→34 工具。598 测试全绿（43 test files）。
- 2026-08-08（T-M2-004 实施完成待提交）：TTS skill 实施完成，4 RPC handler + 3 studybuddy_* 工具 + TtsContext 双引擎可注入 + SAPI 默认 + edge-tts 失败降级 + 朗读状态机 + Streams["tts.state"] 推送 + 无独立 TTS 表 + 不写 StudyEvent + 全 mock。562 测试全绿（38 test files）。
- 2026-08-07（T-M2-004 候选登记）：T-M2-003 已正式完成（origin/master 0cc3915 推送成功），前置门禁通过，用户批准 T-M2-004 TTS skill 开工，登记候选 + 设计依据，创建详细计划。
- 2026-08-07（T-M2-003 正式完成）：master 0cc3915 + origin/master 推送成功（§8.4 三者齐全）。前置 .plan/00 标记"待推送"为过时信息，本次修正。S7 课堂采集 2 RPC handler + 2 工具 + WhisperCppAdapter 可注入 + PCM WAV 字节级验证 + handoff 到 S2。509 测试全绿（34 test files）。
- 2026-08-07（T-M2-003 实施完成）：S7 课堂采集实施完成，待用户授权提交推送。
- 2026-08-07（T-M2-002 实施完成）：S6 家长报告 11 RPC handler + 3 工具 + 规则生成 + AI 仅润色降级 + UUID 泄漏检测 + 渠道独立失败隔离。455 测试全绿。
- 2026-08-07（T-M2-001 正式完成）：master 9ddcaec + origin/master 推送成功（§8.4 三者齐全）。
- 2026-08-07（T-M2-001 实施完成）：S5 期末冲刺 8 RPC handler + 2 工具 + MockExamGenerator 可注入 + source_hash 防重复。420 测试全绿。
- 2026-08-07（T-M1-004 完成 + 推送）：S4 错题/薄弱点 10 RPC handler + 4 工具。383 测试全绿。commit 9118c60。
- 2026-08-07（T-M1-003 完成 + 推送）：S3 限时练习 5 RPC handler + 3 工具 + QuestionGenerator 可注入 + grader 三策略。336 测试全绿。commit f864e4c。
- 2026-08-07（T-M1-002 完成 + 推送）：S2 资料/笔记/知识模块 17 RPC handler + 6 工具。295 测试全绿。commit 939f6fe。
- 2026-08-07（T-M1-001 完成 + 推送）：S1 学习节奏 25 RPC handler + 6 工具。237 测试全绿。commit addec03。
- 2026-08-07（T-M0-009 完成 + 推送）：M0 系统冒烟完整（M0 收官）。commit 378aa7d。
- 2026-08-07（T-M0-008 完成 + 推送）：09-UI 三栏布局 + 标签页骨架。commit 034969c。
- 2026-08-07（T-M0-007 完成）：studybuddy-extension 空壳。
- 2026-08-07（T-M0-005 完成 + 提交）：file-watch。commit 47a2357。
- 2026-08-07（T-M0-004 完成）：toolchain 框架。
- 2026-08-07（T-M0-003 完成）：credential-vault。
- 2026-08-07（T-M0-006 完成）：数据层 schema。
- 2026-08-07（T-M0-002 完成）：contract 类型化契约面。
- 2026-08-07（T-M0-001 完成）：四进程骨架 + 自研 RPC + 最小 contract。

## 下一步

T-M2-005 备份恢复已正式完成，无执行中任务。M2 剩余任务（不预写计划，AGENTS.md §4.4）：S5-S7+TTS+备份恢复 UI / E2E-04~09。等待用户明确选择下一任务。
