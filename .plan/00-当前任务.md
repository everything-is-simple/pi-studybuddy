# 当前任务

**状态**：⚪ 无执行中任务
**日期**：2026-08-07
**说明**：设计阶段已闭环，治理体系已就绪，等待用户明确选择 M0 骨架首个任务并批准开工

## 单一执行任务门禁（AGENTS.md §4.4）

`.plan/` 同一时刻只允许存在一个**正在执行**的详细任务计划。

创建新计划的三项前置条件（必须同时满足）：
1. 上一项任务已完成并在 04-Todo 记录（或这是第一个任务）
2. 用户已明确选择该任务并批准开工
3. 该任务即将进入实施

**未选任务**只能在本文件作为候选名称出现，**不得**预写文件清单、命令、预期输出或实现步骤。

## 候选任务（仅名称登记，不预写计划）

| task-id | 标题 | 里程碑 | 状态 |
|---|---|---|---|
| T-M0-001 | Electron 四进程骨架（main + preload + renderer + agent-host） | M0 | 📝 候选，待用户批准 |

> 其余 M0-M3 任务大纲详见 [docs/04-Todo §7](../docs/04-任务清单-Todo-List.md)
