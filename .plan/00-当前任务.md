# 当前任务

**任务 ID**：T-M4-026
**计划文件**：.plan/T-M4-026-ai-provider-config.md
**状态**：已完成（本地实现、配置、完整验收与 Git 收口完成）
**日期**：2026-08-12
**里程碑**：M4 业务接线 + AI provider 配置扩展
**标题**：AI 多 provider、多模型与 DPAPI 凭证配置
**实施分支**：agent/T-M4-026-ai-provider-config
**集成分支**：master
**集成基线**：master=origin/master（T-M4-021 收口后）

## 当前门禁

- [x] 用户明确要求配置 `H:\pi-references` 中的原站/中转站凭证与模型分类。
- [x] T-M4-026 已在 `docs/04-Todo` 登记，唯一执行计划已建立；未启动其他任务。
- [x] 7 个 provider key 已通过 Electron 主进程 safeStorage/Windows DPAPI 写入本机 vault。
- [x] 已配置 7 个 provider catalog；默认模型为 `agnes:agnes-2.5-flash`。
- [x] 文本、图像输入多模态聊天、图像/视频生成模型已分类；生成模型未进入 ChatTab 选择器。
- [x] 模型切换先创建生产 pi session，成功后才更新默认配置；失败保留旧 session 与配置。
- [x] 定向 4 files/17 tests、完整 `verify --stage=full`、本机配置不变量、文档治理和 diff 检查均通过。
- [x] `.record/T-M4-026-实施记录.md` 已建立。
- [x] Git commit/merge/push：已获用户授权并完成；功能提交 `10d50eb`，治理登记提交 `c3d2db3`，两者已快进合并到 master，Node24 master 完整质量门复验通过；网络恢复后 `origin/master` 已推送并核验 `master=origin/master=869de2f`。

## 范围约束

- 凭证仅存 `%LOCALAPPDATA%\PiStudyBuddy\config\credentials.json` 的 DPAPI 密文条目；不写入仓库、日志或模型 catalog。
- 已知 provider：DeepSeek、火山方舟、云雾 API、Agnes、小鸡 GPT、鲨鱼辣椒、小鸡 Kiro。
- 未提供模型清单的中转站保留 provider 与凭证，不猜测模型 ID，不执行外网 probe。
- 不发送真实 API 请求，不向任何 provider 传输学生资料或请求正文。
- 多模态输入能力仅登记模型元数据；图片附件消息 payload/UI 不属于本任务。
- 测试运行数据位于 `H:\pi-studybuddy-tmp\runs\T-M4-026\`；真实业务配置仅位于本机业务数据根。

## Git 约束

Git 收口已获用户明确授权并完成：功能提交 `10d50eb` 与治理提交 `c3d2db3` 已快进合并进入 `master`，Node24.14.0 master 完整质量门复验通过；网络恢复后 `git push origin master` 成功并核验 `master=origin/master=869de2f`（远端 refs/heads/master 一致）。既有未跟踪 `pi-session-*.html` 未纳入本任务。
