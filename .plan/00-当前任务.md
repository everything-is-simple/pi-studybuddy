# 当前任务

**状态**：in_progress（阶段 1-5 实现与验证已完成；专属真实 Electron E2E、Node24 非安装器质量门和部分原生 UAT通过；当前追加方案 B 测试夹具型 setup 生成与包内容审计；仍缺七类设置逐类原生保存→重启回读、双独立审查和用户明确收尾。T-M5-006 的实现、自动化、真机 UAT及任务分支提交事实保留，但独立审查和受控收尾未完成，现为 blocked 等待，不宣告 done。）
**日期**：2026-08-17
**当前任务**：T-M5-011「本机配置资产与设置能力控制台闭环」
**唯一计划**：`.plan/T-M5-011-local-config-settings-console.md`
**实施分支**：`agent/T-M5-011-local-config-settings-console`
**运行根**：`H:\pi-studybuddy-tmp\runs\T-M5-011\`

## 当前门禁

- [x] 用户明确批准 T-M5-011 开工；`docs/04`、唯一计划、当前分支与运行根已同步。
- [x] T-M5-010 已 done；其模型目录、报告目标、备份恢复与数据根迁移闭环保持既有事实。
- [x] T-M5-006 边界已核对：它仅负责受管运行资源、派生运行能力状态及离线/缺失降级。本任务只消费脱敏派生状态，不持久化 health、不重做运行资产。
- [x] T-M5-006 的实现提交 `5911722` 已推送任务分支，但双独立审查、实施记录和用户明确收尾仍缺失，故为 blocked 等待；不代表 done，不合并 master。
- [x] 完成 `DATA-CFG-*` 资产盘点与设计落点：统一版本 envelope、字段校验、原子写、迁移、损坏恢复、备份/恢复/卸载边界；普通 JSON 与 DPAPI 边界已同步到 05/06/09/traceability。
- [x] 先写并运行 RED：缺失配置、旧版本迁移、损坏恢复、只读失败、DPAPI 不可用与 Renderer 脱敏；证据位于 `H:\pi-studybuddy-tmp\runs\T-M5-011\red\`。
- [x] 完成七类设置控制台实现及定向测试；通用、学习技能、关于与更新的自动化 Electron 保存/重启回读通过，模型/运行能力/家长渠道/数据与备份边界已接线；外部服务全部 mock。
- [x] 完成专属真实 Electron E2E、Node24 完整非安装器质量门和部分原生 UAT；证据位于 `H:\pi-studybuddy-tmp\runs\T-M5-011\e2e-settings-console\` 与 `uat\`。
- [x] 生成方案 B 测试夹具型 x64 setup（已按用户命名重命名）：`pi-studybuddy-test--方案b-setup包.exe`，正式 handler 合成 fixture、三文件 `test-bundle`、包内容扫描、win-unpacked 带 test profile 双启动、隔离静默安装和安装后双启动均通过；SHA-256 为 `09c5ed2a27b8e4521596e5bc8fe7e3765bfe9fdadfc04e0c40aa71b5f87d6a98`，130418209 bytes。首次安装扫描与首次安装后 smoke 超时保留为环境证据。
- [ ] 补齐模型、学习技能、运行能力、数据与备份、关于与更新等七类逐类原生 UI 保存→关闭→重启→回读，以及迁移/损坏/写失败/DPAPI 不可用的可见恢复证据；当前不把部分 UAT升级为完整通过。
 - [ ] 完成两名独立审查并等待用户明确受控收尾；方案 B setup 仅作为测试资产生成并单独审计，不替代 T-M5-007/008；T-M5-007/008 不提前启动；未跟踪 `nul` 和 `omp-windows-*.exe` 不读取、不运行、不暂存、不提交、不删除。

## 当前范围

T-M5-011 负责 `<dataRoot>/config/` 的版本化 JSON 生命周期、校验、迁移、损坏恢复、原子写与七类设置控制台，并在用户明确授权下生成隔离的方案 B 测试夹具型 x64 setup。测试 setup 只携带合成 fixture、占位配置、显式 test profile 启动器、受控 mock 运行标识和合法运行资源；不读真实凭证、生产数据、`~/.pi` 或现有安装器。学习事实及关系配置继续留 SQLite；密钥继续只经 DPAPI vault；运行能力 health 仅由 T-M5-006 的运行时状态派生，不写入配置。所有测试与证据使用 `H:\pi-studybuddy-tmp\runs\T-M5-011\`；新 setup 输出仅写 `release-test/`。

## 剩余顺序

1. T-M5-011：盘点、RED→GREEN、真实 Electron E2E 与隔离真机 UAT；完成后仍保持 in_progress，等待独立审查和用户明确收尾授权。
2. T-M5-006：在不影响当前唯一计划的前提下，等待独立审查、实施记录和用户明确受控收尾；不得视为 done。
3. 方案 B 测试 setup：生成隔离 x64 setup，执行包清单/敏感内容扫描、安装前 win-unpacked smoke 和新 setup 文件哈希；不把该过程计为 T-M5-007 发布前 UAT或 T-M5-008 最终发行验收。
4. T-M5-007：仍按原计划执行发布前真实 Electron 全功能 UAT与回归，不由方案 B setup 替代。
5. T-M5-008：生成最终候选并在干净 Windows 完成安装、升级/卸载、数据保留及 T-M5-005 三条遗留 UAT。

未经用户单独授权，不执行 Git 收口；方案 B setup 仅为本次明确授权的测试资产，T-M5-007/008 的最终发布验收仍按原门禁执行。
