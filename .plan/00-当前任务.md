# 当前任务

**任务 ID**：T-M4-010
**计划文件**：`.plan/T-M4-010-s1-home-rpc.md`
**状态**：✅ done（Git 收口完成；2026-08-09）
**日期**：2026-08-09
**里程碑**：M4 业务接线 + 打包部署
**标题**：S1 首页 Tab RPC 接线（dailyBrief + tasks + exams）

## 当前门禁

- [x] 前置 T-M4-008（AppShell 数据流）与 T-M4-009（首次 x64 setup）均已完成、master 复验并推送 `origin/master`。
- [x] 开工前 `master` 与 `origin/master` 同为 `474976dee886e2ca375d791cbd25dc407be9ff14`，工作区干净。
- [x] 无其他执行中详细计划；T-M4-009 计划仅作为历史证据保留。
- [x] 用户已明确批准 T-M4-010 的计划与实施。
- [x] `docs/04-Todo` 已登记 T-M4-010 为 done。
- [x] 已切换隔离分支 `agent/T-M4-010-s1-home-rpc`。
- [x] Git commit / merge：提交 `a06d8a5` 已快进合并进入本地 `master`。
- [x] Git push：网络恢复后已推送，并在本次最终治理同步前核验本地、`origin/master` 与远端 `refs/heads/master` 同为 `b9a3c49`；最终治理提交推送后复验。

## 范围约束

本任务只在 AppShell 的唯一 `semesterId/courseId` 上下文内接通首页 `tasks.dailyBrief`、`tasks.list`、`exams.list`，实现独立的受控 loading/error/empty/ready 状态、竞态/卸载保护和安全展示。不得使用无 courseId 的跨学期列表调用；不新增或改变 API、handler、schema；不实现 T-M4-011~021，不做 TTS、AI 重试、错题建议或全链 E2E 任务；不执行 Git 收口。

## 当前进度

- [x] 权威文档、当前代码与相关设计/API/UI/测试文档已读取。
- [x] Git、前置依赖、运行时基线与单一任务门禁已复核。
- [x] 唯一任务计划已创建，状态已登记。
- [x] RED 测试：旧实现上失败，新增首页 RPC/倒计时断言。
- [x] GREEN 实现：三个独立 `useTabData` 数据域与课程门控。
- [x] REFACTOR：复用既有生命周期 hook，无 API/handler/schema 变化。
- [x] 定向测试、type-check、build、smoke/E2E 与完整质量门：Node 24.14.0 通过。
- [x] 代码/文档审查、实施记录和文档治理收尾：实施记录已创建，本地合并完成。

> T-M4-010 已完成 Git 收口并保留计划原件作为历史范围与验收证据；当前无执行中任务，等待用户明确选择下一任务；不得自动启动 T-M4-011 或其他后续任务。
