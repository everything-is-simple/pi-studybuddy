# 当前任务

**任务 ID**：T-M4-018
**计划文件**：.plan/T-M4-018-tts-control-rpc.md
**状态**：done（本地实施、双维度独立审查、质量门与 Git 收口完成；master 集成与 origin/master 核验通过）
**日期**：2026-08-11
**里程碑**：M4 业务接线 + 打包部署
**标题**：TTS 控制条 RPC 接线（speak + control + switchEngine + getStatus）
**实施分支**：agent/T-M4-018-tts-control-rpc（待计划批准后建立）
**集成分支**：master
**集成基线**：master=origin/master=87afbe0（T-M4-017 Git 收口事实核验）

## 当前门禁

- [x] T-M4-017 已完成治理事实收口，`docs/04-Todo` v0.1.131 已登记 done，master=origin/master=87afbe0 已核验。
- [x] 用户已明确选择 T-M4-018 并批准开工（2026-08-11"/plan" 选定 T-M4-018 TTS 控制条 RPC 接线）。
- [x] 已创建唯一执行计划 `.plan/T-M4-018-tts-control-rpc.md`（状态 📝 待审查）。
- [x] `.plan/` 无其他执行中任务（单一执行任务门禁满足）。
- [x] master 分支干净（仅未跟踪 pi-session html 用户 dirty 文件，不混入）。
- [x] 步骤 4：独立审查计划。
- [x] 步骤 5：用户批准计划（2026-08-11“批准”；裁决 1A/2A/3A/4A）。
- [x] 步骤 6：建立隔离分支 `agent/T-M4-018-tts-control-rpc` 并进入 RED → GREEN 实施。
- [x] 步骤 7~15：TDD（RED 初次失败 → GREEN 27/27）/ type-check / build / test（定向 + 全量 117 files/1119 tests）/ smoke 6/6 + 真实 Electron E2E 23 files/129 tests / 独立审查（busyRef 同步防重复修复）/ 治理同步（04-Todo v0.1.133 + 00-索引 v0.1.137）/ 文档治理 / diff 检查均完成。
- [x] 步骤 16：Git 收口（用户明确授权 2026-08-11）：功能提交 `dd4b909` 与治理登记提交 `e92c567`（及中间事实 `3dfef67`）已快进进入 master，Node24 master 完整 `verify --stage=full` 复验通过（unit/integration 117 files/1119 tests、真实 Electron E2E 23 files/129 tests），`git push origin master` 成功并核验 `master=origin/master=3dfef67`（远端 refs/heads/master 一致；网络恢复后推送），任务登记为 done（docs/04 v0.1.136）。

## 范围约束

本任务仅接通 renderer/UI 与既有 TTS RPC（contract 保持 127/127，不新增 API/handler/schema）：

- `tts.speak({ text, engine })`：SAPI 默认（mock adapter 离线可用）；edge-tts 失败降级 fallbackUsed=true。
- `tts.control({ playbackId, action: "play"|"pause"|"stop", rate? })`：播放/暂停/停止/语速。
- `tts.switchEngine({ engine })`：引擎切换。
- `tts.getStatus`：初始化/恢复兜底；主状态来源为 `subscribe("tts.state")`。
- `events.markReviewed({ refType, refId })`：朗读完成"标记已复习"（待用户裁决 4）。
- 内嵌朗读按钮接线仅限既有 NotesTab（笔记预览）与 MistakesTab（错题详情）2 处；其余 7 处朗读入口不纳入本轮。
- 不重写 TTS handler/adapter/状态机/stream 契约；不接真实 SAPI/edge-tts；不将朗读持久化。
- 不启动 T-M4-019~021。

## 待用户裁决的设计决策

1. **TTS 状态来源**：tts.state stream 订阅（推荐，方案 A）vs getStatus 轮询（方案 B）。
2. **控制条"当前朗读内容"标题**：renderer 记录最近朗读短标题（推荐，方案 A）vs 仅状态+进度无标题（方案 B）。
3. **内嵌朗读按钮接线范围**：仅既有 NotesTab/MistakesTab 2 处（推荐，方案 A）vs 全部 9 处（方案 B，范围蔓延）。
4. **标记已复习**：接入 events.markReviewed（推荐，方案 A）vs 本轮不接入保留静态按钮（方案 B）。

## 测试隔离

所有测试数据、日志与 Electron 运行产物必须位于：

`H:\pi-studybuddy-tmp\runs\T-M4-018\`

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`，不得连接真实 SAPI、edge-tts、AI、SMTP、飞书、WPS COM、whisper.cpp 或其他外部服务。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获任务选择与开工登记授权，未获计划批准与 Git 收口授权；不得混入本工作区既有用户 dirty 修改（未跟踪 pi-session html）。
