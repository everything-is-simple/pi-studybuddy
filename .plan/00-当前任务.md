# 当前任务

**状态**：✅ T-M2-003 实施完成（待用户授权提交推送）
**日期**：2026-08-07
**里程碑**：M2 完整闭环（第 3 任务）
**任务**：T-M2-003 S7 课堂采集（许可确认 / PCM WAV / whisper.cpp / handoff）
**计划文件**：[.plan/T-M2-003-S7-class-capture.md](./T-M2-003-S7-class-capture.md)

> T-M2-003 S7 课堂采集实施完成：2 RPC handler + 2 studybuddy_* 工具 + WhisperCppAdapter 可注入 + PCM WAV 文件头服务端字节级验证 + 许可确认强制 + 错误消息固定文案不泄漏路径/stdout/stderr/密钥 + 原始音频 finally 清理 + handoff 到 S2 materials+normalized_texts+study_events。509 测试全绿（34 test files），type-check + build + smoke 6 项全通过。04-Todo v0.1.26 + 00-索引 v0.1.34 + AGENTS.md v0.1.14 同步。待用户授权提交推送。

## 候选任务设计依据

- **04-Todo §6.4**（M2 范围含 S7 课堂采集 + §7.3 M2 任务大纲 S7 两行）
- **07-WF §2.7**（S7 课堂采集流程：5 步 + 关键约束 7 条 + 错误处理）
- **06-API §3.9**（S7 2 方法：classCapture.transcribe / classCapture.saveTranscription）
- **05-ERD**（不建独立表，复用 S2 materials + normalized_texts）
- **03-Arch §3.3**（whisper.cpp Adapter）
- **08-Test §3.3.2**（whisper.cpp Adapter 单件测试 + §5.4 不连真实外部服务 mock）

## 候选任务范围（仅名称 + 设计依据，不预写实施步骤，AGENTS.md §4.4）

**T-M2-003 S7 课堂采集**

- **设计依据**（权威源，优先级 4）：
  - [07-Workflow §2.7](../docs/07-工作流-Workflow.md)：S7 课堂采集流程（5 步：许可确认 → 选 PCM WAV → transcribe → 修改文本 → saveTranscription handoff 到 S2）+ 关键约束 7 条 + 错误处理
  - [06-API §3.9](../docs/06-API契约-API-Contracts.md)：2 RPC 方法（classCapture.transcribe / classCapture.saveTranscription）
  - [05-ERD §3.2](../docs/05-数据模型-ERD-Data-Model.md)：不建独立表，复用 S2 materials（file_type='text', source_type='class_audio_transcription', status='converted'）+ normalized_texts（source_type='class_audio_transcription'）
  - [03-Arch §3.3](../docs/03-架构设计-Architecture-Design.md)：whisper.cpp Adapter（本机同步转写，不回退云端）
  - [08-Test §3.3.2](../docs/08-测试验收-Test-Plan.md)：whisper.cpp Adapter 单件测试 + §5.4 不连真实外部服务 mock
- **范围概述**（不含实施步骤）：
  - 2 RPC handler + studybuddy_* 工具注册
  - whisper.cpp Adapter（可注入 mock，CLI/模型路径只来自配置）
  - PCM WAV 文件头服务端重新验证（RIFF/WAVE/PCM/16kHz/单声道/16-bit，不信任浏览器 MIME）
  - 许可确认强制（permissionConfirmed=false → BAD_REQUEST）
  - 原始音频只暂存 tmp/class-capture/<request-id>/，finally 清理
  - handoff 到 S2：创建 material + normalized_text + study_events（event_type=class_handoff_saved, source_system='S7'）
  - CLI/模型路径/stdout/stderr/密钥不泄漏；固定错误码，不返回路径或全文
- **不做什么**（07-WF §2.7）：MP3/M4A/WebM/视频/FFmpeg 转码、实时录音/流式字幕、说话人分离、云端上传、原始音频留存
- **治理阶段**：阶段 2-4（单件 → 集成 → 组装）
- **安全不变量**：不连真实 whisper.cpp（mock）+ 路径不泄漏 + 原始音频不留存

## 单一执行任务门禁（AGENTS.md §4.4）

`.plan/` 同一时刻只允许存在一个**正在执行**的详细任务计划。

创建新计划的三项前置条件（必须同时满足）：
1. ✅ T-M2-002 已正式完成（docs/04 done + master aba93e9 + origin/master 推送成功，§8.4 三者齐全）
2. ⏳ 等待用户明确批准 T-M2-003 S7 课堂采集开工
3. ⏳ 用户批准后该任务即将进入实施

> 未获用户明确批准前，本文件只登记候选任务名称与设计依据，**不预写**文件清单、命令、预期输出或实现步骤（§4.4）。

## 历史

- 2026-08-07（T-M2-003 实施完成）：S7 课堂采集 2 RPC handler + 2 studybuddy_* 工具 + WhisperCppAdapter 可注入（mock/failing/real）+ PCM WAV 文件头服务端字节级验证（44 字节 8 项校验）+ 许可确认强制 + 错误消息固定文案不泄漏路径/stdout/stderr/密钥 + 原始音频 finally 清理 + handoff 到 S2（materials + normalized_texts + study_events，source_type='class_audio_transcription' / status='converted' / permission_confirmed=1 / content_hash=SHA-256 / event_type='class_handoff_saved' source_system='S7'）。前置 DTO/schema 已就绪无需修改。studybuddy-extension 接入 S7 共 26 工具；扩展装配测试同步更新 24→26。509 测试全绿（34 test files），type-check + build + smoke 6 项全通过。04-Todo v0.1.26 + 00-索引 v0.1.34 + AGENTS.md v0.1.14 同步。待用户授权提交推送。
- 2026-08-07（T-M2-002 实施完成）：S6 家长报告 11 RPC handler + 3 studybuddy_* 工具注册 + 规则报告生成 + AI 仅润色降级 + UUID 泄漏检测 + 渠道独立失败隔离 + 冻结状态机 + DTO 对齐 ERD §2.2/§3.6。455 测试全绿（29 test files），verify 7+2 全通过。04-Todo v0.1.25 + 00-索引 v0.1.33 + AGENTS.md v0.1.13 同步。待用户授权提交推送。
- 2026-08-07（T-M2-001 正式完成）：master 9ddcaec + origin/master 推送成功（§8.4 三者齐全）。前置 .plan/00 标记"待推送"为过时信息，本次修正。
- 2026-08-07（T-M2-002 预选）：用户预选 S6 家长报告为 M2 第 2 任务，登记候选名称 + 设计依据，等待用户批准开工。
- 2026-08-07（T-M2-001 实施完成）：S5 期末冲刺 8 RPC handler + 2 studybuddy_* 工具 + MockExamGenerator 可注入 + source_hash 防重复 + 复用 S3 grader 三策略 + cramCards/cramPlan 确定性只读 DTO + 模块分析 weakness_level + study_events 写入 + DTO 对齐 ERD §3.5。420 测试全绿，verify 7+2 全通过。04-Todo v0.1.24 + 00-索引 v0.1.32 + AGENTS.md v0.1.12 同步。
- 2026-08-07（T-M2-001 计划创建 + 开工）：S5 期末冲刺 8 RPC handler + 2 studybuddy_* 工具 + MockExamGenerator 可注入 + 确定性聚合器 + DTO 对齐 ERD §3.5。前置门禁已核查。
- 2026-08-07（T-M1-004 完成 + 推送）：S4 错题/薄弱点 10 RPC handler + 4 studybuddy_* 工具注册 + 幂等归档 + 状态机 + 薄弱点归纳 + DTO 对齐 ERD §3.4。383 测试全绿。commit 9118c60 推送 origin/master。
- 2026-08-07（T-M1-004 开工）：S4 错题/薄弱点 10 RPC handler + 4 工具 + 幂等归档 + 状态机 + DTO 对齐 ERD §3.4。
- 2026-08-07（T-M1-003 完成 + 推送）：S3 限时练习 5 RPC handler + 3 studybuddy_* 工具注册 + QuestionGenerator 可注入 + grader 规则批改三策略 + DTO 对齐 ERD §3.3。336 测试全绿。commit f864e4c。
- 2026-08-07（T-M1-003 计划创建 + 开工）：S3 限时练习 5 RPC handler + 3 工具注册。前置 DTO 对齐 ERD §3.3。
- 2026-08-07（T-M1-002 完成 + 推送）：S2 资料/笔记/知识模块 17 RPC handler + 6 studybuddy_* 工具注册 + DTO 对齐 ERD。295 测试全绿。commit 939f6fe。
- 2026-08-07（T-M1-002 计划创建）：S2 资料/笔记/知识模块 17 RPC handler + 工具注册。前置 DTO 对齐 ERD。计划待审查。
- 2026-08-07（T-M1-001 完成 + 推送）：S1 学习节奏 25 RPC handler + 6 studybuddy_* 工具注册。237 测试全绿。commit addec03。
- 2026-08-07（T-M1-001 开工）：S1 学期/课程/考试/课表/任务 工具注册 + API。前置 DTO 对齐 schema。
- 2026-08-07（T-M0-009 完成 + 推送）：M0 系统冒烟完整（M0 收官）。commit 378aa7d。
- 2026-08-07（T-M0-008 完成 + 推送）：09-UI 三栏布局 + 标签页骨架。commit 034969c。
- 2026-08-07（T-M0-007 完成）：studybuddy-extension 空壳。
- 2026-08-07（T-M0-005 完成 + 提交）：file-watch。commit 47a2357。
- 2026-08-07（T-M0-004 完成）：toolchain 框架。
- 2026-08-07（T-M0-003 完成）：credential-vault。
- 2026-08-07（T-M0-006 完成）：数据层 schema。
- 2026-08-07（T-M0-002 完成）：contract 类型化契约面。
- 2026-08-07（T-M0-001 完成）：四进程骨架 + 自研 RPC + 最小 contract。

## 下一步

T-M2-003 S7 课堂采集实施完成（509 测试全绿 + type-check + build + smoke 6 项全通过），待用户授权提交推送（AGENTS.md §8.4 三者齐全：docs/04 done + master 复验 + origin/master 推送）。M2 剩余 6 任务：TTS / 备份恢复 / S5-S7 UI / E2E-04~09。
