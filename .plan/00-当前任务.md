# 当前任务

**任务 ID**：T-M5-003
**计划文件**：`.plan/T-M5-003-chat-session-model-closure.md`
**状态**：本地实施/验收/审查完成；Git 收口本地完成（功能 `e754c78` + 治理 `ef047df` 已 ff-only 进入本地 master）；`git push origin master` 因 GitHub 443 不可达失败，按 §8.4 保持 in_progress 待网络恢复推送核验（2026-08-12）
**日期**：2026-08-12
**里程碑**：M5 用户可用性验收 + UI 修订 + 一键交付
**标题**：对话/会话/模型/文件引用真实用户闭环修订
**实施分支**：`agent/T-M5-003-chat-session-model-closure`
**集成分支**：`master`
**集成基线**：`master=origin/master=9ec9b1e`（实施时核验；T-M5-002 已快进并入 master）

## 当前门禁

- [x] 用户明确批准 T-M5-003 开工（“P0 项按序推进 T-M5-003”）。
- [x] T-M5-002 已受控收尾为 done（真机 UAT 两阶段 + 完整质量门事实已登记）。
- [x] docs/04 §7.7 登记 T-M5-003 为 `in_progress`；唯一执行计划 `.plan/T-M5-003-chat-session-model-closure.md` 已建立。
- [x] 任务启动提示词 `.pi/prompts/task-execution/T-M5-003-chat-session-model-closure.md` 已完善（充足上下文 + 治理）。
- [x] 当前 shell 已在 `agent/T-M5-003-chat-session-model-closure` 实施并核验。
- [x] RED：生产空数据无 fixture 会话；真实会话所有权/持久化、模型状态、错题/文件引用与失败可见测试已记录初次失败（10 条全按预期失败）。
- [x] GREEN：仅以既有契约完成真实闭环（contract 保持 127/127，未新增 API/schema）；turn_end L3 真实会话归属（用户裁决）。
- [x] 验收：定向测试、真实 Electron E2E（32 files/141 tests）、完整质量门、双独立审查、真机 UAT 两阶段（纯 UI + 重启持久化）均完成。
- [x] Git 收口（本地）：功能 `e754c78`（feat(m5)）+ 治理 `ef047df`（docs(m5)）已 ff-only 合并进入本地 master。
- [ ] Git 收口（远端）：`git push origin master` 因 GitHub 443 不可达失败，origin/master 仍为 9ec9b1e；待网络恢复后推送并核验 master=origin/master。

## 范围约束

- 只修复对话、会话、模型状态、错题关联与 `@` 文件引用的真实用户闭环；移除生产 `defaultSessionFixture()` 及 `sess-001`/`mist-001`/`sess-new` 依赖。
- 不新增 API 或数据库 schema（contract 保持 127/127）；既有能力不足时记录 RED 证据并停止请求裁决。
- 不修改 S1-S7/TTS/备份/设置产品控件（T-M5-004/005）、不处理运行依赖自包含（T-M5-006）、不启动 T-M5-004~008。
- 不使用真实凭证、学生资料、完整 UUID 或真实业务数据；测试与 UAT 数据只写入 `H:\pi-studybuddy-tmp\runs\T-M5-003\`。
- 不连真实 AI、SMTP、飞书、WPS、OCR 或 whisper 外部服务。

## Git 约束

- 业务实现只在 `agent/T-M5-003-chat-session-model-closure` 工作并已完成收口。
- 已完成 commit/merge（用户明确批准两提交 ff-only 序列）；push 因网络失败待恢复后重试（无需另行授权）。
