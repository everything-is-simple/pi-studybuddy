# 当前任务

**任务 ID**：T-M4-019
**计划文件**：.plan/T-M4-019-backup-restore-rpc.md
**状态**：in_progress（计划 ✅ 已批准，进入 RED→GREEN 实施）
**日期**：2026-08-11
**里程碑**：M4 业务接线 + 打包部署
**标题**：备份恢复面板 RPC 接线 + TabBar 入口
**实施分支**：agent/T-M4-019-backup-restore-rpc（待计划批准后建立）
**集成分支**：master
**集成基线**：master=origin/master=586d1f0（T-M4-018 Git 收口事实核验）

## 当前门禁

- [x] T-M4-018 已完成治理事实收口，`docs/04-Todo` v0.1.136 已登记 done，master=origin/master=586d1f0 已核验。
- [x] 用户已明确选择 T-M4-019~021 序列继续（2026-08-11"下一个任务 T-M4-019~021 开始了 先做prompt 做plan"）；T-M4-019 prompt 资产已就绪（v0.1.96），本任务为序列下一项（执行序 38）。
- [x] 已创建唯一执行计划 `.plan/T-M4-019-backup-restore-rpc.md`（状态 📝 待审查）。
- [x] `.plan/` 无其他执行中任务（单一执行任务门禁满足；T-M4-020/021 各在前序完成后单独做计划）。
- [x] master 分支干净（无未跟踪/未提交脏文件）。
- [x] 步骤 4：独立审查计划。
- [x] 步骤 5：用户批准计划（2026-08-11“批准”；裁决 1A/2A/3A/4A）。
- [x] 步骤 6：建立隔离分支 `agent/T-M4-019-backup-restore-rpc` 并进入 RED → GREEN 实施。
- [x] 步骤 7~15：TDD（RED 初次失败 → GREEN 28/28）/ type-check / build / test（定向 45 + 全量 118 files/1130 tests）/ smoke 6/6 + 真实 Electron E2E 24 files/130 tests / 独立审查（进度残留修复 + layout Tab 9→10 同步）/ 治理同步（04-Todo v0.1.138 + 00-索引 v0.1.142 + 09-UI v0.1.5）/ 文档治理 / diff 检查均完成。
- [x] 步骤 16：Git 收口（用户明确授权 2026-08-11）：功能提交 `1bc68e2` 与治理登记提交 `8a6f952` 已快进进入 master，Node24 master 完整 `verify --stage=full` 复验通过（unit/integration 118 files/1130 tests、真实 Electron E2E 24 files/130 tests），`git push origin master` 成功并核验 `master=origin/master=8a6f952`（远端 refs/heads/master 一致），任务登记为 done（docs/04 v0.1.139）。

## 范围约束

本任务仅接通 renderer/UI 与既有 backup.* RPC（contract 保持 127/127，不新增 API/handler/schema）：

- `backup.course({ courseInstanceId, targetPath })`：备份此课程（课程门控 + in-flight 防重复）。
- `backup.allCourses({ semesterId, targetPath })`：备份全部课程。
- `backup.restore({ zipPath, targetSemesterId, conflictResolution })`：zip 选择（dialog rawPath + zip filter）+ 冲突策略显式选择 + RestoreResult 摘要。
- `backup.list({ semesterId })`：历史列表（不显示 targetPath 完整路径）。
- `backup.configureSchedule/listSchedules/toggleSchedule`：调度配置/启停。
- `subscribe("backup.progress")`：备份进度（host 生产接线，对齐 T-M4-018 tts.state 先例）。
- TabBar 新增"备份"Tab（第 10 个）→ BackupPanel 可达。
- desktop dialog 新增 directory capability（shell 层扩展，对齐 rawPath 先例；contract 127/127 不变）。
- 不实现运行态 cron 守护进程；不做归档触发备份 UI；不执行真实用户资料备份/恢复。
- 不启动 T-M4-020/021。

## 待用户裁决的设计决策

1. **BackupPanel 可达入口**：TabBar 新增"备份"Tab（推荐，方案 A，同步 09-UI §4.1/§2.1）vs 设置页入口（方案 B）。
2. **恢复冲突策略**：恢复前显式选择覆盖/新建（推荐，方案 A）vs 不选择（host 默认 create_new，方案 B）。
3. **历史行"恢复"按钮**：触发 zip 选择恢复流程（推荐，方案 A）vs 仅展示（方案 B）。
4. **备份目标目录选择**：新增 dialog directory capability（推荐，方案 A，含 E2E seam）vs 固定默认目录（方案 B）。

## 测试隔离

所有测试数据、日志、Electron 运行产物与备份 zip 必须位于：

`H:\pi-studybuddy-tmp\runs\T-M4-019\`

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`，不得连接真实外部服务。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获任务序列选择与开工登记授权，未获计划批准与 Git 收口授权；不得混入本工作区既有用户 dirty 修改（未跟踪 pi-session html）。
