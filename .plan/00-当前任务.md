# 当前任务

**任务 ID**：T-M4-012
**计划文件**：.plan/T-M4-012-s2-notes-rpc.md
**状态**：done（T-M4-012 已完成 Git 收口）
**日期**：2026-08-10
**里程碑**：M4 业务接线 + 打包部署
**标题**：S2 笔记 Tab RPC 接线（notes.get/update + modules.list/update）
**实施分支**：agent/T-M4-012-s2-notes-rpc
**集成分支**：master

## 当前门禁

- [x] T-M4-011 已完成收官，当前基线 master/origin/master 为 2497de4。
- [x] 用户明确批准本轮只实施 T-M4-012，并选择方案 A：NotesTab 内局部显式资料选择。
- [x] materialId 来源裁决已完成：复用既有 materials.list({ courseId })，由学生在 NotesTab 内明确选择；不默认第一条、不写死、不新增 AppShell 跨 Tab 状态。
- [x] 已创建唯一执行计划并切换隔离分支。
- [x] 初始 RED 已记录；首轮两名独立审查发现异步写回、DOM 隐私、NOT_FOUND 新建与模块过滤 P1，已退回 RED 补测并完成最小修复。
- [x] 首轮 P1 修复后的历史 Node24 质量门证据已保留，不作为当前最终证据。
- [x] 当前 E2E 已改为应用启动前隔离 fixture 预置，补齐真实 Electron renderer 路径和 UUID/Windows/POSIX/file URI/错误栈可见文本断言。
- [x] 当前 Node24.14.0 / pnpm11.20.0 `verify --stage=full` 通过（unit 107 files / 1047 tests；真实 Electron E2E 17 files / 119 tests）；两名独立复核无 P0/P1。
- [x] T-M4-012 功能提交 `2e1e7f6` 已快进进入 master。
- [x] `origin/master` 已推送并核验为 `2e1e7f6`。
- [x] 未启动 T-M4-013~021。

## 范围约束

本任务仅接通 renderer/UI：

- NotesTab 局部加载当前课程资料并提供显式选择；
- 选择后接通 notes.get / notes.update；
- 复用 modules.list / modules.updateLearnStatus；
- 维持当前学期/课程竞态保护与 archived 只读边界；
- 不新增 API、handler、schema 或 AppShell 全局资料状态；
- 不启动 T-M4-013~021，不连接真实外部服务，不写真实业务数据根。

## 测试隔离

所有测试数据、日志与 Electron 运行产物必须位于：

H:\pi-studybuddy-tmp\runs\T-M4-012\

不得写入 %LOCALAPPDATA%\PiStudyBuddy。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点；当前未获 Git 收口授权。
