# 当前任务

**任务 ID**：T-M4-013
**计划文件**：.plan/T-M4-013-s3-practice-rpc.md
**状态**：in_progress（T-M4-013 本地实施、两名独立审查、修复复验、实施记录与治理检查已完成；Git 收口待用户单独授权）
**日期**：2026-08-10
**里程碑**：M4 业务接线 + 打包部署
**标题**：S3 练习 Tab RPC 接线（createSession + getQuestions + submit + getResult）
**实施分支**：agent/T-M4-013-s3-practice-rpc
**集成分支**：master
**集成基线**：master=origin/master=f4e54c2

## 当前门禁

- [x] T-M4-012 已完成 Git 收口；live 基线为 `master=origin/master=f4e54c2`。
- [x] 用户已明确批准本轮仅实施 T-M4-013。
- [x] T-M4-013 已在 04-Todo 登记为 in_progress。
- [x] 已创建唯一执行计划与隔离分支 `agent/T-M4-013-s3-practice-rpc`。
- [x] 已完成现有 S3 renderer / contract / handler 影响面审计。
- [x] 已记录并运行 RED 测试（初次 3/3 失败）。
- [x] 已完成最小 GREEN 实现与 REFACTOR。
- [x] 已完成定向、全量与真实 Electron E2E 验收（verify full：109 files/1057 tests + 18 files/120 tests）。
- [x] 两名独立审查者 A/B 已完成交叉复核；P1/P2 发现项已修复并复验。
- [x] 已创建 `.record/T-M4-013-实施记录.md`（8 章节）。
- [x] 文档治理、完整质量门、UUID 泄漏与 diff-check 已复验通过。
- [ ] Git 收口另需用户明确授权。

## 范围约束

本任务仅接通 S3 PracticeTab renderer/UI：

- 复用既有 `practice.createSession`、`practice.getQuestions`、`practice.submit`、`practice.getResult`；
- 复用当前 AppShell `semesterId/courseId` 上下文与既有模块读取能力；
- 由学生在 PracticeTab 局部显式选择模块；不得默认第一条、写死 ID 或新增 AppShell 跨 Tab 模块状态；
- 作答前不得向 renderer DOM/state 泄露 `correct_answer`、`acceptable_answers`、`explanation`；
- 维持课程/学期竞态保护、卸载保护、错误净化与 archived 只读双层边界；
- 不新增 API、handler、schema，不连接真实外部服务，不写真实业务数据根；
- 不启动 T-M4-014~021。

## 测试隔离

所有测试数据、日志与 Electron 运行产物必须位于：

H:\pi-studybuddy-tmp\runs\T-M4-013\

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获实施授权，未获 Git 收口授权。
