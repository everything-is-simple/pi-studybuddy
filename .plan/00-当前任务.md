# 当前任务

**任务 ID**：T-M5-004
**计划文件**：`.plan/T-M5-004-s1-s5-structured-learning-ui.md`
**状态**：🔨 in_progress（v0.1.176：v4c 原生可见 UI、完整质量门与双独立复审均已完成且 P0/P1=0；仅 Git 收口尚未获用户单独授权）
**日期**：2026-08-14
**里程碑**：M5 用户可用性验收 + UI 修订 + 一键交付
**标题**：S1-S5 结构化学习页面逐控件修订
**实施分支**：`agent/T-M5-004-test-database-real-data`
**集成分支**：`master`
**集成基线**：`master=origin/master=dacec56`（当前远端同步基线；本轮未获新的 Git 收口授权）

## 当前门禁

- [x] 用户明确批准 T-M5-004 开工；本轮又明确要求系统测试使用专门测试数据库，不依赖 mock 业务数据。
- [x] T-M5-003 已受控收尾为 done；docs/04 仍将 T-M5-004 登记为 `in_progress`。
- [x] 专用测试数据库：新增 `tests/helpers/test-database.ts`，在 `H:\pi-studybuddy-tmp\runs\T-M5-004\...` 下建立真实 `global.db` 与 `semester/<id>/sem.db`；正式 schema/handler 建立 S1/S2/S5 前置数据，运行中的 E2E 不再暴露或调用 `test.seedModule`。
- [x] Windows SQLite 生命周期：`initializeDataRoot()` 建库后关闭临时连接，可交给随后真实 Electron 独占打开；测试根保留为可审计运行数据，不把 Windows 文件句柄释放时序伪装成质量门结论。
- [x] 质量门历史检查点：Node24.14.0 `node scripts/verify.mjs --stage=full` 于 2026-08-13 通过（unit/integration 130 files/1189 tests；真实 Electron E2E 33 files/141 tests；contract 128/8/35、安全 6/6、docs 治理与 diff-check）。日志内 `completed_utc` 的 2026-08-14 为宿主机时钟原始标签，不能替代本任务日期。
- [x] 真机 UAT v3 的 S1 与 S4 证据保持；S2 的可见文件选择/转换/局部创建及重启回读保持。S3 因同一资料出现两个同名模块而降级，不能作为 PASS。全过程未使用 `webContents.executeJavaScript`、CDP、RPC/handler 直调或数据库预置；原始证据留存于 `H:\pi-studybuddy-tmp\runs\T-M5-004\native-uat-v3-20260813-0119\evidence\`。
- [x] P1 最小修复：同一资料活动同名模块在 `modules.create` 拒绝；NotesTab 显示脱敏可操作提示；定向测试 3 files/46 tests 通过。
- [x] 真机 UAT 重验：v4c 已完成全新隔离根的 S2 单模块、重复创建脱敏提示、S3 选择唯一模块与作答/提交；第二次重启发现 renderer 未消费既有 `practice.listSessions`，不能可见回读提交结果。现已在 T-M5-004 既定 S3“结果读取”范围内接通既有 typed RPC，并在同一未预置隔离根完成第二次重启可见结果回读（2026-08-14）；不得注入/直调/预置。
- [x] 双独立复审：审查 A 与审查 B 最终均确认 P0=0、P1=0；P1-01 同资料同名模块防线、P1-02 治理 Markdown 格式与 08-Test v0.1.7 头部同步均已闭合。
- [ ] Git 收口：本轮未 commit / merge / push；不得报告 done，等待用户单独授权。

## 范围约束（回顾）

- 只修订 S1-S5 纳入控件（首页残余动作/资料/笔记/练习/错题/冲刺）的真实可达、可用、失败/禁用/重试语义；不重复 T-M5-002 S1 管理闭环与 T-M5-003 对话闭环。
- 除用户于 2026-08-13 明确同意且已完成的 `modules.create` 最小 RPC/handler/NotesTab 局部入口外，不新增 API/schema/handler/Stream/跨 Tab 全局状态；`materialId` 仅来自 NotesTab 显式选择，contract 实测 128 Api handlers / 8 PiBridge methods / 35 工具；CTRL-MISTAKE-04 仍采用方案 A（mistakes.get DTO 扩展，方法数不变）。
- 不修改 S6/S7/TTS/备份/设置（T-M5-005）、不处理运行依赖自包含（T-M5-006）、不启动 T-M5-005~008。
- 测试与 UAT 数据只写入 `H:\pi-studybuddy-tmp\runs\T-M5-004\`，未污染真实数据根。

## Git 约束

- 本轮未 commit / merge / push；任何 Git 收口须等待用户单独授权。
- 下一任务 T-M5-005~008 保持 pending，等待用户单独选择。
