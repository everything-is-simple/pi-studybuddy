# 当前任务

**任务 ID**：T-M4-015
**计划文件**：.plan/T-M4-015-s5-cram-rpc.md
**状态**：done（本地实现、双维度独立审查、质量门与 Git 收口完成；master 集成与 origin/master 核验通过）
**日期**：2026-08-11
**里程碑**：M4 业务接线 + 打包部署
**标题**：S5 冲刺 Tab RPC 接线（mockExams + cramCards + cramPlan）
**实施分支**：agent/T-M4-015-s5-cram-rpc
**集成分支**：master
**集成基线**：master=origin/master=2d63bf5（T-M4-015 Git 收口事实核验）

## 当前门禁

- [x] T-M4-014 已完成治理事实收口，`docs/04-Todo` 已登记 done，master=origin/master=ddca0c8。
- [x] 用户已明确选择并批准本轮实施 T-M4-015（2026-08-11"现在开始 T-M4-015"）。
- [x] 已创建唯一执行计划 `.plan/T-M4-015-s5-cram-rpc.md` 并切换隔离分支 `agent/T-M4-015-s5-cram-rpc`。
- [x] RED：新增 T-M4-015 integration 失败用例（S5-RED-01~08），初次 8/8 失败（CramTab 未接通 RPC），RED 证据已存 `H:\pi-studybuddy-tmp\runs\T-M4-015\red\red-initial-failure.log`。
- [x] GREEN/REFACTOR：CramTab 接通既有 S5 RPC（已确认考试局部显式选择 + 模拟考三阶段 + 速背卡只读翻页 + 计划 DTO 展示）；in-flight ref 防重复 mutation。
- [x] 定向 unit/integration：既有 renderer-cram-tab 14 tests + 新增 T-M4-015 integration 8 tests 全绿；type-check/build 通过。
- [x] 真实 Electron renderer E2E：t-m4-015-cram-renderer 2 tests（主流程 + archived）通过；运行根 `H:\pi-studybuddy-tmp\runs\T-M4-015\`。
- [x] Node24/pnpm11 完整质量门：unit/integration 111 files/1077 tests、真实 Electron E2E 20 files/124 tests、contract 127/127、security 6/6、smoke 6/6、UUID 7/7、docs 治理与 diff-check 通过；证据存 `runs\T-M4-015\evidence\`。
- [x] 双维度独立审查，P0/P1 无遗留：审查者 A（功能/契约）发现并确认修复 S5 host 归档防线缺失；审查者 B（治理/安全）确认范围无越界、隐私净化/归档双层/质量门真实；结论写入实施记录与 docs/04 v0.1.122。
- [x] 创建 `.record/T-M4-015-实施记录.md`（8 章节）。
- [x] 同步 `docs/04-Todo`（v0.1.122）与 `docs/00-文档索引-Index.md`（v0.1.126），任务保持 in_progress。
- [x] 用户明确授权 Git 收口（2026-08-11“提交 推送 到 远端 然后 合并”），功能提交 `7974423` 与治理登记提交 `2d63bf5` 已快进进入 master，Node24 master 完整质量门复验通过，origin/master 已推送并核验，任务登记为 done（docs/04 v0.1.123）。

## 范围约束

本任务仅接通 S5 冲刺 Tab renderer/UI：

- 复用既有 `mockExams.generatePaper/getPaper/startAttempt/submitAttempt/getResult/getModuleAnalyses`、`cramCards.get`、`cramPlan.get`，以及 S1 既有 `exams.list({ courseId, confirmationStatus: "confirmed" })` 构造已确认考试门控。
- 已确认考试在本 Tab 局部显式选择；不新增 AppShell 跨 Tab 模块状态。
- 模拟卷生成前 assessmentAttempt 必须 confirmed（由后端触发器判定）；题目/答案/评分/模块分析全部来自既有 API。
- 速背卡/冲刺计划是确定性只读 DTO：不持久化、不依赖 AI、不暴露题干/答案/作答、不替学生改写事实。
- 维持课程/考试/试卷竞态保护、卸载保护、重复 mutation 防护、错误净化与 archived 只读双层边界。
- 不新增或修改 API contract、handler、schema、AppShell 全局状态；不启动 T-M4-016~021。

## 测试隔离

所有测试数据、日志与 Electron 运行产物必须位于：

`H:\pi-studybuddy-tmp\runs\T-M4-015\`

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`，不得连接真实 AI、SMTP、飞书、WPS COM、whisper.cpp 或其他外部服务。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获实施与治理证据同步授权，未获 Git 收口授权；不得混入本工作区既有用户 dirty 修改（未跟踪 pi-session html）。
