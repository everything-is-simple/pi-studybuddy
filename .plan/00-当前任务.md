# 当前任务

**任务 ID**：T-M4-021
**计划文件**：.plan/T-M4-021-m4-release-acceptance.md
**状态**：in_progress（计划 ✅ 已批准，进入收官验收实施）
**日期**：2026-08-11
**里程碑**：M4 业务接线 + 打包部署（收官）
**标题**：M4 收官验收 + 打包冒烟（安装 + 启动 + RPC 往返 + 安全不变量）
**实施分支**：agent/T-M4-021-m4-release-acceptance（待计划批准后建立）
**集成分支**：master
**集成基线**：master=origin/master=dda2a54（T-M4-025 Git 收口事实核验）

## 当前门禁

- [x] T-M4-025 已完成治理事实收口，`docs/04-Todo` v0.1.147 已登记 done，master=origin/master=dda2a54 已核验。
- [x] 用户已明确选择 T-M4-021（2026-08-11"收官验收"）；T-M4-021 prompt 资产已就绪 v0.1.96。
- [x] 已创建唯一执行计划 `.plan/T-M4-021-m4-release-acceptance.md`（状态 📝 待审查）。
- [x] `.plan/` 无其他执行中任务（单一执行任务门禁满足；本任务为 M4 最后一项）。
- [x] master 分支干净。
- [x] 步骤 4：独立审查计划。
- [x] 步骤 5：用户批准计划（2026-08-11“批准”；裁决 1A/2A）。
- [x] 步骤 6：建立隔离分支 `agent/T-M4-021-m4-release-acceptance` 并进入收官验收实施。
- [x] 步骤 7~15：验收 E2E（RED：构建网络依赖 + CDP 首启竞态 → ELECTRON_MIRROR/缓存/CDP+launch 三级重试 → GREEN）/ type-check / build / test（全量 118 files/1130 tests 不回归）/ smoke 6/6 + 真实 Electron E2E 29 files/137 tests / 独立审查 / 治理同步（04-Todo v0.1.149 + §6.6 门槛全勾选 + 00-索引 v0.1.153）/ 文档治理 / diff 检查均完成。
- [x] 步骤 16：Git 收口（用户明确授权 2026-08-11）：功能提交 `82738f9` 与治理登记提交 `cce49e0`（及健壮性修复 `b86caed`）已快进进入 master，Node24 master 完整 `verify --stage=full` 复验通过（unit/integration 118 files/1130 tests、真实 Electron E2E 29 files/137 tests），`git push origin master` 成功并核验 `master=origin/master=b86caed`（远端 refs/heads/master 一致），任务登记为 done（docs/04 v0.1.150）；**M4 全部 25 任务完成，v0.1 里程碑收官**。

## 范围约束

本任务为 M4 收官验收（P4），不实现新功能：

- 干净 master 重新构建 x64 NSIS setup（`pnpm package:win`，electron-builder 26.15.3）→ SHA-256 记录。
- 隔离静默安装（`setup.exe /S /D=<runs\T-M4-021\install>`）→ 至少两次启动。
- CDP 受控 piBridge 全链验证（127.0.0.1）：renderer → preload → system.ping → global.db 建库 → 代表性业务 RPC（semesters.create）往返（决策 1A）。
- 新增 `tests/e2e/t-m4-021-release-acceptance.test.ts` 自动化验收断言 + 发布证据矩阵（决策 2A）。
- M4 退出门槛（04-Todo §6.6 8 项）逐条对照 + 双维度独立审查。
- 不绕过安装/启动/RPC 失败（必须如实暴露并修复）；不触碰真实数据根/密钥；不自动 commit/merge/push。
- 运行数据全部写入 `H:\pi-studybuddy-tmp\runs\T-M4-021\`。

## 待用户裁决的设计决策

1. **安装包验证通道**：CDP 受控 piBridge 全链（推荐方案 A，复用 T-M4-009 package-smoke 模式）vs 进程级仅启动检查（方案 B，弱）。
2. **验收证据自动化**：新增 E2E 测试文件自动化断言（推荐方案 A，可复跑）vs 一次性脚本 + 日志（方案 B）。

## 测试隔离

所有测试数据、安装目录、用户数据、日志与安装包必须位于：

`H:\pi-studybuddy-tmp\runs\T-M4-021\`

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`，不得连接真实外部服务或真实密钥。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获任务选择与开工登记授权，未获计划批准与 Git 收口授权；不得混入本工作区既有用户 dirty 修改（未跟踪 pi-session html）。
