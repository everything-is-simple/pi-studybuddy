# 当前任务

**任务 ID**：T-M4-020
**计划文件**：.plan/T-M4-020-e2e-full-regression.md
**状态**：in_progress（计划 ✅ 已批准，进入 RED→GREEN 实施）
**日期**：2026-08-11
**里程碑**：M4 业务接线 + 打包部署
**标题**：E2E 全链回归（后端断裂修复 + 设置页/学期切换 + S1-S7 接线）
**实施分支**：agent/T-M4-020-e2e-full-regression（待计划批准后建立）
**集成分支**：master
**集成基线**：master=origin/master=11ba50e（T-M4-019 Git 收口事实核验）

## 当前门禁

- [x] T-M4-019 已完成治理事实收口，`docs/04-Todo` v0.1.139 已登记 done，master=origin/master=11ba50e 已核验。
- [x] 用户已明确选择 T-M4-020（2026-08-11"计划 T-M4-020"）；T-M4-020 prompt 资产已就绪 v0.1.96。
- [x] 已创建唯一执行计划 `.plan/T-M4-020-e2e-full-regression.md`（状态 📝 待审查）。
- [x] `.plan/` 无其他执行中任务（单一执行任务门禁满足；T-M4-021 待本任务完成后单独做计划）。
- [x] master 分支干净（无未跟踪/未提交脏文件）。
- [x] 步骤 4：独立审查计划。
- [x] 步骤 5：用户批准计划（2026-08-11“批准”；裁决 1A/2A/3A）。
- [x] 步骤 6：建立隔离分支 `agent/T-M4-020-e2e-full-regression` 并进入 RED → GREEN 实施。
- [x] 步骤 7~15：TDD（home E2E RED → 修复 → 4 文件 6 tests 全绿）/ type-check / build / test（全量 118 files/1130 tests 不回归）/ smoke 6/6 + 真实 Electron E2E 28 files/136 tests / 独立审查（无 P0/P1）/ 治理同步（04-Todo v0.1.141 + 00-索引 v0.1.145）/ 文档治理 / diff 检查均完成。
- [ ] 步骤 16：Git 收口（★ 用户单独授权）。

## 范围约束

本任务为回归类（P4），补齐 renderer E2E 覆盖缺口 + 全量回归执行，不引入新功能/API/schema：

- 新增 renderer E2E：`t-m4-020-home-renderer`（S1 首页 dailyBrief/tasks/exams + 门控 + 隐私）、`t-m4-020-materials-renderer`（S2 资料 list + 受控 convert + 归档只读）、`t-m4-020-settings-renderer`（设置页 settings/models/credentials.listKeys/toolchains + 返回）、`t-m4-020-semester-switch-renderer`（多学期切换 + 归档只读提示）。
- 全量回归：`verify --stage=full`（unit 118 files + E2E 24+4 files + contract/security/smoke/UUID/docs/diff）+ 双维度独立审查。
- 生产缺陷修复（如回归发现）：最小化、先 RED、登记影响证据；fixture/确定性等待类允许最小修复。
- 不引入未完成 Tab 新功能；不新增产品/API/schema（需要时停止按缺陷单独裁决）。
- 不触碰真实数据根/真实密钥；E2E 全部写入 `H:\pi-studybuddy-tmp\runs\T-M4-020\`。
- 不启动 T-M4-021。

## 待用户裁决的设计决策

1. **设置页 E2E 密钥断言范围**：仅 credentials.listKeys（隔离根为空断言，推荐方案 A）vs 真实 vault 往返（方案 B，违反 §5.3/§9.2）。
2. **S2 资料 E2E 覆盖方式**：SQL 预置资料 + 受控 convert（推荐方案 A，不动 MaterialsTab）vs upload 测试 seam（方案 B，改业务组件）。
3. **学期切换 E2E 覆盖**：多学期切换 + 归档只读提示（推荐方案 A）vs 仅单学期（方案 B）。

## 测试隔离

所有测试数据、日志与 Electron 运行产物必须位于：

`H:\pi-studybuddy-tmp\runs\T-M4-020\`

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`，不得连接真实外部服务或真实密钥。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获任务选择与开工登记授权，未获计划批准与 Git 收口授权；不得混入本工作区既有用户 dirty 修改（未跟踪 pi-session html）。
