# 当前任务

**任务 ID**：T-M4-023
**计划文件**：`.plan/T-M4-023-cross-review-remediation.md`
**状态**：🔄 in_progress（独立交叉审查问题修订；Git 收口待单独授权）
**日期**：2026-08-09
**里程碑**：M4 业务接线 + 打包部署
**标题**：独立交叉审查问题修订（架构/契约 + 治理/任务）

## 当前门禁

- [x] T-M4-010 已完成 Git 收口，当前基线 `master`/`origin/master` 为 `6cd1e903`。
- [x] 用户明确要求完成本轮独立交叉审查发现的修订。
- [x] 已切换隔离分支 `agent/T-M4-023-cross-review-remediation`。
- [x] 功能与治理修订完成并通过验证（Node 24.14.0 full verify：104 files/1028 tests、真实 Electron E2E 16 files/118 tests；contract 127/127、security 6/6、smoke 6/6、UUID 7/7、文档治理与 diff check 通过）。
- [ ] Git commit / merge / push（未获本次授权，不执行）。

## 范围约束

本任务只修订本轮 A/B 交叉审查已确认的问题：生产 `agent.send` 不得静默降级为 fixture；契约覆盖必须阻塞缺失/解析遗漏；真实 Electron E2E 增加代表性业务 RPC 与 agent.send 路由断言；credential-vault 文档拓扑、默认 Tab 文档、文档元数据、P0 历史审计证据与 T-M4-010 最终 Git 事实同步。不得启动 T-M4-011~021，不接入真实外部模型调用，不修改真实业务数据根。

## 开发纪律

- 先 RED 后 GREEN，再 REFACTOR。
- 测试运行数据只写 `H:\\pi-studybuddy-tmp\runs\T-M4-023\`。
- 生产模型路径使用显式配置/凭证；无配置必须返回固定脱敏错误，不使用 fixture。
- 测试 fixture 只能由测试显式注入。
- 不执行 commit、merge、push。
