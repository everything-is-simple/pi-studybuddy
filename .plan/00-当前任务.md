# T-M4-022 当前执行计划

**状态**：🟡 实施与质量门完成；两名独立复审 PASS；Git 收口待用户明确授权
**日期**：2026-08-08
**里程碑**：M4 业务接线 + 打包部署
**任务**：T-M4-022 Electron 生产运行时 / SQLite 兼容修复 + 真实桌面启动验证
**Git 基线**：master 与 origin/master 同步于 `f404f69`

## 1. 任务裁决与新增原因

- T-M4-022 是用户明确提供并批准进入治理登记的新增 task-id，不重编号历史任务。
- 真实问题是 Electron 33.4.11 内嵌 Node 20 与当前 `node:sqlite` / `DatabaseSync` 数据层不兼容；既有 E2E 使用系统 Node 子进程绕过真实 Electron，导致测试与生产路径不一致。
- T-M4-022 被放在 T-M4-005 后、T-M4-006 前，作为 P0 运行时阻塞修复；不启动 T-M4-006。
- 用户继续执行后已视为批准本计划，已完成事实调查、RED→GREEN→REFACTOR 与真实 Electron 验证；当前不自动 commit/push，故保留 in_progress 直到 Git 收口授权。

## 2. 任务边界

### 允许修改的文件（需事实调查后按实际需要收敛）

- `src/data/sqlite.ts`
- `src/data/global.ts`
- `src/data/semester.ts`
- `src/main/data-root-init.ts`
- `src/main/main.ts`
- `src/main/ipc.ts`
- `src/agent-host/index.ts`
- `src/preload/preload.ts`
- `tests/e2e/helpers/electron-launcher.ts`
- `tests/e2e/test-main.js`（仅删除/替换 Node 子进程冒充 Electron 的测试路径，不能继续绕过真实 Electron）
- `tests/e2e/helpers/rpc-driver.ts`（仅同步真实 Electron 回环 TCP 通信说明）
- `tests/e2e/electron-launcher-runtime.test.ts`
- 新增或修改 T-M4-022 专用 Electron 真实启动测试与脚本
- `package.json`、`pnpm-lock.yaml`（仅当 Electron 升级或 SQLite Adapter 选择确需改变依赖）
- `scripts/smoke.mjs`（仅当真实桌面冒烟证据需要扩展）
- `docs/04-任务清单-Todo-List.md`
- `.record/T-M4-022-实施记录.md`
- 如运行时契约/测试方法确实变化，再按权威链更新对应设计文档

### 明确不做

- 不启动或实现 T-M4-006。
- 不修改设置页、Provider UI、DeepSeek/MiniMax/GPT 配置、S1-S7 Tab、TTS 控制条或备份恢复面板。
- 不用 mock/fallback 冒充生产 SQLite，不修改测试阈值，不用 Node 子进程代替 Electron E2E。
- 不自动 commit、push、merge 或启动其他任务。

## 3. 实施顺序（计划批准后）

1. **运行时事实调查**：记录当前 Node、Electron、内嵌 Node、`process.getBuiltinModule`、`node:sqlite`、`DatabaseSync`、构建产物、native module/package manager/ABI 状态与启动失败最小复现。
2. **RED**：先写并运行真实 Electron 失败测试，至少覆盖 runtime capability、main.ts/global.db、首次启动、二次幂等、BrowserWindow、preload bridge、renderer→`system.ping`；所有运行数据写 `H:\pi-studybuddy-tmp\runs\T-M4-022\`。
3. **组件调查**：对比升级 Electron、替换 SQLite Adapter 或其他可行方案；验证 Electron ABI、Windows x64、pnpm install/rebuild、开发/打包加载与数据层 schema/API 兼容性。
4. **GREEN**：选择证据最充分的最小方案，保留现有同步事务和数据层 API，修复真实生产路径并让 RED 测试通过。
5. **REFACTOR**：只整理 SQLite Adapter、Electron/Node 测试边界、runtime probe、启动错误和测试夹具；重跑受影响测试。
6. **集成验证**：按 Prompt 运行 `pnpm type-check`、`pnpm test`、`pnpm build`、`pnpm smoke`、`pnpm test:e2e`、contract/security/UUID/docs governance 检查；业务 E2E 必须由真实 Electron 承载并通过回环 TCP 通道通信；Node 25 若触发 policy 失败，改用认可的 Node 24.14.0，不能改阈值掩盖。
7. **真实桌面双启动**：使用 `H:\pi-studybuddy-tmp\runs\T-M4-022\desktop-smoke\`，验证数据根、`global.db`、BrowserWindow、preload、renderer、`system.ping`、退出码、二次启动幂等、真实业务根与 `~/.pi` 不污染。
8. **独立交叉审查**：审查者 A 关注真实 Electron/生产路径/SQLite/退出码；审查者 B 关注数据隔离、迁移幂等、安全、ABI、文档状态；结论写入实施记录。

## 4. 预期测试与退出条件

- RED 测试 ID：`T-M4-022-RED-01` 至 `T-M4-022-RED-07`。
- 所有新增测试先证明当前实现失败，再以最小实现转绿。
- 退出前必须有真实 Electron 进程、真实 SQLite、首次建库、二次幂等、BrowserWindow、renderer、preload、`system.ping` 全链路证据。
- 必须通过：type-check、unit/integration、build、smoke、真实 Electron E2E、契约覆盖、安全不变量 6/6、UUID 泄漏、文档治理和 `git diff --check`。
- 必须更新 `docs/04-任务清单-Todo-List.md` 为事实状态并创建 `.record/T-M4-022-实施记录.md`（8 章节）。
- 只有 docs/04 证据、master 复验、origin/master 推送三者齐全才能报告完成；本任务不会自动执行后两项。

## 5. 当前门禁

- [x] 用户已明确选择 T-M4-022
- [x] T-M4-022 已登记为唯一 in_progress 任务
- [x] 旧 T-M4-006 候选未启动
- [x] 治理登记已同步到 AGENTS.md / docs/00 / docs/04
- [x] 计划已获继续执行授权
- [x] 代码实施与 targeted/full 质量门完成
- [x] 业务 E2E harness 已迁移为真实 Electron（16 files / 117 tests），无 Node fork 冒充路径
- [x] 两名独立审查者完成最终交叉审查（A：运行时；B：治理/证据，均 PASS）
- [x] docs/04、实施记录与治理版本同步完成
- [ ] 用户授权 commit/push，满足 AGENTS.md §8.4 完成判据

## 6. 历史

- 2026-08-08：T-M4-022 登记；M4 由 21 任务扩展为 22 任务；T-M4-022 前置于 T-M4-006。
- 2026-08-08：master 与 origin/master 基线为 `f404f69`；保留任务启动前的旧计划状态作为治理事实已被本计划替换。
- 2026-08-08：实际选择 Electron `36.9.5`（内嵌 Node `22.19.0`，真实 `node:sqlite` 可用），完成 IPC/preload/agent-host 竞态修复与真实 Electron 6 用例；Node `25.4.0` 触发既有 toolchain unverified policy；按 Prompt 使用认可的 Node `24.14.0` 重跑 `pnpm verify` 全量质量门通过。
- 2026-08-08：独立审查 A/B 最终复审均 PASS；Node `24.14.0` 下 `pnpm verify` exit 0，默认 Node `25.4.0` 的既有 unverified policy 已按 Prompt 记录，未改阈值。
