# 当前任务

**状态**：in_progress（runtime-resources/pi native skills/package smoke、发布态 agent-host 装配、SAPI/edge-tts 生产装配、OCR/whisper/WPS 缺失降级和运行能力可见状态已 GREEN；真机 UAT 已通过，manifest 完整性诊断已补强；双独立审查与受控收尾仍在实施）
**日期**：2026-08-17
**当前任务**：T-M5-006「全部必需运行依赖自包含与离线能力装配」
**唯一计划**：`.plan/T-M5-006-runtime-dependencies.md`
**实施分支**：`agent/T-M5-006-runtime-dependencies`
**运行根**：`H:\pi-studybuddy-tmp\runs\T-M5-006\`

## 当前门禁

- [x] T-M5-010 已完成设置、投递、学期备份与数据根迁移闭环；功能 `ef9eece` 已推送任务分支并 ff-only 合并 master，Node24 master 完整质量门通过。
- [x] 本治理登记提交随后推送并核验 `master=origin/master`；T-M5-010 已登记 done。
- [x] 用户已明确批准 T-M5-006 开工；`docs/04`、唯一计划、当前分支与运行根已同步。
- [x] 阶段 1 审计已写入 `H:\pi-studybuddy-tmp\runs\T-M5-006\audit\`；pi native skills 采用“在 extension 之外加装”的发行裁决，OCR/whisper/edge-tts 先许可核验，WPS 固定外部可选。
- [x] 首批 RED→GREEN 完成：`runtime-resources` manifest/resolver、StudyBuddy 自有 `study-planning` / `study-review` native skills、pi `additionalSkillPaths` 注入、electron-builder `extraResources`、运行能力派生状态和 `scripts/package-smoke.mjs` 有界 smoke harness。
- [x] 当前证据：首批定向 8 文件/46 tests、运行时回归定向 10 文件/46 tests、TTS/外部降级回归 6 文件/43 tests、TTS 专项 3 文件/29 tests；真实 Electron production 设置页运行能力 E2E 1/1（受管 pi/skills/SAPI/OCR/whisper/WPS/edge-tts 状态与脱敏断言）；完整 `pnpm test` 139 文件/1240 tests、历史全量 `pnpm test:e2e` 34 文件/142 tests；补正 `vitest.e2e.config.ts` 文件级串行后，排除唯一会生成 setup 的历史 T-M4-021 package acceptance，真实 Electron 回归 33 文件/141 tests 全绿。Node24 `verify --stage=full --skip=e2e`（8 执行/2 跳过，E2E 确实跳过）、`pnpm type-check`、`pnpm build`、contract 128/128、desktop security 6/6、docs-governance、`git diff --check` 均通过；当前 `release/win-unpacked` 已验证 runtime-resources 完整性，以及解包 agent-host/生产依赖后两次隔离启动的 `global.db`、`system.ping` 与业务 RPC；新增完整性回归证明受管资源篡改后运行能力变为 unsupported。
- [x] WPS 生产未配置路径：旧 Office 转换固定失败为 `conversion_failed` 并显示恢复指引；现代本地转换回归保持通过。
- [x] 原生真机 UAT已通过：`H:\pi-studybuddy-tmp\runs\T-M5-006\uat\native-runtime-settings-uat-correction.json` 记录全新隔离根两次真实可见启动、设置打开/返回、重启回读，以及运行能力卡的受管 skills、pi runtime、StudyBuddy extension、SAPI、edge-tts、WPS、OCR、whisper 可见截图；未见绝对路径、密钥、完整 UUID 或错误栈。OCR/whisper 仅验证设置页状态与恢复文案，未执行真实外部引擎。
- [x] 用户授权的任务分支 Git 事实：实现提交 `5911722`（`feat(m5): 装配受管运行依赖与离线降级`）已推送 `origin/agent/T-M5-006-runtime-dependencies` 并核验同位；未合并 master，不改变任务状态。
- [!] 完整 Electron E2E 包含历史 `T-M4-021` package acceptance，首次运行中误触发其 setup 构建；该生成物未被读取、复制、暂存、提交或作为 T-M5-006 发行证据。后续无发行回归显式排除该测试；`verify --skip=e2e` 的数字标签匹配已修复并实证跳过，T-M5-008 前不再生成候选。
- [!] 一次与大规模 Electron/文件系统任务并发的复验出现 file-watch 事件漏收与 package smoke RPC 超时；无残留进程后的单文件、串行全量和独立 package smoke 均通过，作为环境并发证据保留，未降级为产品通过替代。
- [ ] T-M5-005 继续 blocked；其报告投递、真实恢复、TTS 已复习三条遗留 UAT并入 T-M5-008 最终干净机安装验收。
- [ ] T-M5-011/007/008 依次 pending 且不启动；现有未跟踪 `nul` 和 `omp-windows-*.exe` 不读取、不运行、不暂存、不提交、不删除。

## 当前范围

T-M5-006 仅负责 pi runtime、StudyBuddy extension、受管 pi native skills、运行资源 manifest/resolver、合法可分发的 OCR/whisper/edge-tts 候选、SAPI 生产装配，以及 WPS/外部能力的可解释降级。当前生产路径已默认注入 SAPI；edge-tts 仅配置存在时启用，未配置时固定失败并按既有逻辑降级 SAPI；OCR/whisper 未配置时固定中文失败并给恢复指引；WPS 未配置时旧 Office 转换固定失败且不影响现代本地转换；设置页显示经脱敏的状态、说明和恢复动作，测试路径显式 mock。配置生命周期与七类设置控制台已登记为后续 T-M5-011。

## 剩余顺序

1. T-M5-006：完成双独立审查、实施记录和用户明确收尾授权后再执行受控收尾；当前受管运行资产、SAPI/edge-tts 与 OCR/whisper/WPS 缺失降级、真机 UAT和自动化质量门已通过。
2. T-M5-011：另获开工授权后治理 `DATA-CFG-*` 与七类设置；不得提前创建详细计划。
3. T-M5-007：不生成 setup 的发布前真实 Electron 全功能 UAT与回归。
4. T-M5-008：生成最终候选并在干净 Windows 完成安装、升级/卸载、数据保留及 T-M5-005 三条遗留 UAT。

未经用户单独授权，不执行 Git 收口；当前任务分支已推送，但双独立审查、实施记录和受控收尾授权仍是 T-M5-006 的未关闭门禁；T-M5-007/008 前不得生成最终 setup。
