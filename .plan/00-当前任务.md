# 当前任务

**任务 ID**：T-M4-011
**计划文件**：`.plan/T-M4-011-s2-materials-rpc.md`
**状态**：🟡 in_progress（本地实施、master 复验与 origin/master 推送已同步；target-machine acceptance pending）
**日期**：2026-08-10
**里程碑**：M4 业务接线 + 打包部署
**标题**：S2 资料 Tab RPC 接线（upload + convert + generateNote + list）

## 当前门禁

- [x] T-M4-010 已完成 Git 收口，当前基线 `master`/`origin/master` 为 `49904bd`。
- [x] 用户明确选择并批准本轮只实施 T-M4-011。
- [x] 已完成治理、设计、契约、既有实现与测试的只读重理解，未发现阻止 renderer 接线的文档冲突；文件导入/storage 语义缺口已登记为未解决架构阻塞。
- [x] 已切换隔离分支 `agent/T-M4-011-s2-materials-rpc`。
- [x] RED→GREEN→REFACTOR、定向测试、构建/冒烟/静态质量检查和两名独立审查已完成并留存证据。
- [x] Node 24.14.0 / pnpm 11.20.0 已安装并通过完整质量门（unit/integration 106 files / 1037 tests；真实 Electron E2E 16 files / 118 tests）。
- [x] renderer 已从 main dialog 结果接收一次性 `importToken/fileName/fileSize`，不再把源文件路径交给 host；全量 fixture/E2E 已适配并复验通过。
- [ ] target-machine acceptance 尚未完成。
- [x] Git commit / merge / push 已完成并核验：feature `516675b` 快进进入 master，治理同步 `73a95ad` 已推送，`master=origin/master=73a95ad`。

## 范围约束

本任务只接通当前学期/课程上下文下的 S2 资料列表、文件选择、上传、转换、生成笔记、刷新与状态显示；复用既有 `materials.*` 契约和 `useTabData` 竞态门控。不启动 T-M4-012~021，不连接真实外部服务，不修改真实业务数据根。

## 环境与证据

- 已安装 Node `v24.14.0` 到 `C:\Users\Administrator\.tools\node-v24.14.0`，并以 pnpm `11.20.0` 通过完整质量门；用户 PATH 已同步并广播环境变更。
- 测试运行数据根与日志：`H:\pi-studybuddy-tmp\runs\T-M4-011\`。
- 定向 renderer：3 files / 18 tests passed；资料 Electron RPC E2E：以本任务 `e2e` 隔离根重跑后 1 file / 10 tests passed。
- Node24 全量 unit/integration：105 files / 1036 tests passed；`verify --stage=full` 通过，真实 Electron E2E：16 files / 118 tests passed。Node25 的旧失败仅保留为已解决环境漂移历史。
- target-machine acceptance：`pending-target-machine`。
