# 当前任务

**任务 ID**：T-M4-017
**计划文件**：.plan/T-M4-017-s7-capture-rpc.md
**状态**：📝 待审查（计划已创建，等待用户批准后实施）
**日期**：2026-08-11
**里程碑**：M4 业务接线 + 打包部署
**标题**：S7 采集 Tab RPC 接线（classCapture.transcribe + saveTranscription）
**实施分支**：agent/T-M4-017-s7-capture-rpc（用户批准计划后建立）
**集成分支**：master
**集成基线**：master=origin/master=62fa21d（T-M4-016 Git 收口事实核验）

## 当前门禁

- [x] T-M4-016 已完成治理事实收口，`docs/04-Todo` v0.1.127 已登记 done，master=origin/master=62fa21d 已核验。
- [x] 用户已明确选择并批准开工 T-M4-017（2026-08-11“计划 T-M4-017”）。
- [x] 已创建唯一执行计划 `.plan/T-M4-017-s7-capture-rpc.md`（状态 📝 待审查）。
- [x] `.plan/` 无其他执行中任务（单一执行任务门禁满足）。
- [x] master 分支干净（仅未跟踪 pi-session html 用户 dirty 文件，不混入）。
- [x] 步骤 4：独立审查计划（实施者审阅后提交用户批准）。
- [x] 步骤 5：用户批准计划（2026-08-11“批准计划”），建立隔离分支 `agent/T-M4-017-s7-capture-rpc` 并进入 RED → GREEN 实施。
- [x] RED：新增 T-M4-017 integration 失败用例（C-RED-01~09），初次 8/9 失败，RED 证据已存 `H:\pi-studybuddy-tmp\runs\T-M4-017\red\red-initial-failure.log`。
- [x] GREEN/REFACTOR：CaptureTab 接通既有 classCapture.transcribe/saveTranscription；main dialog rawPath capability；定向 9/9 全绿；type-check/build 通过。
- [x] 真实 Electron renderer E2E：t-m4-017-capture-renderer 2 tests（主流程 + 归档只读）通过。
- [x] Node24/pnpm11 完整质量门：unit/integration/security 116 files/1105 tests、真实 Electron E2E 22 files/128 tests、contract 127/127 + 8 PiBridge + 35 tools、安全 6/6、smoke 6/6、UUID 7/7、docs 治理与 diff-check 通过。
- [x] 双维度独立审查（功能/契约 + 治理/安全）P0/P1/P2 无遗留。
- [x] 创建 `.record/T-M4-017-实施记录.md`（8 章节）。
- [x] 同步 `docs/04-Todo`（v0.1.129）与 `docs/00-文档索引-Index.md`（v0.1.133），任务保持 in_progress。
- [ ] Git 收口（需用户单独授权）。

## 范围约束

本任务仅接通 S7 采集 Tab renderer/UI 与既有 RPC（contract 保持 127/127，不新增 API/handler/schema）：

- `classCapture.transcribe({ courseId, audioFile, permissionConfirmed })`：许可确认强制 + PCM WAV 服务端重验证 + whisper（默认 mock）+ tmp finally 清理（host 既有，核验不改）。
- `classCapture.saveTranscription({ courseId, transcription, title })`：S7→S2 handoff（materials/normalized_texts/study_events，host 既有）。
- 文件选择走 desktop dialog rawPath capability（shell 层扩展：`DialogOptions.rawPath` / `DialogResult.rawPath`，对齐 T-M4-011 importToken 先例；preload 通道不变）。
- 不做拖拽导入（需 webUtils，留待用户裁决）；不接 TTS 朗读（T-M4-018）；不自动 generateNote（S2 自行）。
- 维持课程门控、归档只读（renderer isReadOnly + host 结构性防线核验）、竞态/卸载保护、重复 mutation 防护、错误净化与隐私展示。
- 不启动 T-M4-018~021。

## 待用户裁决的设计决策

1. **文件获取**：desktop dialog rawPath capability（推荐，方案 A）vs webUtils 拖拽（方案 B，改动大）。
2. **E2E 文件选择**：renderer 测试 seam `window.__PI_CAPTURE_FIXTURE__`（推荐，方案 A）vs 仅 RPC 级 E2E（方案 B，覆盖弱）。
3. **保存标题**：文件名派生 + 可编辑输入框（推荐，方案 A）vs 固定标题（方案 B）。

## 测试隔离

所有测试数据、日志与 Electron 运行产物必须位于：

`H:\pi-studybuddy-tmp\runs\T-M4-017\`

不得写入 `%LOCALAPPDATA%\PiStudyBuddy`，不得连接真实 AI、SMTP、飞书、WPS COM、whisper.cpp 或其他外部服务。

## Git 约束

功能实现、治理同步、commit、master 快进合并、完整复验、push 均为独立证据与授权节点。当前仅获实施与治理证据同步授权，未获 Git 收口授权；不得混入本工作区既有用户 dirty 修改（未跟踪 pi-session html）。
