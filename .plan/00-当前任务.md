# 当前任务

**状态**：✅ T-M1-005 OCR venv Adapter 已完成（in_progress → done）
**日期**：2026-08-08
**里程碑**：M1 核心闭环（§7.5 全局执行顺序表第 5 行）
**任务**：T-M1-005 OCR venv Adapter（课表图片识别）
**计划文件**：[.plan/T-M1-005-ocr-adapter.md](./T-M1-005-ocr-adapter.md)
**下一任务**：§7.5 全局执行顺序表第 6 行（T-M1-006 WPS COM 桥）

> T-M1-005 OCR venv Adapter 已正式完成（§8.4 三者齐全：docs/04 v0.1.40 done + master 复验通过 + origin/master 推送成功）。

> T-M2-009 E2E-04~09 已正式完成（§8.4 三者齐全）。M1/M2 完整闭环退出门槛全达成。当前推进 OCR venv Adapter——S1 课表图片识别真实组件，v0.1 代码写但测试用 mock（AGENTS.md §5.4）。venv 阶段1 已下载（04-Todo §4.1 看板 ✅），从阶段2 单件测试开始。
>
> **OCR 契约**：03-Arch §3.3 onnxruntime/PIL 全图格式（jpg/jpeg/png/webp/gif/bmp/tiff）+ 复用 H:\AIStudyBuddy\runtime\venv\Scripts\python.exe + 子进程 JSON 协议 + 手写 OCR 走本地 RapidOCR（不走多模态 AI）。08-Test §3.3.3 pytest 7 格式参数化断言 run_ocr 返回非空字符串。

## 下一步

T-M1-005 已完成（quality gate 全通过 + 治理文档同步 + 实施记录创建）。等待用户选择下一任务（T-M1-006 WPS COM 桥 / T-M1-007 资料转换管道 / T-M1-008 跨切钩子，或 M3 任务）。

## 单一执行任务门禁（AGENTS.md §4.4）

`.plan/` 同一时刻只允许存在一个**正在执行**的详细任务计划。

创建新计划的三项前置条件（必须同时满足）：
1. ✅ T-M2-009 已正式完成（§8.4 三者齐全：04-Todo v0.1.38 + master 671f010 + origin/master 推送成功）
2. ✅ 用户已明确批准 T-M1-005 OCR venv Adapter 开工
3. ✅ 该任务即将进入实施（详细计划已创建）

## 历史

- 2026-08-08（T-M1-010 实施完成待提交）：E2E-01~03 实施完成，3 E2E 文件 36 测试 + child_process.fork + Node.js IPC + 防泄露铁律 + 幂等归档 + 薄弱点 evidence_count≥2 + 规则批改可证伪 + M1 退出门槛全通过。722 单元/集成 + 36 E2E 测试全绿，verify full 阶段全通过。
- 2026-08-08（T-M1-010 计划创建）：T-M1-010 E2E-01~03 计划创建，用户批准 vitest + Electron 方案。08-Test v0.1.2 同步框架选择变更。
- 2026-08-08（T-M2-008 正式完成）：09-UI S5-S7+TTS+备份恢复 UI 已正式完成（§8.4 三者齐全），master c94650c + origin/master 推送成功。722 测试全绿（54 test files）。M2 done 6/9。
- 2026-08-08（T-M2-005 正式完成）：备份恢复已正式完成（§8.4 三者齐全），master be2401a + origin/master 推送成功。598 测试全绿（43 test files）。M2 业务 Adapter 层全部完成。
- 2026-08-08（T-M2-004 实施完成待提交）：TTS skill 实施完成，4 RPC handler + 3 studybuddy_* 工具 + TtsContext 双引擎可注入 + SAPI 默认 + edge-tts 失败降级 + 朗读状态机 + Streams["tts.state"] 推送 + 无独立 TTS 表 + 不写 StudyEvent + 全 mock。562 测试全绿（38 test files）。
- 2026-08-07（T-M2-003 正式完成）：master 0cc3915 + origin/master 推送成功（§8.4 三者齐全）。S7 课堂采集 2 RPC handler + 2 工具 + WhisperCppAdapter 可注入 + PCM WAV 字节级验证 + handoff 到 S2。509 测试全绿（34 test files）。
- 2026-08-07（T-M2-002 实施完成）：S6 家长报告 11 RPC handler + 3 工具 + 规则生成 + AI 仅润色降级 + UUID 泄漏检测 + 渠道独立失败隔离。455 测试全绿。
- 2026-08-07（T-M2-001 实施完成）：S5 期末冲刺 8 RPC handler + 2 工具 + MockExamGenerator 可注入 + source_hash 防重复。420 测试全绿。
- 2026-08-07（T-M1-004 完成 + 推送）：S4 错题/薄弱点 10 RPC handler + 4 工具。383 测试全绿。commit 9118c60。
- 2026-08-07（T-M1-003 完成 + 推送）：S3 限时练习 5 RPC handler + 3 工具 + QuestionGenerator 可注入 + grader 三策略。336 测试全绿。commit f864e4c。
- 2026-08-07（T-M1-002 完成 + 推送）：S2 资料/笔记/知识模块 17 RPC handler + 6 工具。295 测试全绿。commit 939f6fe。
- 2026-08-07（T-M1-001 完成 + 推送）：S1 学习节奏 25 RPC handler + 6 工具。237 测试全绿。commit addec03。
- 2026-08-07（T-M0-009 完成 + 推送）：M0 系统冒烟完整（M0 收官）。commit 378aa7d。
- 2026-08-07（T-M0-008 完成 + 推送）：09-UI 三栏布局 + 标签页骨架。commit 034969c。
- 2026-08-07（T-M0-007 完成）：studybuddy-extension 空壳。
- 2026-08-07（T-M0-005 完成 + 提交）：file-watch。commit 47a2357。
- 2026-08-07（T-M0-004 完成）：toolchain 框架。
- 2026-08-07（T-M0-003 完成）：credential-vault。
- 2026-08-07（T-M0-006 完成）：数据层 schema。
- 2026-08-07（T-M0-002 完成）：contract 类型化契约面。
- 2026-08-07（T-M0-001 完成）：四进程骨架 + 自研 RPC + 最小 contract。
