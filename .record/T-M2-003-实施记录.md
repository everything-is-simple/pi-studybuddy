# T-M2-003 实施记录

**任务**：S7 课堂采集（许可确认 / PCM WAV / whisper.cpp / handoff）工具注册 + API
**里程碑**：M2 完整闭环（第 3 任务）
**日期**：2026-08-07
**状态**：✅ 完成（待用户授权提交推送）

---

## 1. 任务裁决与范围

**权威依据**：04-Todo §7.3（M2 任务大纲 S7）+ §6.4（M2 范围 + 退出门槛含"whisper.cpp Adapter 单件测试"）+ 07-WF §2.7（S7 课堂采集流程：5 步 + 关键约束 7 条 + 错误处理 3 条）+ 06-API §3.9（S7 2 方法：classCapture.transcribe / classCapture.saveTranscription）+ 05-ERD §3.2.1（materials.source_type='class_audio_transcription' + permission_confirmed 字段已存在）+ §3.2.2（normalized_texts.source_type 字段已存在）+ §3.1.5（study_events.source_system CHECK 含 'S7'）+ 03-Arch §3.1（studybuddy_* registerTool 工具 S7 清单 2 工具）+ §3.3（whisper.cpp Adapter 设计契约：CLI/模型路径只来自配置；不猜路径不回退云端；子进程 stdout；路径/stdout/stderr/密钥不泄漏；固定错误码）+ 08-Test §3.3.2（whisper.cpp Adapter 单件测试 3 断言）+ §5.4（不连真实外部服务全 mock）+ AGENTS.md §5.4（不连真实 whisper.cpp）。

**范围**：实现 S7 课堂采集全链路——2 个 RPC handler（classCapture.transcribe / classCapture.saveTranscription）+ 2 个 studybuddy_* registerTool 工具 + S7Context 数据层句柄管理 + WhisperCppAdapter 可注入接口（mock/failing/real 三实现）+ PCM WAV 文件头服务端字节级验证（44 字节 8 项校验）+ 许可确认强制 + 错误消息固定文案不泄漏路径/stdout/stderr/密钥 + 原始音频 tmp/class-capture/<request-id>/ finally 清理 + handoff 到 S2（materials + normalized_texts + study_events，source_type='class_audio_transcription' / status='converted' / permission_confirmed=1 / content_hash=SHA-256 / event_type='class_handoff_saved' source_system='S7'）+ studybuddy-extension 接入 S7 工具（共 26 工具）+ 扩展装配测试同步更新 24→26。

**非目标**：真实 whisper.cpp 子进程集成测试（08-Test §5.4 全 mock，真实集成留待 E2E 受控夹具）；MP3/M4A/WebM/视频/FFmpeg 转码（受控 PCM WAV 单一输入）；实时录音 / 流式字幕 / 说话人分离 / 云端上传；原始音频留存（finally 清理）；建独立 S7 表/Job/Worker（复用 S2 既有路径）；自动 generateNote（学生保存后在 S2 自行触发）；09-UI S7 标签页业务 UI（壳层任务）；E2E-05（属 M2 后续测试任务）；改 semester.db 表结构（T-M0-006 已定）；改 api.ts 方法签名（前置 DTO/schema 已就绪无需修改）。

## 2. 实际交付

### 前置：DTO/schema 对齐核查（已确认无需修改）

与 T-M1-001/T-M1-002 启动时的"前置 DTO 对齐 schema"不同，本任务前置已就绪：
- `src/contract/api.ts` §3.9：classCapture.transcribe + classCapture.saveTranscription 2 方法已定义（params/result 类型完整）✅
- `src/contract/types.ts` Material：sourceType 含 'class_audio_transcription' + permissionConfirmed 字段已存在 ✅
- `src/data/schema/semester.sql.ts` materials：source_type CHECK IN 含 'class_audio_transcription' + permission_confirmed 字段已存在 ✅
- `src/data/schema/semester.sql.ts` normalized_texts：source_type TEXT 字段已存在 ✅
- `src/data/schema/semester.sql.ts` study_events：source_system CHECK 含 'S7' ✅

### 新建文件（15）

**S7 handler 层（src/agent-host/handlers/s7/，9 文件）**：
- `context.ts`：S7Context 类，复用 S1-S6 模式管理 global.db / semester.db 句柄缓存；额外注入 `WhisperCppAdapter` 接口（构造参数可选，默认 createMockWhisperAdapter）+ whisperCliPath/whisperModelPath（CLI/模型路径只来自配置）+ tmpRoot（原始音频暂存根目录，默认 <dataRoot>/tmp/class-capture），PI_STUDYBUDDY_DATA_ROOT 隔离注入
- `whisper-adapter.ts`：WhisperCppAdapter 接口 + 3 实现
  - createMockWhisperAdapter()：确定性返回固定文本，不调真实子进程（08-Test §5.4 全 mock），仍先调 validatePcmWav 服务端重新验证文件头
  - createFailingWhisperAdapter()：测试用失败路径，验证完文件头后抛 INTERNAL_ERROR + 固定文案"转写失败"
  - createRealWhisperAdapter(opts)：真实 spawn whisper.cpp CLI 框架（路径校验 + 文件头验证 + spawn 调用 + stdout 解析），集成测试不触发此路径，真实集成留待 E2E 受控夹具
- `wav-validator.ts`：validatePcmWav(filePath)，读取 44 字节头部字节级验证 8 项：文件存在且可读 + RIFF magic + WAVE magic + PCM format（0x0001）+ 单声道（0x0001）+ 16kHz 采样率（0x00003E80）+ 16-bit 位深（0x0010）+ 文件大小≥44 字节；错误消息固定文案"仅支持 PCM WAV 格式（16kHz/单声道/16-bit）"，不含文件路径；finally 关闭 fd
- `class-capture.ts`：2 方法
  - handleTranscribe：许可确认强制（permissionConfirmed=false → BAD_REQUEST + "需要课堂采集许可确认"）+ 提取 audioFilePath + 创建 tmp/class-capture/<request-id>/ 目录 + 调 WhisperCppAdapter.transcribe（内部先验证 PCM WAV 文件头，再转写）+ 成功返回 { transcription }（不返回 stdout 全文）+ 失败用 handler 层固定文案"转写失败"（防止 adapter 实现泄漏 stdout/stderr）+ finally 清理 tmp 目录（无论成功/失败）
  - handleSaveTranscription：参数校验（transcription/title 非空）+ findSemesterByCourseId 定位 semester.db + 创建 material（file_type='text' / source_type='class_audio_transcription' / status='converted' / permission_confirmed=1）+ 创建 normalized_text（content_hash=SHA-256 / char_count / source_type='class_audio_transcription' / UNIQUE(material_id)）+ 写 study_event（class_handoff_saved / source_system='S7' / source_ref_id=material_id）+ 返回 Material DTO
- `dto.ts`：mapMaterial，materials 行→Material DTO 映射对齐 ERD 字段（复用 S2 字段映射规则）
- `errors.ts`：notFound / badRequest / internalError（固定文案，不泄漏路径/stdout/stderr/密钥）
- `events.ts`：writeClassHandoffSavedEvent，写 study_events（event_type='class_handoff_saved', source_system='S7', source_ref_id=material_id）
- `lookup.ts`：findSemesterByCourseId，courseId → semester.db 定位（复用 S6 模式）
- `index.ts`：组装 2 handler 为 createS7Handlers(ctx)，导出 S7Context / WhisperCppAdapter

**S7 工具层（src/agent/tools/s7/，1 文件）**：
- `tools.ts`：2 个 studybuddy_* 工具（studybuddy_transcribe_class / studybuddy_save_transcription），TypeBox schema + execute 薄封装调 handler；工具名匹配 ^studybuddy_[a-z_]+$；导出 S7_TOOL_NAMES + S7_TOOL_COUNT

**测试文件（5 文件）**：
- `tests/unit/s7-wav-validator.test.ts`：14 测试（合法 PCM WAV 通过 + 拒绝 MP3/M4A/WebM/非 PCM/非 16kHz/非单声道/非 16-bit/空文件/截断头部/不存在文件 + 错误消息固定文案断言）
- `tests/unit/s7-whisper-adapter.test.ts`：8 测试（08-Test §3.3.2 三断言：路径未配置→INTERNAL_ERROR 不泄漏 / PCM WAV 文件头验证拒绝 / 转写成功不返回 stdout 全文 + mock 成功 + failing 失败 + real 路径校验 + 安全断言）
- `tests/unit/s7-class-capture.test.ts`：14 测试（transcribe：许可 false→BAD_REQUEST + 合法 WAV + mock 成功 + WAV 验证失败 + adapter 失败 + finally 清理成功/失败 + 安全断言不泄漏路径/stdout/stderr；saveTranscription：创建 material + normalized_text + study_event + content_hash 一致 + DTO 字段对齐 + courseId 不存在→NOT_FOUND）
- `tests/integration/s7-handlers.test.ts`：9 测试（跨库 handoff 到 S2：materials 行存在 + 字段值正确 + normalized_texts 行存在 + content_hash 一致 + UNIQUE(material_id) + study_events 行存在 + source_system='S7' + 返回 Material DTO 与 DB 行一致 + 跨学期隔离）
- `tests/unit/s7-tools.test.ts`：9 测试（工具数量=2 + 工具名匹配 + 必填字段 + studybuddy_transcribe_class execute 成功/失败 + studybuddy_save_transcription execute 成功/失败 + 安全断言）

### 修改文件（4）

- `src/agent/studybuddy-extension.ts`：接入 S7Context + createS7Tools，注册 2 个工具（头部注释更新 S7 2 工具 + setup 内追加 s7Ctx 创建 + s7Tools 注册循环），总工具数 24→26
- `tests/unit/studybuddy-extension.test.ts`：扩展装配测试同步更新 24→26（registerTool 调用次数 + 工具名数量 + 工具名集合含 S7 2 工具 + describe/it 描述更新）
- `tests/integration/studybuddy-extension-contract.test.ts`：扩展契约集成测试同步更新 24→26（registerTool 调用次数 + 工具名数量 + 多次调用 factory 72→78 + describe/it 描述更新）
- `docs/04-任务清单-Todo-List.md` + `docs/00-文档索引-Index.md` + `AGENTS.md`：版本号同步（§3.1 表格 + §七当前状态 + §八版本历史 + §12 修订记录）

## 3. 偏差

无偏差。计划与实现完全一致：
- 计划 9 源文件 + 1 工具文件 + 5 测试文件，实际交付 9 + 1 + 5 = 15 新建文件
- 计划 4 修改文件（extension + 2 扩展测试 + 3 文档），实际修改 6 文件（含 00-索引/04-Todo/AGENTS.md 版本同步）
- 计划测试用例数：单件 wav-validator 11 + whisper-adapter 3 + class-capture 12 + 集成 s7-handoff 5 + tools-s7 6 = 37；实际 14 + 8 + 14 + 9 + 9 = 54（超出计划，覆盖更全面）

## 4. 问题及根因

**问题 1：扩展装配测试期望 24 工具但实际 26 工具**
- 现象：pnpm test 全量运行时，tests/unit/studybuddy-extension.test.ts + tests/integration/studybuddy-extension-contract.test.ts 共 5 测试失败，期望 24/72 但实际 26/78
- 根因：S7 工具接入 studybuddy-extension 后总工具数从 24 增至 26，但扩展装配测试仍期望旧值 24
- 修复：更新两个测试文件的期望值 24→26（含单次调用 24→26 + 三次调用 72→78）+ 工具名集合补 S7 2 工具 + describe/it 描述更新
- 验证：修复后全量 509 测试全绿

**无其他问题**：前置 DTO/schema 已就绪无需修改（与 T-M1-001/T-M1-002 不同），PCM WAV 文件头验证、WhisperCppAdapter 可注入、handoff 到 S2、finally 清理、错误消息固定文案等关键实现一次通过。

## 5. 关键决定及依据

| 决定 | 依据 |
|---|---|
| S7 属 M2 第 3 任务，序号 003 | 04-Todo §6.4（M2 范围含 S7）+ §7.3.1 M2 任务登记表 |
| PCM WAV 文件头服务端字节级验证（不信任浏览器 MIME） | 07-WF §2.7 关键约束 + 05-ERD §3.2.1；44 字节 8 项校验（RIFF/WAVE/PCM/16kHz/单声道/16-bit） |
| WhisperCppAdapter 可注入接口（mock/failing/real 三实现） | 08-Test §3.3.2 + §5.4 不连真实外部服务；参考 S3 QuestionGenerator / S5 MockExamGenerator / S6 ReportPolisher 模式；默认 createMockWhisperAdapter + createFailingWhisperAdapter 测试失败路径 + createRealWhisperAdapter 真实 spawn 框架（集成测试不触发） |
| 错误消息固定文案不泄漏路径/stdout/stderr/密钥 | 07-WF §2.7 关键约束 + 03-Arch §3.3；3 条固定错误码：文件头验证失败→BAD_REQUEST + "仅支持 PCM WAV 格式（16kHz/单声道/16-bit）" / whisper.cpp 路径未配置→INTERNAL_ERROR + "语音转写未配置，请在设置中指定 whisper.cpp 路径" / 转写失败→INTERNAL_ERROR + "转写失败，请检查音频文件是否完整" |
| 原始音频 tmp/class-capture/<request-id>/ finally 清理 | 07-WF §2.7 关键约束：不留存原始音频；finally 块无论成功/失败都清理 |
| handoff 到 S2 复用既有表（不建独立 S7 表） | 07-WF §2.7 + 05-ERD §3.2.1/§3.2.2；materials.source_type='class_audio_transcription' + normalized_texts.source_type='class_audio_transcription' + study_events.source_system='S7' 字段已存在 |
| 不返回 stdout 全文，只返回 { transcription } | 08-Test §3.3.2 断言 3：转写成功不返回 stdout 全文 |
| CLI/模型路径只来自配置（不猜路径不回退云端） | 03-Arch §3.3：whisper.cpp Adapter 设计契约；createRealWhisperAdapter 路径校验 cliPath/modelPath 非空，否则 INTERNAL_ERROR |
| 前置 DTO/schema 已就绪无需修改 | T-M0-002 contract 类型化契约 + T-M0-006 数据层 schema 已含 S7 所需字段（source_type / permission_confirmed / source_system CHECK 'S7'） |
| 不实现真实 whisper.cpp 子进程集成测试 | 08-Test §5.4 不连真实外部服务；全部 mock，真实集成留待 E2E 受控夹具 |

## 6. 测试证据

**S7 专项测试**：
- 单件 wav-validator：14 测试全绿（合法 PCM WAV 通过 + 拒绝 MP3/M4A/WebM/非 PCM/非 16kHz/非单声道/非 16-bit/空文件/截断头部/不存在文件 + 错误消息固定文案断言）
- 单件 whisper-adapter：8 测试全绿（08-Test §3.3.2 三断言 + mock 成功 + failing 失败 + real 路径校验 + 安全断言）
- 单件 class-capture：14 测试全绿（transcribe 许可/WAV/adapter/finally/安全 + saveTranscription material/normalized_text/study_event/content_hash/DTO/NOT_FOUND）
- 单件 s7-tools：9 测试全绿（工具数量 + 工具名 + 必填字段 + 2 工具 execute 成功/失败 + 安全断言）
- 集成 s7-handlers：9 测试全绿（跨库 handoff materials/normalized_texts/study_events + content_hash + UNIQUE + DTO 一致 + 跨学期隔离）
- 合计：54 测试全绿

**全量测试**：
- 34 test files，509 tests passed，0 failed，0 type errors
- Duration: 24.64s

**质量门**：
- type-check ✅（tsc 双配置全通过）
- build ✅（tsc + vite build 成功，37 模块）
- test ✅（509 passed）
- smoke ✅（6/6 passed：build 产物 10 项 + RPC system.ping 往返 + global.db 4 表建库 + semester.db 25 表建库 + credential-vault set→get 往返 + 安全不变量六条）
- docs-governance ✅（13 份设计文档 + 2 Skill + 2 prompt，1 条既有警告不阻塞）
- git diff --check ✅（无 whitespace 错误）

**测试隔离**（AGENTS.md §5.3）：
- s7-wav-validator.test.ts → H:\pi-studybuddy-tmp\runs\T-M2-003\unit-wav-validator
- s7-whisper-adapter.test.ts → H:\pi-studybuddy-tmp\runs\T-M2-003\unit-whisper-adapter
- s7-class-capture.test.ts → H:\pi-studybuddy-tmp\runs\T-M2-003\unit-class-capture
- s7-tools.test.ts → H:\pi-studybuddy-tmp\runs\T-M2-003\unit-tools
- s7-handlers.test.ts → H:\pi-studybuddy-tmp\runs\T-M2-003\integration
- 绝不污染 %LOCALAPPDATA%\PiStudyBuddy

## 7. Git 证据

- 分支：agent/T-M2-003-s7-class-capture
- commit：18d15f7 feat(s7): S7 课堂采集工具注册 + API（25 文件，+2838/-53）
- 合并：git merge --ff-only（aba93e9..18d15f7 Fast-forward）
- 推送：git push origin master（aba93e9..18d15f7 ✅）
- master 复验：working tree clean，up to date with origin/master
- §8.4 三者齐全：docs/04 v0.1.26 done ✅ + master 18d15f7 ✅ + origin/master 推送 ✅

## 8. 未解决事项/下一步约束

- 待用户授权提交推送（AGENTS.md §8.4 三者齐全才算完成：docs/04 done + master 复验 + origin/master 推送）
- 真实 whisper.cpp 子进程集成待 E2E 受控夹具（08-Test §3.3.2 注释，当前全部 mock）
- 09-UI S7 标签页业务 UI 待壳层任务
- agent-host Host RPC 接入待壳层 UI 任务统一补全（与 S1-S6 一致）
- E2E-05 课堂采集→S2 handoff 待 M2 后续测试任务
- TTS / 备份恢复 独立任务，不预选
- M2 剩余 6 任务：TTS / 备份恢复 / S5-S7 UI / E2E-04~09
