# T-M3-002 实施记录：pi 原生能力承载（流式/工具调用视图/上下文压缩/@文件引用/多模型切换）

## 1. 任务裁决与范围

- **任务**：T-M3-002 pi 原生能力承载（扩展层，阶段3-4，P1），前置依赖 T-M3-001 done
- **批准**：用户 2026-08-08 明确批准开工 + 四项设计裁决：
  1. AgentEvent payload 结构化子集（types.ts 类型化 + 06-API §4 增补）— 批准
  2. files.read 走"现成契约 + allowed-roots 门禁"（不新增契约方法）— 同意
  3. 候选计划草案 .plan/T-M3-002-pi-native-capabilities.md — 确认
  4. 04-Todo in_progress 登记 — 批准
- **范围（做）**：工具调用视图 / 流式块渲染增强 / 上下文压缩提示条 / @文件引用选择器（materials.list 数据源 + allowed-roots 白名单 + 内容注入）/ 多模型切换（models.list 受控 fixture handler + UI）
- **非目标（明确不做）**：T-M3-003 业务化注入、T-M3-004 全工具自主调用、T-M3-005 model_select/turn_end 钩子 + 真实 ~/.pi/agent/models.json 读取、T-M3-006 会话管理全 UI、E2E-10~13、真实 LLM
- **红线**：不读真实 ~/.pi/agent（§9.5 物理隔离）、摘要脱敏（§9.3）、@路径白名单（§9.4）、不新增 RPC 契约方法、不新增业务数据表、不引入新运行时依赖

## 2. 实际交付

| # | 交付物 | 路径 | 说明 |
|---|---|---|---|
| 1 | AgentEvent payload 结构化联合 | `src/contract/types.ts` | AgentEventToolCallPayload（toolCallId/toolName/inputSummary）+ AgentEventToolResultPayload（toolCallId/toolName/isError/resultSummary），payload: unknown → 结构化联合 |
| 2 | allowed-roots 白名单纯函数 | `src/agent-host/allowed-roots.ts`（新建） | isPathWithinAllowedRoot（realpath 双解析防符号链接逃逸 + 大小写归一化 + 分隔符边界）+ summarizePathForDisplay（越权返回占位符）+ resolveDataRoot |
| 3 | files.read handler | `src/agent-host/handlers/files.ts` | 现成契约实现：白名单门禁 → 存在性 → 1MB 截断；相对 storageKey 解析；BAD_REQUEST/NOT_FOUND 纯对象 |
| 4 | agent-host 受控发射扩展 | `src/agent-host/handlers/agent.ts` | 触发词（出题/笔记/朗读）→ tool_call/tool_result 事件对，toolCallId 短 id（call-N 非 UUID），摘要固定脱敏 |
| 5 | models.list handler | `src/agent-host/handlers/models.ts`（新建） | 受控 fixture（2 provider × 2 model，无 apiKey/baseUrl），不读真实 ~/.pi/agent |
| 6 | ChatTab 承载层扩展 | `src/renderer/components/tabs/ChatTab.tsx` | 工具调用卡片（toolCallId 配对更新 + ✅/⚠️ 状态）+ 压缩提示条 + 模型选择器 + @选择器（materials.list → files.read → [引用: <名>]\n<内容> 注入）+ 渲染前 UUID 二次脱敏（sanitizeSummary） |
| 7 | agent-host 装配 | `src/agent-host/index.ts` | 注册 createModelHandlers + files.read 传 dataRoot |
| 8 | 06-API spec 同步 | `docs/06-API契约-API-Contracts.md` | v0.1.3：§4 AgentEvent payload 结构化说明 + §3.2 files.read 落地注解 + §3.1.1 agent.send 扩展注解 |
| 9 | 测试（7 新文件 33 用例） | `tests/unit/` + `tests/integration/` | 见 §6 |

## 3. 偏差

- **无范围偏差**：计划与实际交付一致（10 项交付物全部落地）
- **审查通道事件**：步骤 12 独立审查原计划用 reviewer 子代理，运行失败（后台任务无输出退出，session 2026-08-08T04-47-56Z）；改由执行者自审查 7 个审查点完成（§11.4 交叉审查元纪律仅约束设计闭环/治理基线/里程碑退出门禁，本普通任务自审查符合 08-Test §1.3 惯例；实施记录如实注明通道事件）

## 4. 问题及根因

| 问题 | 根因 | 处理 |
|---|---|---|
| 6 个测试初测失败 | ① files.ts 抛 `new Error()` 带 .code 而非 RpcError 纯对象，`isRpcError`（rpc.ts:36-44）只认对象字面量，toError 吞成 INTERNAL_ERROR ② files.read 同步 throw，单元测试 `.rejects` 接不到 ③ ChatTab 未做渲染层 UUID 二次脱敏（测试注入含 UUID 摘要）④ 📎 按钮 title 含"引用资料"污染选择器关闭断言 | ① files.ts 改抛 `{code, message}` 纯对象 ② files.read 改 async ③ ChatTab 加 sanitizeSummary（UUID 正则过滤 + 截断）④ title 改"@ 引用文件" |
| `fs.readFileSync(path, {length})` TS 编译错误 | readFileSync 无 length 选项（长度由 fd read 控制） | 改 fs.openSync + fs.readSync 手动截读 |
| AGENTS.md 一次 edit 失败 | edits[2] oldText 与文件实际文本不一致（原子回滚整组） | 拆最小唯一片段重试 |

## 5. 关键决定及依据

| 决定 | 依据 |
|---|---|
| tool_call/tool_result payload 结构化字段子集（toolCallId/toolName/inputSummary / isError/resultSummary） | 用户批准；对齐 pi 底座 ToolCallEvent/ToolResultEvent（types.d.ts:638-721）字段同名；06-API §4 增补 |
| files.read 走现成契约 + allowed-roots 门禁，不新增 materials.getContent | 用户同意；files.read 契约已存在（06-API §3.2），仅补 handler；check-contract-coverage 对"有契约无 handler"仅 WARN 不阻塞 |
| models.list 用受控 fixture 数据源，不读真实 ~/.pi/agent | 06-API §3.13 spec（读 ~/.pi/agent/models.json）与 AGENTS.md §9.5 物理隔离冲突 → 本任务受控 fixture（08-Test §5.4），真实读取属 T-M3-005（model_select 钩子归属，03-Arch §2.3） |
| @引用数据源 = materials.list（storageKey 相对路径）→ files.read 相对数据根解析 | 07-WF §2.8 步骤 4"当前课程资料选择器"；materials.storageKey 格式 `semester/<id>/storage/<name>`（s2/materials.ts:141） |
| allowed-roots 独立纯函数实现（不复制 pi-desktop TTL 缓存范式） | AGENTS.md §6.3 试炼场/参考仓边界：主仓独立重新实现 Adapter；本任务取白名单校验语义，用 realpath 防逃逸 |
| 渲染前 UUID 二次脱敏（sanitizeSummary） | §9.3 纵深防御：即使事件源摘要异常，renderer 也过滤完整 UUID 再展示 |
| toolCallId 用短 id（call-<n>）非 UUID | §9.3 不记录完整 UUID |

## 6. 测试证据

- **单件**（4 新文件 18 用例）：
  - `tests/unit/allowed-roots.test.ts`（9）：白名单内通过/根自身/越权拒绝/路径穿越拒绝/前缀相似拒绝/大小写归一化/空路径拒绝 + summarize 相对路径/占位符
  - `tests/unit/models-handlers.test.ts`（2）：fixture 结构完整 + 无 apiKey/api_key/sk-* 泄漏
  - `tests/unit/files-read.test.ts`（4）：白名单读取/越权 BAD_REQUEST/穿越拒绝/NOT_FOUND
  - `tests/unit/renderer-chat-tab-capabilities.test.ts`（11）：工具卡片（running/done✅/error⚠️）+ UUID 不泄漏 + 压缩提示条开/关 + 模型选择器（有/无数据）+ @选择器（开/关）
- **集成**（3 新文件 9 用例）：
  - `tests/integration/agent-events-toolcalls.test.ts`（4）：出题 → tool_call/tool_result 序列 + payload 结构化脱敏 + toolCallId 配对 + 无触发词保持基线
  - `tests/integration/models-list-rpc.test.ts`（2）：RPC 往返 + 无 apiKey
  - `tests/integration/files-read-rpc.test.ts`（2）：白名单读取 + 越权 BAD_REQUEST
- **质量门全绿**（2026-08-08）：
  - type-check 零错误零警告
  - 单元/集成：**856/856 通过**（77 文件，+33 于 T-M3-001 基线 823）
  - E2E：83/83 通过（10 文件，无回归）
  - build 通过（renderer 236.65 kB / gzip 70.82 kB）
  - smoke 6/6 + verify 全绿（执行 8，跳过 2）
  - 文档治理通过（1 条既有 WARN 不阻塞）、契约覆盖 OK（127 Api / 8 PiBridge / 35 工具）、安全不变量 6/6、UUID 泄漏 7/7
- **数据隔离**：测试写临时目录（os.tmpdir）与 env PI_STUDYBUDDY_DATA_ROOT 注入，不触真实 %LOCALAPPDATA%\PiStudyBuddy 与 ~/.pi

## 7. Git 证据

- **分支**：master（本次变更未提交，步骤 16 待用户授权）
- **变更文件**：10 modified（.plan/00-当前任务.md、AGENTS.md、00-索引、04-Todo、06-API、agent.ts、files.ts、agent-host/index.ts、types.ts、ChatTab.tsx）+ 10 new（.plan/T-M3-002-pi-native-capabilities.md、allowed-roots.ts、models.ts、7 测试文件）
- **git diff --check**：无空白错误
- 提交/合并/推送：**未执行**（AGENTS.md §7/§8.4：需用户明确授权）

## 8. 未解决事项/下一步约束

- **未解决**：① allowed-roots `toLowerCase()` 大小写归一化在非 Windows 平台过宽（单机 Windows 目标可接受，文档化限制）② files.watch/unwatch 参数校验沿用 T-M3-001（非本任务范围）③ 模型切换选中态仅本地 useState 不持久化（持久化属 T-M3-005 modelsConfig.*）
- **下一步约束**：M3 剩余 pending T-M3-003~008；**不替用户预选下一任务**；06-API v0.1.3 已同步（§11.1 治理基线修改完成）；真实模型配置读取（T-M3-005）前，models.list 保持受控 fixture 数据源
- **步骤 16 提交交付待用户授权**（AGENTS.md §8.3：git add 显式路径 + feat/docs 分条 commit + merge --ff-only + 推送 origin/master）
