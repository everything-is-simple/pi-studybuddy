# T-M4-011 独立审查 A（架构/契约/测试）

**日期**：2026-08-10
**方式**：独立只读审查；未修改文件、未执行 Git 收口。

## 审查结论

- P0：0。
- P1：3，故不建议 Git 收口：
  1. `materials.upload` 仍只登记 metadata，未把选中文件受控导入业务 storage，`size: 0` 也不是真实大小；当前只能证明 renderer→RPC 接线，不能证明上传后可真实转换。
  2. 归档只读已经在 renderer 层拦截，但 S2 agent-host 写 handler 仍可被直接 RPC 绕过，需另行在 host 侧按 semester archived 状态拒绝。
  3. Node24.14.0/pnpm11.20.0 目标基线和完整质量门未通过；当前 Node25 全量仅 1034/1036 tests 通过。
- P2：4：`pending_quality_check` 无 UI 恢复动作、未接 `jobs.progress`、转换/生成笔记刷新与同 key action token 测试仍可加强、Electron E2E 不是 renderer dialog/真实导入 E2E。

## 已复核的修复

- dialog 返回后的跨课程旧上传已被 context token 丢弃；有 mounted happy-dom 回归测试。
- 归档课程 UI 写按钮禁用；有回归测试。
- action 递增 id 防止旧 Promise 清理新动作。
- 上传成功后 list 动态刷新已有测试。
- main open dialog 已仅限 `openFile`。
- TypeScript courseId 收窄已通过 type-check。

## 约束

任务继续 `in_progress`；不得将当前实现称为完整资料生产闭环，不得 commit/merge/push，不启动 T-M4-012。
