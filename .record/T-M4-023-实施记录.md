# T-M4-023 实施记录：独立交叉审查问题修订

## 1. 任务裁决与范围

- 任务：T-M4-023，处理审查者 A（架构/契约）与审查者 B（治理/任务）独立洞集的交叉审查问题。
- 用户于 2026-08-09 明确要求完成修订；本记录不替换原任务历史，不启动 T-M4-011~021。
- 范围：生产 `agent.send` 模型路径与 fixture 边界、loader 运行时模型装配、API/IPC 契约覆盖阻塞、真实 Electron 代表性路由、P0 历史审计恢复、credential-vault 拓扑、默认对话入口及 00-12 文档元数据。
- 非范围：真实外部 LLM/SMTP/飞书/WPS/whisper 调用、其他业务 Tab 接线、Git commit/merge/push。

## 2. 实际交付

- 生产 agent-host 不再静默使用 `runMockFixture`；没有可用模型时返回固定 `MODEL_NOT_CONFIGURED`，fixture 仅通过 VITEST 显式注入。
- `createStudyBuddySession` 从业务数据根模型配置与 credential-vault 解密凭证创建无磁盘 `ModelRuntime`；设置页展示名按受控映射解析为 pi runtime model ID；不读取 `~/.pi` 模型/凭证。
- 契约覆盖脚本递归解析生产 handler registry 与 spread，未解析、缺失、重复、未知 handler 均阻塞；IPC 常量通道与 main 注册覆盖同步校验。
- 补齐生产 handler surface 的安全占位/边界实现（files、skills、models）及 8 个桌面 IPC 注册端点，未引入外部联网行为。
- 真实 Electron 生产路径覆盖 `system.ping`、`semesters.list` 与无模型 `agent.send` 固定错误；同步 00-12 文档版本、credential-vault 实际拓扑和默认对话入口说明。
- 新增本记录作为审计恢复记录：明确 T-M4-001~005 历史批次未逐任务留存计划/记录的事实，不伪造历史证据；T-M4-010 记录补充“当时关闭提交”与当前基线的区别。

## 3. 偏差

- 无法回溯重建 T-M4-004/T-M4-005 当时不存在的独立计划文件或单任务记录；本记录只登记审计发现、现存代码/测试证据和恢复性验证，不把事后验证冒充原始收尾证据。
- 本轮不执行 Git 收口，因此 T-M4-023 保持 `in_progress`；正式完成判据仍需用户另行授权后按 AGENTS.md §7/§8 执行。

## 4. 问题及根因

- 原生产 host 为了让无模型 session 可运行，直接调用测试 fixture，造成“E2E 通过但生产不是真实模型路径”的 P0。
- 原 loader 只注入 extension，不构造业务配置对应的 pi model runtime；settings 中的 DeepSeek 展示名也没有解析为 pi runtime ID。
- contract checker 只做浅层对象属性与字面量 IPC 扫描，未覆盖 spread registry、常量 channel 和缺失 handler 阻塞。
- 早期设计文档保留了 pi-desktop 的 `credential-vault/*.enc` 描述，而实现实际使用 `config/credentials.json`；09-UI 早期目标句与默认 Tab 实现及 PRD/Workflow/Test 不一致。

## 5. 关键决定及依据

- 生产与测试路径硬隔离：依据 AGENTS.md §5.4、§9.2、§9.5 与 08-Test §5.4；测试 fixture 只能显式注入，生产不允许 fallback。
- 未配置模型使用固定可操作错误 `MODEL_NOT_CONFIGURED`，不暴露 provider、路径、凭证或异常栈；依据 06-API §2.2、02-PRD §5.3。
- 继续保持 `Api` 方法总数 127，不以删除/隐藏未实现契约来“通过”覆盖；对当前未启用能力使用显式安全占位或固定错误；依据 AGENTS.md §2、§11.2。
- 历史审计缺口通过本次恢复记录显式记录，不 retro-create 原始计划；依据 AGENTS.md §4.5、§7、§11.2、§11.4。

## 6. 测试证据

- 验证运行时：Node `v24.14.0`；完整质量门命令 `node scripts/verify.mjs --stage=full` 退出码 0；日志：`H:\pi-studybuddy-tmp\runs\T-M4-023\verify-full-node24.log`。
- 完整质量门：unit/integration **104 files / 1028 tests passed**；真实 Electron E2E **16 files / 118 tests passed**。
- 契约覆盖：**127/127 API handlers**、**35 registerTool tools**、**8 IPC channels**；未解析、缺失、重复、未知 handler 与 IPC 常量覆盖均按阻塞规则通过。
- 安全与运行：desktop security **6/6**；smoke **6/6**；UUID 泄漏独立检查 **7/7**。
- 文档治理：`scripts/check-docs-governance.mjs` 通过，**13 design docs + 2 Skills + 2 prompts**。
- 质量差异：`git diff --check` 通过，无空白错误；PowerShell 报告的 LF→CRLF 提示不是 diff-check 失败。
- 定向证据：agent 事件/RPC 与真实内核边界 **2 files / 11 tests**；loader **1 file / 5 tests**（含 UI 展示名到 runtime ID 映射）；真实 Electron 生产代表性路径通过 `semesters.list` 与 `agent.send` 的 `MODEL_NOT_CONFIGURED` 断言。

## 7. Git 证据

- 实施分支：`agent/T-M4-023-cross-review-remediation`。
- 基线：`6cd1e903ea8622f1afd5678264de29f01b66b62a`。
- 当前未获 commit/merge/push 授权；未执行上述操作，不能报告正式完成。

## 8. 未解决事项/下一步约束

- 本地实施与 Node 24.14.0 完整验收已完成；T-M4-023 仍保持 `in_progress`，因为尚未获得 Git commit/merge/push 授权，尚不满足 AGENTS.md §8.4 的正式完成判据。
- 需用户另行授权后，才可按 AGENTS.md §8.2/§8.4 显式路径提交、快进合并、master 复验并推送；在此之前不启动 T-M4-011~021。
- 真实 provider 的网络连通性与目标机器接受测试仍未完成；本轮仅验证受控配置装配和无配置安全失败边界。
