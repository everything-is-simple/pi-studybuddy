# T-M5-005 实施记录：S6/S7/TTS/备份/设置与整体 UX 修订

**状态**：in_progress（功能/证据提交已合并至 master、定向回归和完整质量门通过；原生 Electron UAT 仍有未覆盖路径；两轮独立审查已完成，最终结论 P0/P1=0）
**日期**：2026-08-14
**任务计划**：`.plan/T-M5-005-s6-s7-tts-backup-settings-ux.md`
**实施分支**：`agent/T-M5-005-s6-s7-tts-backup-settings-ux`
**运行根**：`H:\pi-studybuddy-tmp\runs\T-M5-005\`

## 1. 任务裁决与范围

用户明确批准 T-M5-005 开工。范围是既有 S6 报告、S7 课堂采集、TTS、备份恢复、设置和整体壳层 UX 的事实盘点、RED→GREEN、定向回归和真实 Electron 隔离 UAT。T-M5-006 依赖自包含、T-M5-007 干净机 UAT、T-M5-008 最终发行不属于本任务；不新增 API/schema/handler/stream/跨 Tab 状态，不连接真实外部服务，不读写 `%LOCALAPPDATA%\PiStudyBuddy`。用户已明确授权本轮任务改动的 commit/merge/push；实际 Git 事实仅在执行后登记。

## 2. 实际交付

- 新增 `tests/integration/t-m5-005-renderer-ux.test.ts`，覆盖 S5 速背卡、S7 转写结果、S6 报告详情的 TTS 文本净化边界，以及备份 `overwrite` 的确认/取消边界。
- `CramTab.tsx` 新增可选 `onSpeakText` 和「朗读速背卡」，朗读文本仅来自净化后的可见卡片内容。
- `CaptureTab.tsx` 新增可选 `onSpeakText` 和非空「朗读转写结果」，只传当前转写文本。
- `ReportTab.tsx` 新增可选 `onSpeakText` 和「朗读报告」，只传脱敏可见汇总/章节文本。
- `AppShell.tsx` 复用既有 `useTtsPlayback` 所有权，将朗读回调注入三个 Tab；未新增播放状态或 RPC。
- `BackupPanel.tsx` 对 `overwrite` 增加确认态；首次选择只显示覆盖风险，点击「确认覆盖」后才调用既有 `backup.restore`，`create_new` 流程不变。
- 原生 UAT 在隔离根 `native-uat-20260814/app-data-2` 完成首次创建后的重启回读：学期和课程均持久化；重启初始树折叠，展开后显示 `uat course`，选择后顶部恢复 `t m5 005 uat semester / uat course`。
- 本轮修正 `useTtsPlayback`：只有目标同时具备 `refType/refId` 时，停止后才显示「标记已复习」；报告等仅标题朗读不再显示不可持久化操作。可持久化笔记正向真实 Electron E2E 已通过，报告朗读停止边界经隔离 Electron 回归确认。

## 3. 偏差

- 设置重启回读：后续真实 Electron 可见截图 `native-uat-20260814/evidence/60-argv-settings-61-after-relaunch-settled.png` 显示设置页「每日目标时长」为 `61`；同一隔离根 `app-data-2/config/settings.json` 的 `dailyGoalMinutes` 为 `61`。早期专用 runner 的 `phase-two.json` 失败事实保留，但由该后续纯 UI 回读证据取代“未完成”结论。
- UAT 早期存在 Electron 白屏/退出实例，证据保留于 `native-uat-20260814/evidence/01-first-start.png`、`02-first-start-focused.png`、`03-load-wait-2s.png`、`03-load-wait-5s.png`、`03-load-wait-10s.png`；后续实例已通过可见 UI 完成向导和工作台操作，不能删除早期失败事实。
- `overwrite` 确认是保守的破坏性动作前置确认，不是实际同名冲突预检；当前接口没有冲突预检合同。
- 文件对话框截图中可见隔离运行目录/驱动标签，未写入 renderer；它们属于运行证据，不得复制到 DOM 或生产日志。

## 4. 问题及根因

- RED 初始为 `4 tests | 4 failed`：三个 Tab 缺少可见朗读入口，`overwrite` 直接调用恢复 RPC。根因是既有 TTS 控制条只覆盖常驻控制和部分文字区，三个计划范围内文字区未注入回调；备份恢复缺少破坏性选择后的确认边界。
- 重启后课程第一次未见不是数据丢失：应用恢复后学期树处于折叠态；通过可见展开操作显示了持久化课程。选择课程后上下文栏恢复，证明 UI 需要用户重新展开/选择而不是自动选中。
- 采集路径在合规确认已勾选、未选择 WAV 时正确保持「请选择录音文件开始转写」且「开始转写」禁用；本轮没有把未完成文件选择误报为转写成功或依赖自包含通过。
- 独立代码审查发现新增冻结键缓存跨学期未清空，可能在碰撞 `reportKey` 下泄漏“已冻结”状态；已在学期切换 effect 清空缓存，并新增跨学期碰撞回归，定向报告 RPC 11/11 通过。

## 5. 关键决定及依据

- 只修 RED 证明的 renderer 缺口，复用既有 `onSpeakText(text, target)` 和 `useTtsPlayback`；依据 `.plan/T-M5-005` §6.2、docs/07 TTS 朗读边界和 AGENTS.md §5。
- 朗读文本采用「已显示 + 已净化」原则，不传绝对路径、完整 UUID、凭据、base URL、错误栈或资料原文；依据 AGENTS.md §9.3、docs/13 §4/§6。
- `overwrite` 先确认再 RPC；不得把确认文案称为冲突检测；依据 docs/09 恢复危险动作边界和现有 backup contract。
- 重启 UAT 使用隔离数据根，不使用 renderer 注入、CDP、`webContents.executeJavaScript`、handler/RPC 直调或数据库预置；依据 AGENTS.md §7 和 docs/traceability/release-evidence.md。

## 6. 测试证据

| 门 | 命令/证据 | 结果 |
|---|---|---|
| RED | `H:\pi-studybuddy-tmp\runs\T-M5-005\red-renderer-ux.log` | 4 tests failed，缺口与预期一致 |
| GREEN 定向合同 | `C:\node-v24.14.0-win-x64\node.exe node_modules/vitest/vitest.mjs run tests/integration/t-m5-005-renderer-ux.test.ts` | 1 file / 4 tests passed，Type Errors 0 |
| 报告冻结回归 | `C:\node-v24.14.0-win-x64\node.exe node_modules/vitest/vitest.mjs run tests/integration/t-m4-016-report-rpc.test.ts` | 1 file / 11 tests passed；覆盖返回列表保留冻结和跨学期相同 reportKey 隔离 |
| TypeScript | `pnpm type-check`（Node24 shell） | 退出码 0 |
| 受影响 Electron renderer 回归 | `vitest run -c vitest.e2e.config.ts` 分别运行 `t-m4-018-tts-renderer.test.ts`、`t-m4-016-report-renderer.test.ts`、`t-m4-019-backup-renderer.test.ts`、`t-m4-017-capture-renderer.test.ts`、`t-m4-020-settings-renderer.test.ts` | TTS 1/1、报告 2/2、备份 1/1、采集 2/2、设置 1/1 通过；均为自动化回归，不等于真机 UAT |
| TTS 定向合同 | `vitest run tests/integration/t-m4-018-tts-rpc.test.ts tests/integration/t-m5-005-renderer-ux.test.ts` | TTS 11/11 通过；无引用标题停止后无「标记已复习」，笔记引用仍可标记；已有 React `act(...)` warnings 保留 |
| TTS 构建复验 | `pnpm type-check`；`pnpm build` | 均退出码 0；Node24 shell，build 61 modules transformed |
| 报告朗读隔离回归 | 新构建隔离 Electron `app-data-2`，报告详情→朗读→停止 | 控制条显示「家长报告 · 已停止」，未出现「标记已复习」；CDP 回归仅作行为检查，不计入原生 UAT |
| 资源异常 | `pnpm test:e2e -- tests/e2e/...` 首次误用 `--` 导致全套 E2E 并发；随后单文件过滤运行 | 全套误用命令因 Electron 多进程 OOM 失败；单文件定向结果以上述通过项为准，不判为业务回归 |

## 7. Git 证据
- 集成：`112da31` 已 ff-only 合并并推送 `master`；治理事实提交 `87d40f8` 已推送。实时核验当前 `HEAD=origin/master=2442646cfcfa07c0b1f84de882816a9990c2bd24`，`112da31` 与 `87d40f8` 均为其祖先。
 - 独立审查 A：发现冻结缓存跨学期 P1；已修复 `effectiveSemesterId` 切换时清空 `frozenReportKeysRef`，并由 S6-RED-05 覆盖。
 - 独立审查 B：复核修复后确认跨学期相同 `reportKey` 不继承冻结状态；无 P0/P1，结论 PASS。
- 独立代码审查：第三轮复核 `AppShell` TTS 回调注入、`useTtsPlayback` 复习资格、`BackupPanel` 覆盖确认、`ReportTab` 冻结缓存和敏感信息边界，结论 PASS，P0/P1=0。
- 独立治理审查：复核 `AGENTS.md`、唯一计划、`docs/00`、`docs/04`、release evidence、interaction catalog 的版本、Git 祖先关系、任务状态和 UAT/自动化边界，结论 PASS，P0/P1=0；确认任务必须保持 `in_progress`。

## 8. 未解决事项/下一步约束

- T-M5-005 仍为 `in_progress`，不得报告完成或启动 T-M5-006~008。
- 已修正 `ReportTab` 的本会话冻结状态：成功完成 `reports.freeze` 后按 `reportKey` 保留已验证状态，返回列表再进入详情仍显示“已冻结”；该状态不冒充跨重启持久化证据。
- 设置重启回读已由真实 Electron 纯 UI 证据 `native-uat-20260814/evidence/60-argv-settings-61-after-relaunch-settled.png` 和隔离 `app-data-2/config/settings.json` 证明；早期专用 runner 失败保留为历史事实。
- S6 报告投递重启循环、真实备份恢复后业务数据完整性/重启回读、TTS 原生已复习持久化仍未覆盖。
- 本轮 `docs/traceability/`、`docs/04`、`docs/00` 与 AGENTS Git 事实已同步；实时 `master=origin/master=2442646cfcfa07c0b1f84de882816a9990c2bd24`。S6 报告投递重启、S7 成功转写/结果回读、真实备份恢复后业务数据回读、TTS 原生已复习持久化等原生 UAT仍未完成；独立代码审查与治理审查均 PASS，P0/P1=0；任务仍 in_progress。
- 真实恢复、升级、卸载、文件—数据库补偿和孤儿处理继续保持未覆盖；这些证据不能由本轮自动化替代。
- 用户已决定暂置当前客观缺口并重装干净 Windows；后续运行 setup，在真实已安装应用中完成三条原生 UI 闭环后再判断任务是否达到收尾条件。当前不继续扩展业务代码；安装器问题或真实使用问题出现后，按证据针对性修复。
