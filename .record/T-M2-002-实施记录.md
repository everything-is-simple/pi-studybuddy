# T-M2-002 实施记录

**任务**：S6 家长报告（规则生成/冻结/脱敏/投递）工具注册 + API
**里程碑**：M2 完整闭环（第 2 任务）
**日期**：2026-08-07
**状态**：✅ 完成（待用户授权提交推送）

---

## 1. 任务裁决与范围

**权威依据**：04-Todo §7.3（M2 任务大纲 S6）+ §6.4（M2 范围 + 退出门槛含"家长报告 UUID 泄漏检测通过"+"投递渠道独立失败隔离通过"）+ 07-WF §3（S6 报告生成流程：规则优先 + AI 仅润色 + 冻结快照 + assertNoSensitiveLeak + 渠道独立失败隔离 + 至少一次投递 + 最多重试 3 次 + retained_locally）+ 06-API §3.8（S6 11 方法：reports 4 + deliveries 3 + reportTargets 4）+ 05-ERD §2.2（parent_report_targets 全局库）+ §3.6（parent_reports + parent_report_deliveries 学期库）+ 02-PRD §5.2（家长报告脱敏边界）+ 03-Arch §3.1（studybuddy_* registerTool 工具 S6 清单）+ 08-Test §3.1/§3.2（单件/集成断言）+ §5.4（assertNoSensitiveLeak UUID 泄漏检测）+ §5.5（AI 降级）+ §6.2（E2E-06 S6 家长报告）。

**范围**：实现 S6 家长报告全链路——11 个 RPC handler（reports.generate/freeze/get/list + deliveries.deliver/retry/list + reportTargets.list/create/update/delete）+ 3 个 studybuddy_* registerTool 工具 + S6Context 数据层句柄管理 + ReportGenerator 规则生成器（聚合 S1-S5 摘要，6 section）+ ReportPolisher 可注入 AI 润色接口（默认 mock，失败降级保留规则报告）+ DeliveryChannel 可注入投递渠道（4 渠道独立失败隔离）+ leak-detector UUID 泄漏检测 + report 冻结状态机（draft→frozen）+ content_hash SHA-256 冻结快照 + study_events 写入 + DTO 对齐 ERD §2.2/§3.6 + studybuddy-extension 接入 S6 工具（共 24 工具）。

**非目标**：真实 SMTP/飞书 Webhook/AI 润色（全部 mock，08-Test §5.4 不连真实外部服务）；09-UI S6 标签页业务 UI（壳层任务）；E2E-06（属 M2 后续测试任务）；定期调度自动生成（daily/weekly/monthly/exam_reminder 自动触发属调度层，本任务仅支持手动触发）；print 渠道真实打印（mock 返回成功）；改 semester.db 表结构（T-M0-006 已定）；改 api.ts 方法签名（仅 types.ts DTO 对齐）；S7/TTS/备份恢复（独立任务）。

## 2. 实际交付

### 前置：DTO 对齐 ERD（src/contract/types.ts 修正）

修正 S6 DTO，按权威链 05-ERD（优先级4）> types.ts（优先级7）：
- ParentReport：补 reportKey/reportType/periodStart/periodEnd/status/contentHash/generatedAt/frozenAt 字段，status 用 `draft` | `frozen`
- ReportDelivery：补 reportKey/channel/status/sentAt/errorMessage/attemptCount/retainedLocally 字段
- ParentReportTarget：补 enabled/channelConfigJson 字段

### 新建文件（16）

**S6 handler 层（src/agent-host/handlers/s6/，13 文件）**：
- `context.ts`：S6Context 类，复用 S1-S5 模式管理 global.db / semester.db 句柄缓存；额外注入 `ReportPolisher` 接口 + `DeliveryChannel` 接口（构造参数可选，默认 mock），PI_STUDYBUDDY_DATA_ROOT 隔离注入
- `reports.ts`：4 方法（generate / freeze / get / list）
  - generate：规则报告生成（ReportGenerator 聚合 S1-S5 摘要 6 section：study_rhythm / materials / practice / mistakes / exam_reminder / data_quality）+ ReportPolisher 可注入 AI 润色（失败保留规则报告，不阻塞）+ assertNoSensitiveLeak UUID 泄漏检测（content_json 全文扫描 UUID 正则）+ status=draft + content_hash SHA-256
  - freeze：状态机 draft→frozen（frozen 后不可改）+ 冻结快照 content_json 不可变
  - get/list：只读查询
- `deliveries.ts`：3 方法（deliver / retry / list）
  - deliver：渠道独立失败隔离（4 渠道 local_export/smtp/feishu_webhook/print，单渠道失败不影响其他）+ DeliveryChannel 可注入 mock + 至少一次投递语义 + retained_locally 兜底 + study_events 写入（report_delivered, source_system='S6'）
  - retry：最多重试 3 次 + attempt_count 递增
  - list：只读查询
- `report-targets.ts`：4 方法（list / create / update / delete 软删）
  - create：channelType 校验（4 渠道白名单）+ channelConfigJson 合法 JSON 校验 + credentialKey 可选（真实渠道地址在 credential-vault，channelConfigJson 仅存别名）
  - delete：软删除（deleted_at 非空）
- `report-generator.ts`：规则报告生成器，跨库聚合 S1-S5 摘要（study_rhythm/materials/practice/mistakes/exam_reminder/data_quality 6 section）+ 确定性只读不依赖 AI + buildExamReminder 查询 assessment_attempts 用 confirmation_status='confirmed'
- `report-polisher.ts`：ReportPolisher 接口 + createMockReportPolisher() 默认确定性润色 + createFailingReportPolisher() 测试用失败降级
- `delivery-channels.ts`：DeliveryChannel 接口 + createMockDeliveryChannel() 默认 mock（4 渠道，单渠道失败不影响其他）+ createFailingDeliveryChannel() 测试用失败隔离
- `leak-detector.ts`：assertNoSensitiveLeak，content_json 全文扫描 UUID 正则（v4 UUID 格式），命中抛 PARENT_REPORT_PRIVACY_VIOLATION + 降级规则报告
- `dto.ts`：mapReport / mapDelivery / mapTarget，行→DTO 映射对齐 ERD 字段
- `errors.ts`：notFound / badRequest / internalError / privacyViolation（复用 S1-S5 模式 + 新增 privacyViolation）
- `lookup.ts`：findSemesterByReportKey，遍历非 archived 学期库查找报告所属学期
- `events.ts`：writeReportDeliveredEvent，写 study_events（event_type='report_delivered', source_system='S6'）；列名 event_data_json（非 metadata_json）
- `index.ts`：组装 11 handler 为 createS6Handlers(ctx)，导出 S6Context / ReportPolisher / DeliveryChannel

**S6 工具层（src/agent/tools/s6/，1 文件）**：
- `tools.ts`：3 个 studybuddy_* 工具（studybuddy_generate_parent_report / studybuddy_deliver_report / studybuddy_manage_report_targets），TypeBox schema + execute 薄封装调用 handler；导出 S6_TOOL_NAMES + S6_TOOL_COUNT 常量用于断言

**测试文件（2 文件）**：
- `tests/integration/s6-handlers.test.ts`：19 集成测试，覆盖 reports.generate 3（GEN-01~03：规则生成成功 + AI 润色失败降级 + UUID 泄漏检测拦截）+ reports.freeze 2（FREEZE-01~02：draft→frozen + frozen 不可改）+ reports.get/list 2 + deliveries.deliver 3（DELIVER-01~03：4 渠道独立失败隔离 + retained_locally 兜底 + study_events 写入）+ deliveries.retry 2（RETRY-01~02：attempt_count 递增 + 最多 3 次）+ deliveries.list 1 + reportTargets 4（CRUD-01~04：list/create/update/delete 软删）+ 安全不变量 2（SEC-01~02：UUID 泄漏检测 + 渠道独立失败隔离）
- `tests/unit/s6-tools.test.ts`：16 单件测试，覆盖工具集整体契约 3 + 每工具 ≥4 断言（generate_parent_report/deliver_report/manage_report_targets：ToolDefinition 必填 + execute 成功返回 {content, details} + details 字段 + execute 失败 throw Error）

### 修改文件（3）

- `src/agent/studybuddy-extension.ts`：导入 S6Context + createS6Tools；setup 内创建 s6Ctx + 注册 3 个 S6 工具（共 24 工具）
- `tests/unit/studybuddy-extension.test.ts`：registerTool 期望 21→24 + 工具名清单加 S6 3 工具
- `tests/integration/studybuddy-extension-contract.test.ts`：registerTool 期望 21→24（多次调用 63→72）+ 描述加 S6

### 计划文件（1 文件）

- `.plan/T-M2-002-S6-parent-report.md`：按 .pi/prompts/plan.md 模板创建，16 步开发流程

## 3. 偏差

| 偏差 | 原因 | 修正 |
|---|---|---|
| report-generator buildExamReminder 查询 assessment_attempts 用 `confirmed = 1` 失败 | assessment_attempts 表无 `confirmed` 列，实际列为 `confirmation_status`（05-ERD §3.1.2） | 改为 `confirmation_status = 'confirmed' AND deleted_at IS NULL` |
| events.ts 插入 study_events 用 `metadata_json` 列名失败 | study_events 表实际列名为 `event_data_json`（05-ERD §3.1.5） | 改为 `event_data_json` |
| TypeScript 类型错误：values 对象类型不匹配 | node:sqlite prepare().run() 参数类型要求 `Record<string, string \| number \| null>`，原 `Record<string, unknown>` 过宽 | 将 values 类型收窄 + 显式转换参数类型 |

## 4. 问题及根因

| 问题 | 根因 | 解决 |
|---|---|---|
| reports.generate 测试失败 "no such column: confirmed" | report-generator 误用 `confirmed = 1`，实际列为 `confirmation_status` | 修正为 `confirmation_status = 'confirmed'` + 补 `deleted_at IS NULL` |
| deliveries.deliver 测试失败 "no such column: metadata_json" | events.ts 误用 `metadata_json`，实际列名为 `event_data_json` | 修正列名对齐 05-ERD §3.1.5 schema |
| type-check 错误：run() 参数类型不匹配 | node:sqlite 类型要求严格，`Record<string, unknown>` 过宽 | 收窄 values 类型 + 显式类型转换 |
| studybuddy-extension 测试断言失败（预期 21，实际 24） | S6 接入后工具数从 21 增至 24，测试断言未同步 | 更新断言 21→24 + 描述加 S6 |
| 多次调用 factory 断言失败（预期 63，实际 72） | 同上，24 工具 × 3 次调用 = 72 | 更新断言 63→72 |

## 5. 关键决定及依据

| 决定 | 依据 |
|---|---|
| S6 属 M2 第 2 任务，序号 002 | 04-Todo §6.4（M2 范围含 S6）+ §7.3.1 M2 任务登记表 |
| 规则报告生成 + AI 仅润色（非 AI 主导） | 07-WF §3：规则优先 + AI 仅润色 + 失败保留规则报告；02-PRD §5.2 家长报告脱敏边界 |
| ReportPolisher 可注入接口 | 08-Test §5.5 AI 降级 + 参考 S3 QuestionGenerator / S5 MockExamGenerator 模式；默认 createMockReportPolisher + createFailingReportPolisher 测试失败降级 |
| DeliveryChannel 可注入 + 渠道独立失败隔离 | 07-WF §3 + 04-Todo §6.4 M2 退出门槛"投递渠道独立失败隔离通过"；4 渠道 local_export/smtp/feishu_webhook/print，单渠道失败不影响其他 |
| assertNoSensitiveLeak UUID 泄漏检测 | 08-Test §5.4 + 04-Todo §6.4 M2 退出门槛"家长报告 UUID 泄漏检测通过"；content_json 全文扫描 UUID 正则，命中抛 PARENT_REPORT_PRIVACY_VIOLATION + 降级规则报告 |
| report 冻结状态机 draft→frozen | 07-WF §3：冻结快照 content_json + content_hash SHA-256 不可变 |
| retained_locally 兜底 | 07-WF §3：至少一次投递语义 + 所有渠道失败时 retained_locally=true 兜底 |
| study_events 写入 report_delivered | 07-WF §3 + 05-ERD §3.1.5；source_system='S6' |
| 不实现真实 SMTP/飞书/AI | 08-Test §5.4 不连真实外部服务；全部 mock |
| 不实现定期调度自动生成 | 调度层属独立任务，本任务仅支持手动触发 |

## 6. 测试证据

**S6 专项测试**：
- 单件：16 测试全绿（s6-tools.test.ts：TOOLSET-01~03 + GEN-01~04 + DELIVER-01~04 + MANAGE-01~05）
- 集成：19 测试全绿（s6-handlers.test.ts：GEN-01~03 + FREEZE-01~02 + GET-01~02 + DELIVER-01~03 + RETRY-01~02 + LIST-01 + CRUD-01~04 + SEC-01~02）
- 合计：35 测试全绿

**全量测试**：
- 29 test files，455 tests passed，0 failed，0 type errors
- Duration: 24.14s

**质量门（pnpm verify）**：
- typecheck ✅
- docs-governance ✅
- unit-tests ✅（455 passed）
- contract-coverage ✅（126 API + 8 PiBridge + 24 registerTool）
- desktop-security ✅（6 invariants passed）
- build ✅
- smoke ✅（6/6 passed）
- verify ✅（7 executed + 2 skipped）

**测试隔离**（AGENTS.md §5.3）：
- s6-tools.test.ts → H:\pi-studybuddy-tmp\runs\T-M2-002\unit-tools
- s6-handlers.test.ts → H:\pi-studybuddy-tmp\runs\T-M2-002\integration
- 绝不污染 %LOCALAPPDATA%\PiStudyBuddy

## 7. Git 证据

- 分支：agent/T-M2-002-s6-parent-report（待创建，等用户授权提交）
- commit message：feat(s6): S6 家长报告工具注册 + API（待用户授权）
- 未自动提交/推送（AGENTS.md §8 + §7 受控收尾）

## 8. 未解决事项/下一步约束

- 待用户授权提交推送（AGENTS.md §8.4 三者齐全才算完成）
- 真实 SMTP/飞书 Webhook/AI 润色待后续任务（当前全部 mock）
- 09-UI S6 标签页业务 UI 待壳层任务
- agent-host Host RPC 接入待壳层 UI 任务统一补全（与 S1-S5 一致）
- E2E-06 S6 家长报告待 M2 后续测试任务
- 定期调度自动生成（daily/weekly/monthly/exam_reminder）待调度层任务
- S7 课堂采集 / TTS / 备份恢复 独立任务，不预选
