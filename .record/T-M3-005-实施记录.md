# T-M3-005 实施记录：model_select / turn_end 钩子（多模型持久化 + L3 增量索引）

- **日期**：2026-08-08
- **里程碑**：M3 对话与打磨（done 4/8，本任务完成后 5/8）
- **任务**：T-M3-005 model_select / turn_end 钩子（扩展层，阶段3-4）
- **计划文件**：[.plan/T-M3-005-model-select-turn-end.md](../.plan/T-M3-005-model-select-turn-end.md)（已批准 2026-08-08，保留为验收证据）
- **关联文档**：03-Arch §2.3（model_select）+ 08-Test §4.2（model_select 断言）+ 06-API §3.1.1/§3.13 + 09-UI §4.2/§9.2 + 12-目录治理 §6.2/§7（models.json 落点）+ 08-Test §5.7（安全不变量）

---

## 1. 任务裁决与范围

**五裁决（用户 2026-08-08 批准）**：
1. **落点=业务数据根 `config/models.json`**：`__studybuddy_managed` 标记 + 原子写 + 测试隔离；同步修订 03-Arch §2.3 / 08-Test §4.2 / 06-API §3.13 / 09-UI §9.2 加 supersedes，解决 4 文档写 `~/.pi/agent/models.json` 与 AGENTS.md §9.5 物理隔离冲突
2. **turn_end 源=assistant + tool**：不读 `~/.pi`；复合键 `sessionId:turnIndex:role:seq` 幂等 + `max(last_offset)` 增量门控
3. **handler 范围=modelsConfig.get/set 两个**（test/addProvider/probe 留后续）
4. **写文件分工=扩展层钩子 + agent-host 共用 model-config 模块**（单写进程无并发）
5. **真实模型配置（用户提供 agnes 多媒体 + deepseek 文字）仅纳 provider/model 别名入 config** + key 入 credential-vault（`modelProvider:<provider>`，DPAPI，不含明文）

**范围（做）**：model-config 模块（readModelConfig/writeModelConfig 原子写 + `__studybuddy_managed` 标记）+ 扩展层 2 钩子注册（model_select 持久化默认模型 + turn_end L3 增量索引 assistant+tool）+ agent-host 2 handler（modelsConfig.get/set 共用 module）+ ChatTab 模型选择器落库（挂载回填 + 切换持久化）。
**非目标（留后续）**：modelsConfig.test/addProvider/probe handler、真实 LLM 调用（08-Test §5.4 全 mock）、L3 检索 UI 承载、会话管理完整 UI + 真实 sessionId（T-M3-006）。

## 2. 实际交付

| 交付物 | 文件 | 说明 |
|---|---|---|
| model-config 模块 | `src/agent/model-config.ts`（新增） | `ModelConfig` 接口 + `readModelConfig`/`writeModelConfig`（原子写 tmp+rename + `__studybuddy_managed` 标记 + `<dataRoot>/config/models.json`） |
| model_select 钩子 | `src/agent/studybuddy-extension.ts` | `pi.on("model_select")` 持久化默认模型 provider/model 到业务数据根 config/models.json（裁决 1） |
| turn_end 钩子 | `src/agent/turn-end.ts`（新增）+ `src/agent/studybuddy-extension.ts` | `indexTurnEndChunks`：增量索引 assistant + tool 消息 → chunks/chunks_fts，复合键幂等 + max(last_offset) 门控 + closeDatabase 释放文件锁 |
| agent-host handler | `src/agent-host/handlers/models.ts` + `src/agent-host/index.ts` | `modelsConfig.get`/`modelsConfig.set` 共用 model-config 模块；MODEL_FIXTURE 含 deepseek + agnes 别名（不含 baseUrl/密钥，§5.2） |
| ChatTab 落库 | `src/renderer/components/tabs/ChatTab.tsx` | 挂载 `modelsConfig.get` 回填默认模型 + onChange `modelsConfig.set` 拆分 provider:model 组合 id 持久化 |
| 四文档 supersedes | `docs/03 / 08 / 06 / 09`（v0.1.1→v0.1.2 / v0.1.2→v0.1.3 / v0.1.4→v0.1.5 / v0.1.2→v0.1.3） | model_select/modelsConfig 落点 `~/.pi/agent/models.json` → `<dataRoot>/config/models.json`（AGENTS.md §9.5 物理隔离） |
| 12-目录治理 | `docs/12-目录治理-Directory-Governance.md`（v0.1.0→v0.1.1） | §6.2 补 config/models.json + §7.2/§7.3/§12.1 models.json 标记异位修订 |
| 测试 | `tests/unit/model-config.test.ts` + `tests/unit/renderer-chat-tab-models.test.ts` + `tests/integration/models-config-handlers.test.ts` + 扩展 `tests/integration/cross-cutting-hooks.test.ts` | 共 14 新增测试，全绿 |

## 3. 偏差

- **无范围偏差**。一处实现细节修正（不改权威语义）：
  1. `turn_end` 钩子每次调用独立打开 L3 SQLite 连接并 `closeDatabase` 释放，避免测试/运行中 EBUSY 文件锁（汇总前版本连接未关闭）
- 钩子数量断言从 4 更新为 6（新增 model_select + turn_end）

## 4. 问题及根因

| 问题 | 根因 | 解决 |
|---|---|---|
| L3 SQLite EBUSY 错误 | turn_end 索引后连接未关闭，文件被锁 | `indexTurnEndChunks` 末尾 `closeDatabase(db)` + 测试显式关闭 DB |
| 测试会话 ID 重复导致数据干扰 | turn_end 测试复用同一 L3 库 | 清理 L3 数据库文件 |
| MODEL_FIXTURE 含 baseUrl | fixture 竞泄漏密钥边界（02-PRD §5.2） | 移除 baseUrl，仅 provider/model 别名 |
| insert 参数类型错误（TS） | turn-end.ts insert 签名与调用不符 | insert 仅接收 content/role/sourceType |
| 钩子数量断言过时 | 新增 2 钩子 | 4 → 6 |
| 04-Todo 版本历史缺 v0.1.58 | 完成条目覆盖开工条目 | 恢复 v0.1.58 条目并补全头部叙述 |
| 12-目录治理同源不一致 | models.json 标记仍指向 ~/.pi/agent | 修正为业务数据根 config/models.json + 升版本 v0.1.1 |

## 5. 关键决定及依据

| 决定 | 依据 |
|---|---|
| 模型配置落业务数据根 `<dataRoot>/config/models.json` | 用户批准裁决 ①；AGENTS.md §9.5 物理隔离（pi-studybuddy 不侵入 `~/.pi`），4 文档 supersedes 落点修订 |
| `__studybuddy_managed` 标记 + 原子写（tmp+rename） | 裁决 ①；区分 studybuddy 选型与 pi 自管配置 + 防半写 |
| turn_end 增量索引（assistant+tool，复合键幂等 + max(last_offset)） | 裁决 ②；避免重复写入 + 不读真实 `~/.pi` |
| handler 仅 modelsConfig.get/set | 裁决 ③；test/addProvider/probe 留后续，§6.4 禁止过度工程 |
| 扩展层钩子 + agent-host 共用 model-config 模块 | 裁决 ④；单写进程无并发，去重实现 |
| 真实 model 配置仅纳别名入 config + key 入 credential-vault | 裁决 ⑤；不落明文（AGENTS.md §8.5/§9.2），键名 `modelProvider:<provider>` |
| ChatTab 通过 provider:model 组合 id 拆分持久化 | 09-UI §9.2 模型选择持久化业务数据根 |

## 6. 测试证据

- **单件**：model-config 读写（原子写 tmp+rename + `__studybuddy_managed` 标记 + 不存在返回 null + 非法 JSON 返回 null）；renderer-chat-tab-models 静态渲染（模型选择器 value/onChange 落库语义）
- **集成**：models-config-handlers（modelsConfig.get 空返回 / set 写业务数据根 + managed 标记 / get 回读）；cross-cutting-hooks 扩展（model_select 持久化 + turn_end 增量索引 assistant+tool + 幂等 + closeDatabase 释放）
- **安全不变量**（08-Test §5.7）：config/models.json 仅含 provider/model 别名，不含 baseUrl/API key/完整 UUID；credential-vault 键名 `modelProvider:<provider>`（AGENTS.md §9.2）
- **数据隔离**（AGENTS.md §5.3）：测试写 `runs/T-M3-005`，不污染真实数据根
- **质量门**：type-check ✅ + build ✅ + 939 单元/集成测试（925→939，+14）✅ + 83 E2E ✅ + smoke 6/6 ✅ + verify 全绿 ✅ + 文档治理 ✅ + 契约覆盖 127 handlers ✅ + 安全不变量 6/6 ✅ + UUID 泄漏 7/7 ✅

## 7. Git 证据

- **分支**：`agent/T-M3-005-model-select-turn-end`
- **变更文件**：
  - 新增：`src/agent/model-config.ts`、`src/agent/turn-end.ts`、`tests/unit/model-config.test.ts`、`tests/unit/renderer-chat-tab-models.test.ts`、`tests/integration/models-config-handlers.test.ts`、`.record/T-M3-005-实施记录.md`
  - 修改：`src/agent/studybuddy-extension.ts`、`src/agent-host/handlers/models.ts`、`src/agent-host/index.ts`、`src/data/db.ts`、`src/renderer/components/tabs/ChatTab.tsx`、`tests/integration/cross-cutting-hooks.test.ts`、`tests/integration/studybuddy-extension-contract.test.ts`、`tests/unit/studybuddy-extension.test.ts`、`docs/03 / 04 / 06 / 08 / 09 / 12 / 00-索引`、`AGENTS.md`
- **提交**：待授权（AGENTS.md §8 禁止自动提交/推送）

## 8. 未解决事项/下一步约束

- **modelsConfig.test/addProvider/probe handler**：留后续（裁决 ③ 非目标）
- **L3 检索 UI 承载 / 会话管理完整 UI + 真实 sessionId**：T-M3-006
- **E2E-10/12/13 全链回归**：T-M3-007/008
- **真实 LLM 调用**：08-Test §5.4 全 mock 边界（credential-vault 已备 modelProvider:<provider> 键位，收尾时不连真实服务）
- **下一任务**：不预选，等待用户指示（AGENTS.md §4.4/§7）