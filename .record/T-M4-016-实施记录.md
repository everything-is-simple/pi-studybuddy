# T-M4-016 实施记录：S6 报告 Tab RPC 接线

- 任务 ID：T-M4-016
- 实施分支：agent/T-M4-016-s6-report-rpc
- 集成基线：master=origin/master=3dcf93f
- 日期：2026-08-11
- 用户授权：2026-08-11“开始 T-M4-016”开工 + “批准计划”进入实施

## 1. 任务裁决与范围

- 用户明确选择 S6 报告 Tab RPC 接线（reports + deliveries + reportTargets）并批准开工与计划。
- 范围：ReportTab 接通既有 S6 RPC——`reports.list`（既有保留）+ `reports.generate/freeze/get` + `deliveries.list/deliver/retry` + `reportTargets.list`；生成入口（类型/周期选择）、冻结门控、投递状态可视化（sent ✅/failed ✗ 重试/retained_locally/未配置 ─）、投递/重试防重复。
- 裁决：`reportTargets.create/update/delete` 不纳入 renderer 接线（09-UI §4.9 无渠道管理表单 UI 依据，真实地址在 credential-vault 属设置页能力）；但 host 侧 archived 写防线仍补齐到全部 6 个 S6 写 handler（对齐 T-M4-015 S5 先例，防后续接线时留下 host 洞）。
- 范围外：不新增 API/handler/schema 方法（contract 保持 127/127）；不启动 T-M4-017~021；Git 收口另需授权。

## 2. 实际交付

- `src/agent-host/handlers/s6/lookup.ts`：新增 `assertSemesterWritable`（对齐 S3/S5 模式，archived 学期写操作 BAD_REQUEST）。
- `src/agent-host/handlers/s6/reports.ts`：`reports.generate` 接入 `assertSemesterWritable`。
- `src/agent-host/handlers/s6/deliveries.ts`：`deliveries.deliver` / `deliveries.retry` 接入 `assertSemesterWritable`（经 `findSemesterByReportKey` 定位 semesterId）。
- `src/agent-host/handlers/s6/report-targets.ts`：`reportTargets.create/update/delete` 接入 `assertSemesterWritable`（update/delete 从既有行取 semester_id）。
- `src/renderer/components/tabs/ReportTab.tsx`：重写为 RuntimeReportTab（rpc 时）+ 静态渲染兼容（无 rpc 时保持旧 props 语义）；接通 8 个既有 RPC；reportType/period 选择默认本周；冻结门控（frozen 后禁用）；投递渠道状态可视化（已配置渠道 ∪ 有投递记录渠道，四渠道固定排序，未配置 ─）；投递/重试防重复（in-flight ref）；学期切换竞态/卸载保护（contextVersion + mountedRef）；归档只读（isReadOnly 禁用生成/冻结/投递/重试）；错误固定净化文案；脱敏展示（ShortId + safeReportText + 不渲染 channelConfigJson/credentialKey/真实地址）。
- 新增测试：
  - `tests/integration/t-m4-016-report-rpc.test.ts`（9 tests：S6-RED-01~08 + 08b）
  - `tests/integration/t-m4-016-s6-host-boundaries.test.ts`（2 tests：归档前可写 / 归档后 4 写入口拒绝 + 5 只读可用）
  - `tests/e2e/t-m4-016-report-renderer.test.ts`（2 tests：真实 Electron 主流程 + 归档只读）

## 3. 偏差

- 计划中“实施时核对 s6 handler 是否具备 assertSemesterWritable，如缺失按 T-M4-015 先例补齐”被执行：确认 s6 写 handler 全部缺失 host 侧 archived 防线，按先例补齐 6 个写入口（方法签名不变，不新增 API）。
- S6-RED-02/04 测试初版未注册 `reports.get` handler（详情视图依赖），修正测试注册（断言本质不变）。

## 4. 问题及根因

- 问题：归档学期仍可通过 `reports.generate` / `deliveries.deliver/retry` / `reportTargets.*` 写库。根因：s6 `lookup.ts` 只有 `assertSemesterExists`（查存在性），无 archived 状态校验；T-M4-015 已发现同类问题并补齐 S5，S6 未同步。修复：新增 `assertSemesterWritable` 并接入全部写入口。
- 问题：RED 测试 9/9 失败（预期）。根因：ReportTab 原为静态壳（T-M2-008），无运行时 RPC 接线。修复：重写 RuntimeReportTab。

## 5. 关键决定及依据

- `reportTargets.create/update/delete` 不接线：09-UI §4.9 无渠道管理表单 UI；真实渠道地址在 credential-vault（02-PRD §5.2 密钥边界）；渠道配置属设置页能力，留待后续任务/用户裁决。但 host 防线仍补齐，防未来接线时留下 archived 写漏洞（对齐 T-M4-015 审查者 A 发现的 S5 同类洞）。
- 投递渠道展示模型：已配置渠道（targets）∪ 有投递记录渠道（deliveries），固定四渠道顺序，未配置显示 ─；不渲染 target 内部字段（channelConfigJson/credentialKey/id 完整值）。
- 冻结门控：冻结成功后按钮变“已冻结”禁用，且 mutation 不再可用（frozen 状态）。
- 防泄露：错误统一固定中文文案（reportErrorText），DOM 不出现路径/栈/完整 UUID/真实地址（E2E 隐私断言 5 项全过）。

## 6. 测试证据

- RED 初次失败：`H:\pi-studybuddy-tmp\runs\T-M4-016\red\red-initial-failure.log`（9 failed / 2 passed）。
- 定向 integration：`t-m4-016-report-rpc` 9/9 + `t-m4-016-s6-host-boundaries` 2/2 通过。
- 既有 unit 回归：`tests/unit/renderer-report-tab` 10/10（静态渲染兼容）；全量 unit 71 files/725 tests 通过。
- type-check / build / smoke 6/6 / docs 治理 / contract-coverage / desktop-security 6/6 / uuid-leak 7/7 / `git diff --check` 全部通过。
- 真实 Electron E2E：`t-m4-016-report-renderer` 2/2（主流程 + 归档只读），运行根 `runs\T-M4-016\e2e-renderer`。
- Node24.14.0/pnpm11.20.0 `verify --stage=full`：unit/integration 115 files/1096 tests、真实 Electron E2E 21 files/126 tests、contract 127/127、安全 6/6、smoke 6/6、UUID 7/7、docs 治理与 diff-check 通过。

## 7. Git 证据

- 分支：`agent/T-M4-016-s6-report-rpc`（自 master=3dcf93f 创建）。
- 变更文件：5 个 src 文件（s6/lookup/reports/deliveries/report-targets + ReportTab.tsx）、3 个新测试文件、.plan 2 个、.record 1 个、docs 04/00 2 个。
- 未提交（Git 收口未获授权）；未混入用户 dirty 文件（未跟踪 pi-session html 保持不 add）。

## 8. 未解决事项/下一步约束

- Git 收口（commit → master 快进合并 → 完整复验 → push origin/master）待用户单独授权（AGENTS.md §8.4）。
- `reportTargets.create/update/delete` 的 renderer 接线（渠道管理 UI）未做，留待后续任务或用户裁决；host 防线已就绪。
- 不启动 T-M4-017~021；下一任务由用户明确选择。
