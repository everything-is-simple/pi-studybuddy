# T-M5-004 独立审查 A（业务 / UI / 原生 UAT）

**审查日期**：2026-08-14
**方式**：只读复审；未编辑文件、未执行 Git 写操作，且未以 renderer 注入自动化替代真机 UAT。
**结论**：**PASS（无 P0/P1）**；保留 1 项非阻断 P2。

## 1. 本次审查范围

- 同资料同名模块拒绝与 NotesTab 脱敏反馈；
- PracticeTab 对既有 `practice.listSessions` / `practice.getResult` 的最小可见结果读取接线；
- RED→GREEN 集成测试与 DOM 会话标识不泄露；
- v4c 原生可见 UI 的模块唯一性、练习提交、重启历史/结果回读；
- 当前完整质量门记录。

## 2. P1-01 关闭：同资料同名模块

此前 P1-01 已关闭。

- `modules.create` 在写入前按 `material_id + module_name + deleted_at IS NULL` 拒绝活动同名模块；
- NotesTab 将 BAD_REQUEST 显示为固定、脱敏且可操作的中文提示；
- v4c 可见 UI 证据 `68-duplicate-module-friendly-error.json/png` 显示重复创建提示；
- `76-restart-unique-module-readback.json/png` 显示重启后仅一条“极限基本概念”。

## 3. S3 重启结果读取

PracticeTab 仅复用既有 typed RPC：`practice.listSessions({ courseId })` 和 `practice.getResult({ sessionId })`。未新增 API、handler、schema、Stream 或跨 Tab 状态；会话 ID 仅作内部调用/React key，不渲染到 DOM。

- `tests/integration/renderer-practice-actions.test.ts` 的 RED 5 断言已覆盖：读取已完成练习、点击可见“查看结果”、调用既有结果 RPC、DOM 不含 session ID；
- `83-s3-practice-submit-result.json/png` 保留提交结果；
- `93-s3-second-restart-readback.json/png` 保留历史缺口，未被改写；
- `105-second-restart-practice-history-visible.json` 显示“已完成练习 / 查看结果”；
- `106-s3-second-restart-result-readback.json/png` 显示提交后的得分、正确数、用时、状态和逐题结果；
- `107-app-closed-after-result-readback.json` 记录经可见窗口关闭控件结束本次 UAT。

审查确认上述 UAT 证据不依赖 `webContents.executeJavaScript`、CDP、RPC/handler 直调、SQLite 预置或 renderer 注入。

## 4. 质量门

2026-08-14 当前完整质量门日志 `verify-full-s3-readback-20260813.log` 显示：

- Node v24.14.0，pnpm 11.20.0；
- unit/integration：130 files / 1192 tests passed；
- 真实 Electron E2E：33 files / 141 tests passed；
- `exit_code=0`。

## 5. 问题分级

| 分级 | 结论 |
|---|---|
| P0 | 无 |
| P1 | 无；此前 P1-01 已关闭 |
| P2 | 原始 UAT JSON 含测试机 Electron 绝对路径/窗口清单。它们不在应用 renderer DOM，也不构成会话 ID 泄露；若对外共享或纳入 Git 附件，应先脱敏。 |

## 6. 收口约束

审查 A 通过不改变任务状态：`T-M5-004` 仍为 `in_progress`。Git 仍禁止，直到审查 B 完成、所有 P0/P1 均关闭，并由用户单独授权提交、快进合并、master 复验和推送。
