# T-M5-001 实施记录

## 1. 任务裁决与范围

用户明确要求从真实用户视角逐页面、逐按钮验证 UI、功能、设计偏差和交付依赖；T-M5-001 负责建立审计追踪矩阵，不修改业务代码，不启动 T-M5-002~008。执行分支为 `agent/T-M5-001-ui-acceptance-audit`，集成基线为 `master=origin/master=869de2f`。

## 2. 实际交付

- 完成 `H:\pi-studybuddy-tmp\runs\T-M5-001\ui-function-dependency-matrix.md`：覆盖壳层、10 个主 UI 面、TTS、备份、设置、RPC/本机能力、已有测试和后续任务归属。
- 完成 `control-inventory.md`：逐项记录按钮、输入、选择器、复选框、对话框和静态/空态分支。
- 完成 `gap-register.md`：登记 G-P0-01~G-P0-08、G-P1-01~G-P1-14，并补充后续任务和验收证据要求。
- 完成 `package-dependency-audit.md`：记录 setup/unpacked 产物体积、SHA-256、builder 文件边界和 OCR/WPS/whisper/Python 等依赖状态。
- 完成两份独立审查：`review-a-implementation-ux.md` 与 `review-b-packaging-acceptance.md`。
- 在真实安装应用的全新隔离数据根中完成 10 个 Tab 的逐页打开，保存 `installed-tab-audit/` 下 DOM、控件 JSON、截图和摘要。
- 保存真实空数据首屏证据：`package-diagnostic/initial-empty-dom.txt` 与 `initial-empty-renderer.png`。

## 3. 偏差

- M5 退出门槛要求的全功能用户闭环未通过。本任务仅完成审计，未修复发现的问题。
- 真实安装应用的首屏没有创建学期/课程/考试/任务的 UI 入口，却显示“请先在学习计划中创建”。
- 生产 agent-host 仍注入 `defaultSessionFixture()`，空数据首屏显示“极限学习/导数练习”；ChatTab 发送和工具跳转使用固定 `sess-001`，关联错题使用固定 `mist-001`。
- 模型目录可见，但底部状态仍为“模型：未配置”；多个失败路径静默处理。
- package smoke 只验证 piBridge、system.ping、global.db 和 `semesters.create`，不能证明逐按钮 UAT。
- 当前 setup 为 NSIS x64，未声明 portable zip，且没有随包提供 Python/OCR/WPS/whisper 运行资源。

## 4. 问题及根因

- S1 初始化 UI 尚未实现，左侧 `SemesterCourseTree` 只有展开/选择能力。
- session-store 仍把历史 fixture 作为生产入口默认数据源；AppShell 新建会话只设置 `sess-new`，ChatTab 未消费当前会话 ID。
- 生产 S2Context 明确未注入 WPS/OCR；S7 真实 whisper 依赖环境变量 CLI/模型路径；toolchain installer 仅创建目录，不实际安装。
- electron-builder `files` 仅包含 `dist/**/*` 与 `package.json`，排除 `scripts/**/*`，没有 `extraResources` 或 zip target。
- package smoke 在残留进程/Profile 未清理时曾出现 `ping_failed`，错误被脚本归一化；清理后复跑通过，说明验收 harness 仍需要稳定性改进。

## 5. 关键决定及依据

- 不回写 M4 完成事实；所有用户可用性和一键交付缺口归入 M5。
- 不把静态渲染、fixture、代表性 RPC 或 mock adapter视为真实用户证据。
- 不猜测 WPS、Python、OCR、whisper 或第三方模型的再分发许可；由 T-M5-006 做依赖、许可、体积、更新和降级裁决。
- T-M5-001 保持 `in_progress`，后续修复任务不自动启动；依据 AGENTS.md §4.4、§4.5、§7、§8.4。

## 6. 测试证据

- Node `v24.14.0`、pnpm `11.20.0`。
- `pnpm verify -- --stage=full`：通过；118 unit/integration files、1133 tests，29 real Electron E2E files、137 tests。
- contract coverage：127/127；desktop security：6/6；smoke：6/6。
- `node scripts/check-docs-governance.mjs`：通过，13 份设计文档、2 个 Skill、2 个 prompt。
- `git diff --check`：通过；仅有 `.plan/00-当前任务.md` 的 LF/CRLF warning。
- M5 隔离安装：NSIS 静默安装成功；显式清理旧进程/Profile 后 package smoke 两次启动通过，确认真实 Electron、renderer、system.ping、global.db 和代表性业务 RPC。
- 安装态逐页审计：10/10 Tab 可打开并生成 DOM、控件 JSON 和 PNG 证据。

## 7. Git 证据

- 当前分支：`agent/T-M5-001-ui-acceptance-audit`。
- 集成基线：`master=origin/master=869de2f`。
- 未执行 commit、merge、push，符合本任务尚未获 Git 收口授权的约束。
- 用户随后明确删除原有未跟踪 `pi-session-*.html` 文件；删除由用户完成，审计提交不包含这些文件。
- 本记录和审计矩阵在当前工作树中，运行证据位于 `H:\pi-studybuddy-tmp\runs\T-M5-001\`，不进入 Git。

## 8. 未解决事项/下一步约束

- P0 必须由 T-M5-002~006 逐项修订：首次启动/S1 初始化、真实会话/模型/文件引用、S1-S5 控件、S6/S7/TTS/备份/设置 UX、依赖自包含/降级边界。
- T-M5-007 才能在干净 Windows 上做全功能用户 UAT；T-M5-008 才能做最终 setup/zip 发行验收。
- 当前不能宣称“普通用户无需开发环境即可完成全功能闭环”，也不能宣称 M5 完成。
- 等待用户明确选择下一项任务；不得自动创建 T-M5-002~008 的详细计划或修改业务代码。
