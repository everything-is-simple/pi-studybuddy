# T-M5-009 实施记录：测试与运维追溯基线建立

**状态**：done（用户任务特定裁决认可受控文档消费路径验收 PASS；功能 `156756c`、master 复验、治理登记推送和远端同位核验后完成）
**日期**：2026-08-14
**任务计划**：`.plan/T-M5-009-test-operations-traceability-baseline.md`
**实施分支**：`agent/T-M5-009-traceability-baseline`
**运行根**：`H:\pi-studybuddy-tmp\runs\T-M5-009\`

## 1. 任务裁决与范围

用户已明确批准执行顺序 45 的 T-M5-009；T-M5-005~008 不启动。范围仅建立治理基线：`interaction-catalog`、`error-catalog`、`data-asset-catalog`、`system-traceability`、`operations-runbook`、`release-evidence`。不新增业务 API、schema、handler、stream、renderer 入口或跨 Tab 状态，不写生产数据，不连接真实外部服务，不执行未经用户授权的 Git 收口；后续用户已明确授权本任务 Git 收口。

## 2. 实际交付

- 新增 `docs/traceability/interaction-catalog.md`：CTRL→ACT 目录、统计口径、缺口状态与下游消费入口。
- 新增 `docs/traceability/error-catalog.md`：ERR 责任边界、用户恢复动作、脱敏日志边界与证据状态。
- 新增 `docs/traceability/data-asset-catalog.md`：业务 SQLite、测试 SQLite、文件、配置、DPAPI 与临时根的资产和生命周期。
- 新增 `docs/traceability/system-traceability.md`：UI→运行→逻辑→数据→测试/UAT→运维矩阵。
- 新增 `docs/traceability/operations-runbook.md`：启动健康、数据根、SQLite、备份恢复、升级/卸载、依赖诊断与停止条件。
- 新增 `docs/traceability/release-evidence.md`：自动化、真机 UAT、隔离根、安装/恢复、Git 与治理证据模板。
- 新增 `tests/integration/t-m5-009-traceability-baseline.test.ts`，并完成 RED→GREEN。
- 更新 `AGENTS.md`、`docs/00-文档索引-Index.md`、`docs/04-任务清单-Todo-List.md`、`docs/13-测试与运维-Testing-and-Operations.md` 与唯一当前任务看板。

## 3. 偏差

- 现有 T-M5-001 的 60 项 `CTRL-*` 作为输入基线；未将其误报为 `ACT-*` 总量，也未猜测覆盖率。
- 首版只登记当前可由源码、契约、测试或既有审计证据确认的事实；未知项保留 `未覆盖`、`待核验`、`阻塞` 或 `部分证据`。
- `verify --stage=full` 中 format/lint 因当前脚本 guard 未满足而跳过；这不是本任务新增失败，typecheck、文档治理、unit/integration、真实 Electron E2E 均通过，详见运行日志。

## 4. 问题及根因

- RED 阶段六项资产尚不存在，治理测试按预期失败；根因是 T-M5-009 尚未建立追溯资产。
- 初次 `git diff --check` 发现当前任务看板使用 Markdown 硬换行尾随空格；已移除尾随空格后通过。
- 当前任务无 UI/API 变更，因此不存在可由本任务新增的 Electron 用户闭环。用户随后作出任务特定裁决：受控文档消费路径验收可替代本任务真机 Electron UAT；该例外只对 T-M5-009 有效，不得外推。

## 5. 关键决定及依据

- 依照 `docs/04` 执行序 45，T-M5-009 先于 T-M5-005~008。
- 依照 `docs/13`，CTRL、ACT、ERR、DATA、TEST/UAT、OPS 分开建模；自动化证据不等于真机 UAT。
- 依照 AGENTS.md §4.5/§7/§8.4，任务在远端同位前保持 `in_progress`；用户已明确授权后才执行 Git 收口链，且不把自动化结果外推为应用真机 UAT。
- 测试数据只写 `H:\pi-studybuddy-tmp\runs\T-M5-009\`，不访问 `%LOCALAPPDATA%\PiStudyBuddy` 生产根。

## 6. 测试证据

| 门 | 命令/证据 | 结果 |
|---|---|---|
| RED | `H:\pi-studybuddy-tmp\runs\T-M5-009\red-governance-test.log` | 资产缺失导致预期失败 |
| GREEN 定向测试 | `H:\pi-studybuddy-tmp\runs\T-M5-009\green-targeted-test.log` | 1 file / 3 tests passed，type errors 0 |
| 文档治理 | `node scripts/check-docs-governance.mjs` | 通过 |
| 差异检查 | `git diff --check` | 通过 |
| 分支 Node24 完整质量门 | `H:\pi-studybuddy-tmp\runs\T-M5-009\verify-full.log` | 通过；执行 8，跳过 2；unit/integration 131 files/1195 tests，真实 Electron E2E 33 files/141 tests |
| master Node24 完整质量门 | `H:\pi-studybuddy-tmp\runs\T-M5-009\git-closeout-20260814\master-verify-full-rerun.log` | 最终重跑通过；执行 8，跳过 2；unit/integration 131 files/1195 tests，真实 Electron E2E 33 files/141 tests |
| 复验异常审计 | `master-verify-full.log`（初次失败）、`t-m5-003-uat-renderer-rerun.log`、`master-verify-full-rerun.log` | 初次完整 E2E 出现既有 T-M5-003 UAT runner 非持续异常；使用正确 E2E 配置的单文件复验通过，随后完整重跑通过。未修改该任务外源码，也未将初次失败隐去。 |
| 受控文档消费路径验收（替代本任务真机 Electron UAT） | `H:\pi-studybuddy-tmp\runs\T-M5-009\document-consumer-acceptance.json`，`T-M5-009-DOC-CONSUMER-001` | PASS。用户明确认可；逐项读取六份治理文档并核验可见消费字段/标识。未启动 Electron、未调用 handler、未注入 renderer、未写生产数据；例外仅限本无 UI 变更的 T-M5-009，不得外推。 |

## 7. Git 证据

- 基线：分支创建前 `master=origin/master=408865e`，工作区干净。
- 当前分支：`agent/T-M5-009-traceability-baseline`。
- 功能/基线提交：`156756c`（`docs(m5): 建立测试与运维追溯基线`）。
- 已由 `agent/T-M5-009-traceability-baseline` ff-only 合并至本地 `master`。
- 本治理登记提交推送并核验 `origin/master` 后，记录最终 commit 与同位事实；在此之前不把本地 master 当作正式完成。

## 8. 未解决事项/下一步约束

- 本任务的 UAT 适用性已由用户明确裁决：`T-M5-009-DOC-CONSUMER-001` 为 PASS；不得将其外推为应用真机 UAT，T-M5-005~008 仍需各自满足 AGENTS.md §7。
- Git 证据以本治理登记推送后的 `master=origin/master` 核验为准；不启动 T-M5-005。
