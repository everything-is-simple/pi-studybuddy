# 实施记录：T-M4-012 S2 笔记 Tab RPC 接线

**任务 ID**：T-M4-012
**日期**：2026-08-10
**计划文件**：.plan/T-M4-012-s2-notes-rpc.md
**分支**：agent/T-M4-012-s2-notes-rpc
**状态**：本地实现与验收证据已完成；Git 收口待用户另行授权，任务仍 `in_progress`

## 1. 任务裁决与范围

**用户裁决**：采用方案 A——NotesTab 内局部显式资料选择，不新增 AppShell 跨 Tab `selectedMaterialId` 状态。

**范围**：
- 在 NotesTab 当前课程下复用 `materials.list({ courseId })` 提供资料选择；
- 仅在学生明确选择资料后调用 `notes.get({ materialId })`；
- 接通 `notes.update`、`modules.list({ courseId })`、`modules.updateLearnStatus`；
- 保持课程/资料切换竞态保护、NOT_FOUND 新建笔记、归档学期只读边界和 renderer 隐私展示边界；
- 真实 Electron renderer 代表性路径补证；
- 所有测试数据位于 `H:\pi-studybuddy-tmp\runs\T-M4-012\`。

**非范围**：不新增 API、handler、schema、AppShell 全局资料状态；不实现 `notes.getMindMap`；不启动 T-M4-013~021；不连接真实外部服务；不写真实业务数据根；不执行 commit/merge/push。

## 2. 实际交付

- `H:\pi-studybuddy\src\renderer\components\tabs\NotesTab.tsx`
  - 局部资料选择，初始不选择第一条；
  - 使用局部 option token，业务 `materialId` 不进入 option value；
  - 接通 notes/modules RPC；
  - NOT_FOUND 后支持手动新建并通过 `notes.update` 保存；
  - 按 `module.materialId` 过滤当前资料模块；
  - 课程/资料/卸载/异步写入 context/version guard；
  - archived 学期禁用 renderer 写入口，host 既有写防线保留；
  - UUID、Windows/POSIX 路径、`file:` URI、错误栈在 renderer 展示边界固定净化。
- `H:\pi-studybuddy\tests\integration\t-m4-012-notes-rpc.test.ts`
  - 10 项 mounted renderer RPC/竞态/隐私/归档测试。
- `H:\pi-studybuddy\tests\e2e\t-m4-012-notes-renderer.test.ts`
  - fixture 在 Electron main 加载前于隔离数据根预置；
  - 真实路径覆盖显式选资料、NOT_FOUND、新建/保存笔记、更新模块状态；
  - 通过 visible text 断言 UUID、Windows/POSIX 路径、`file:` URI、错误栈均不可见；
  - 结果文件写入任务隔离根且不记录原始错误、路径或完整 UUID。
- 治理同步：AGENTS、docs/00、docs/04、两个 `.plan` 文件已同步当前证据；任务保持 `in_progress`。

## 3. 偏差

- 原 E2E 先通过 renderer RPC 创建学期/课程，再 reload 页面；reload 会重新申请 renderer host port，而当前 agent-host 连接模型不支持该测试时序，导致课程树无法加载。
- 已将 E2E fixture 调整为 main 加载前在任务隔离数据根直接预置，应用启动后只验证真实 renderer/preload/host RPC 业务路径；未改变生产实现或 API 契约。
- E2E 结果解析从 stdout 改为隔离根中的 `renderer-result.json`，避免 Electron/Chromium 诊断输出干扰 JSON 证据。
- 一次并行复验出现 Tinypool `Worker exited unexpectedly`；随后在无该并发干扰的 Node24 基线下完整 verify 再次通过，未将该环境瞬态失败归因于 T-M4-012 代码。

## 4. 问题及根因

1. **materialId 来源阻塞**：live renderer 没有现成跨 Tab 资料状态；依据用户批准的方案 A，在 NotesTab 内显式选择并局部持有。
2. **首轮 P1**：保存/读取晚到响应可能跨资料回写；通过 view context、request id、mounted guard 修复并加入延迟测试。
3. **首轮 P1**：DOM 对抗性隐私证据不足；补充 UUID、Windows/POSIX、file URI、错误栈输入及 visible DOM 断言。
4. **首轮 P1**：NOT_FOUND 后新建入口不可达；增加新建笔记状态并复用 `notes.update` 创建。
5. **E2E 导航失败**：初始学期树未展开且 reload 后 host port 时序不适合 fixture 创建；改为启动前隔离预置后解决。

## 5. 关键决定及依据

- `materialId` 只在 NotesTab 内显式选择后产生，不默认第一条、不写死、不伪造。
- 资料列表与模块列表均绑定当前 `courseId`；模块在 renderer 按当前所选 `materialId` 过滤。
- archived 学期采用 renderer 禁用 + host `assertSemesterWritable` 双层防线。
- E2E 仅使用任务隔离数据根，生产数据根保持未写入。
- 当前不将任务登记为 `done`，不执行 Git 收口；依据 AGENTS.md §4.5、§7、§8.4。

## 6. 测试证据

Node24 基线：

```text
node v24.14.0
pnpm 11.20.0
```

定向证据：

- `pnpm type-check`：通过；
- `pnpm build`：通过；
- `tests/integration/t-m4-012-notes-rpc.test.ts`：10/10 通过；
- `tests/e2e/t-m4-012-notes-renderer.test.ts`：1/1 通过；
- `node scripts/check-docs-governance.mjs`：通过；
- `git diff --check`：通过。

完整质量门：

```text
H:\pi-studybuddy-tmp\runs\T-M4-012\verify-full-final.log
unit/integration: 107 files / 1047 tests passed
真实 Electron E2E: 17 files / 119 tests passed
contract coverage: passed
desktop security: 6/6 passed
smoke: passed
UUID leak suite: passed
verify: 全部通过（执行 8，跳过 2）
```

两名独立审查者已对当前变更复核：无 P0/P1；低风险 P2（多个模块并发时共享 busy/error 槽位）不在本轮范围内，未扩展修复。

## 7. Git 证据

- 当前分支：`agent/T-M4-012-s2-notes-rpc`；
- 当前 `HEAD`：`2497de4`，与 `master`、`origin/master` 相同；
- 工作区包含本任务未提交实现、测试、计划和治理同步；
- 未执行 commit、merge、push；
- 因未获 Git 收口授权，不能报告 master/origin 已包含 T-M4-012。

## 8. 未解决事项/下一步约束

- T-M4-012 保持 `in_progress`，等待用户明确 Git 收口授权；
- Git 收口必须另行执行：显式路径 staging、功能提交、master 快进合并、master Node24 复验、origin/master push 与远端核验；
- 在 Git 收口前不得启动 T-M4-013~021；
- 不修复本轮审查识别的低风险模块并发 busy/error 槽位问题，除非用户另行授权扩大范围；
- 不写真实业务数据根，不执行删除、reset、clean、stash。
