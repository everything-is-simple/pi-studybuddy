# T-M4-006 实施记录：设置页 UI

**日期**：2026-08-09  
**状态**：实施与验证完成；Git 收口待用户明确授权，任务保持 `in_progress`  
**关联计划**：`.plan/T-M4-006-settings-ui.md`

## 1. 任务裁决与范围

用户于 2026-08-09 明确批准 T-M4-006。设置裁决为 AppShell 内的独立页面，不新增第 10 个工作台 Tab；左侧栏入口和 `Ctrl+,` 打开，返回时保留工作台 Tab。范围限定为既有 typed RPC 的学习偏好、模型默认项、AI/Email/飞书凭据状态与写入、工具链检查；未修改 `Api` 契约或 `docs/06-API`。

## 2. 实际交付

- 新增 `src/renderer/components/SettingsPage.tsx`：通用 / 安全 / 开发者设置分组，使用 `settings.*`、`models.*`、`modelsConfig.*`、`credentials.*`、`toolchains.*` 的现有方法。
- 修改 `AppShell.tsx`：左侧“⚙ 设置”、`Ctrl+,` 和返回工作台；以 reducer 保持 `activeTabId`。
- 密钥仅以 password DOM 输入局部传递，读取后立即清空；不调用 `credentials.get`，不将密钥写入 React state。
- toolchains stream/list 竞态以最新 stream 快照优先；进入 renderer state 前丢弃 path。
- 新增设置页/导航测试，补齐各保存操作的精确 RPC 参数、凭据状态重读、脱敏、stream 退订和页面状态保留。
- 为真实 PATH 探测及 pi Agent 初始化的慢集成测试设置局部 30 秒时限，保留全部原有断言，消除全量并行时默认 5 秒的误报。

## 3. 偏差

09-UI 的工具健康状态示意包含 WPS Office、whisper.cpp；当前公开 `toolchains` 契约没有两者的独立 capability probe。本任务不能擅自新增 RPC 或伪造状态，故页面明确说明其详细状态由各自流程受控检查。后续若需要真实健康值，必须通过单独批准任务扩展探测/API，或先修订权威 UI 条款。

## 4. 问题及根因

- 复验质量门时，显式 Node 24.14.0 只运行了父级 `verify.mjs`；其 `shell: true` 子进程仍按全局 PATH 解析到 Node 25.4.0。Node 25 超出已验证上限，`js.node` 被正确标为 `unverified`，但破坏了“Node 24 质量门”中 `healthy` 的既有集成断言。最小修复为 `scripts/verify.mjs` 将 `path.dirname(process.execPath)` 前置到子进程 PATH；未放宽探测策略或断言。

- 初审发现路径、堆栈、provider ID 和 stream/list 竞态的安全/稳定性缺口；已以 safe display、白名单过滤和最新 stream 优先修复。
- 初审发现保存动作与页面导航的测试证据不足；已遵循 RED→GREEN，先产生缺少 action helper/reducer 的 3 项失败，再引入最小可测试 helper/reducer 并转绿。
- 全量并行执行时，`studybuddy-extension-loader` 与真实 PATH 工具链探测可超过全局默认 5 秒；单独及 Node 24.14.0 环境下均通过。为反映真实外部进程探测时长，只提高这些局部测试时限至 30 秒，未放宽业务断言。
- Epicurus 最终复审发现 `AppShell.tsx` 文件末尾存在一个多余空白行，导致 `git diff --check` 失败；删除该行后复验通过。

## 5. 关键决定及依据

- 设置为独立页面：依据 09-UI §3.1 与 §4.1 的九个工作台 Tab 边界。
- 凭据只可 list/set/delete：依据 AGENTS.md §9.2/§9.3 与 06-API 既有契约；禁止读取明文。
- 敏感展示统一隐藏 UUID、密钥/Bearer、绝对路径（盘符/UNC/扩展 Windows/POSIX）和栈痕迹：依据 AGENTS.md §9.3、09-UI §11/§14.2。
- 不扩展 API：依据用户授权范围、AGENTS.md §2 和 T-M4-006 计划。

## 6. 测试证据

- RED：设置 action/navigation 测试新增后先失败 3 项（action helper 与 reducer 不存在），最小实现后通过。
- 定向：`renderer-settings-page` + `renderer-layout`：2 files / 28 tests passed。
- 受影响后端回归：credentials/settings、modelsConfig、toolchains manager、extension loader：13/13 passed（Node 24.14.0，局部 30s timeout）。
- 完整质量门：先出现 RED（Node 25.4.0 被 PATH 误选，两个既有 `js.node` healthy 断言失败）；修复 verify 子进程运行时继承后，Node 24.14.0 执行 `scripts/verify.mjs --stage=full` 成功（type-check、文档治理、unit/integration、contract coverage 127 handlers、安全不变量、build、smoke 6/6、真实 Electron E2E 16 files / 117 tests）。
- 文档治理：通过，仅既有 01-TRD 状态字段格式非阻塞警告。
- 独立审查：Heisenberg 与 Epicurus 最终复审均 PASS；Epicurus 发现的 EOF 空白行已删除，最终 `git diff --check` 通过。
- 真实桌面 UI 补强（2026-08-09）：以隔离数据根 `H:\pi-studybuddy-tmp\runs\T-M4-006\manual-ui\data` 启动真实 Electron 36.9.5，人工确认设置入口、通用/安全/开发者三分组、凭据“未配置”状态和脱敏说明、工具链健康行与实验性/调试/简洁模式开关。未输入或写入任何真实密钥。点击“返回学习工作台”后仍显示原“对话”Tab；再以 `Ctrl+,` 成功打开设置。截图：`03-settings-page.png`、`04-settings-security-developer.png`、`05-settings-developer-controls.png`、`06-workspace-after-return.png`、`07-settings-opened-by-shortcut.png`。

## 7. Git 证据

本会话未执行 `git add`、commit、push 或 merge；用户尚未明确授权 Git 收口。依 AGENTS.md §4.5、§7、§8.4，任务继续保持 `in_progress`，不可报告正式完成。

## 8. 未解决事项/下一步约束

- 等待用户明确授权后，才可按显式路径暂存、提交、推送，并在 master/origin 复验后改登记为 done。
- 不自动启动 T-M4-007 或其他后续任务。
- WPS/whisper 独立健康探测需另行批准，不能在本任务中补造。

