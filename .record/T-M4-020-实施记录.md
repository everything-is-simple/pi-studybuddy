# T-M4-020 实施记录

**任务 ID**：T-M4-020
**标题**：E2E 全链回归（后端断裂修复 + 设置页/学期切换 + S1-S7 接线）
**日期**：2026-08-11
**分支**：agent/T-M4-020-e2e-full-regression
**基线**：master=origin/master=11ba50e（T-M4-019 Git 收口事实核验）
**状态**：本地实施与验收完成；Git 收口待用户单独授权（AGENTS.md §8.4 未满足前不报告完成）

---

## 1. 任务裁决与范围

- **用户选择**：2026-08-11 明确选择 T-M4-020（"计划 T-M4-020"；prompt 资产已就绪 v0.1.96）；计划 `.plan/T-M4-020-e2e-full-regression.md` 经用户"批准"后实施，三项设计决策按推荐方案 A 生效：
  1. 设置页 E2E 密钥断言 = 仅 credentials.listKeys（隔离根空断言），不触碰真实密钥；
  2. S2 资料 E2E = SQL 预置资料 + 受控 convert（不驱动原生上传对话框，不动 MaterialsTab）；
  3. 学期切换 E2E = 多学期切换 + 归档只读提示。
- **范围**：补齐 renderer E2E 覆盖缺口（S1 首页/S2 资料/设置页/学期切换）+ 全量回归执行 + 最小缺陷修复；不引入新功能/API/schema。
- **非目标**：不引入未完成 Tab 新功能；不把假阳性当验收；不以覆盖率数字为唯一目标；不触碰真实数据根/密钥；不做安装包冒烟（T-M4-021）；不启动 T-M4-021。

## 2. 实际交付

| 文件 | 变更 |
|---|---|
| `tests/e2e/t-m4-020-home-renderer.test.ts`（新增） | S1 首页主路径：每日学习简报（tasks.dailyBrief 规则聚合）+ 任务列表（tasks.list）+ 考试倒计时（exams.list）+ 隐私断言 |
| `tests/e2e/t-m4-020-materials-renderer.test.ts`（新增） | S2 资料主路径：materials.list 列表 + 状态标识 + 转换接线（materials.convert 提交）+ 归档只读（转换禁用）+ 隐私断言（2 cases） |
| `tests/e2e/t-m4-020-settings-renderer.test.ts`（新增） | 设置页主路径：分区加载（学习偏好/模型供应商）+ 隔离根空凭证（credentials.listKeys）+ 返回学习工作台 + 隐私断言 |
| `tests/e2e/t-m4-020-semester-switch-renderer.test.ts`（新增） | 学期/课程切换：两学期预置 → 切换 → 笔记 Tab 数据刷新 + 归档只读（编辑禁用）+ 隐私断言（2 cases） |
| `docs/04-Todo` / `docs/00-索引` / `.plan/` | 治理同步（v0.1.141 / v0.1.145） |

> 无生产 src 变更（纯回归任务）。

## 3. 偏差

- **无范围偏差**。材料 E2E 仅验证 convert 的 renderer 接线与 Job 登记（转换中状态），不驱动真实文本提取——生产 `S2Context(dataRoot)` 未注入 extractor（T-M1-007 既有"默认未注入则 convert_* 仅登记 Job"边界），若强行断言 converted 完成即为假阳性。此边界登记于 §4/§8，不作为本任务缺陷修复（超出回归最小原则，需另行裁决）。

## 4. 问题及根因

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | home E2E 失败："CHECK constraint failed: task_type IN ('review','practice','note','exam_prep','other')" | E2E 种子任务 taskType='homework' 违反 05-ERD study_tasks.task_type CHECK 约束 | taskType 改为 'practice' → 通过 |

## 5. 关键决定及依据

| 决策 | 依据 |
|---|---|
| 三项设计决策按方案 A（设置页空凭证/资料 SQL 预置/多学期切换） | 用户 2026-08-11"批准"计划（计划 §11） |
| 材料 E2E 不驱动原生上传对话框、不断言假 converted 完成 | 08-Test §5.4 不连外部服务 + T-M1-007 生产 extractor 未注入边界；假阳性不可作为验收 |
| 设置页 E2E 仅空凭证断言 | AGENTS.md §9.2 密钥边界 + 08-Test §5.3 数据隔离 |
| 隐私断言统一覆盖（UUID/Windows/POSIX/file URI/栈/密钥） | AGENTS.md §9.3 + 09-UI §11.1 |

## 6. 测试证据

- **RED**：home E2E 初始失败（taskType CHECK），证据 `H:\pi-studybuddy-tmp\runs\T-M4-020\red\red-initial-failure.log`；materials/settings/semester-switch 首跑即通过。
- **GREEN**：4 个新 renderer E2E 全绿（home 1 + materials 2 + settings 1 + semester-switch 2 = 6 tests）。
- **全量质量门**（Node24.14.0/pnpm11.20.0）：
  - `pnpm test`：unit/integration/security 118 files/1130 tests（基线不回归）；
  - `pnpm test:e2e`：真实 Electron E2E 28 files/136 tests（基线 24/130，+4 files/+6 tests）；
  - `pnpm smoke`：6/6；`check-contract-coverage`：127 Api handlers + 8 PiBridge + 35 tools；
  - `check-desktop-security`：6/6；`check-uuid-leak`：7/7；
  - `check-docs-governance`：OK；`git diff --check`：通过；
  - `pnpm verify -- --stage=full`：全部通过（执行 8，跳过 2）。
- 测试运行数据隔离：`H:\pi-studybuddy-tmp\runs\T-M4-020\`，未写 `%LOCALAPPDATA%\PiStudyBuddy`，未连真实外部服务/密钥。

## 7. Git 证据

- 隔离分支：`agent/T-M4-020-e2e-full-regression`（自 `master=11ba50e` 切出）。
- 变更文件：新增 4 个 E2E + `.plan/T-M4-020-e2e-full-regression.md` + `.record/T-M4-020-实施记录.md`；修改 docs/04、docs/00-索引、.plan/00-当前任务.md。无生产 src 变更。
- 未执行 commit / merge / push（用户未授权 Git 收口）；工作区既有用户 dirty 文件（未跟踪 pi-session html）未混入。

## 8. 未解决事项/下一步约束

- **Git 收口待用户单独授权**：功能提交（测试）+ 治理登记提交 → master 快进合并 → Node24 master 完整 `verify --stage=full` 复验 → origin/master 推送核验。
- **生产 S2Context extractor 未注入边界**（T-M1-007 既有）：生产环境 materials.convert 仅登记 Job 不执行真实提取——学生上传转换功能在生产不可完成的潜在后端缺口，**不在本任务范围**（回归最小原则），建议后续单独裁决（如 T-M4-021 收官验收前评估或另立缺陷任务）。
- **T-M4-021（M4 收官验收 + 打包冒烟）**：pending，待本任务收口后按 §4.4 单独做计划；不自动启动。
