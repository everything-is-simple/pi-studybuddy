# 实施记录：T-M4-013 S3 练习 Tab RPC 接线

**任务 ID**：T-M4-013  
**日期**：2026-08-10  
**计划文件**：`.plan/T-M4-013-s3-practice-rpc.md`  
**分支**：`agent/T-M4-013-s3-practice-rpc`  
**集成基线**：`master=origin/master=7d93560`  
**状态**：`done`；本地实施、独立审查、问题修复、复验、master 集成与 origin/master 核验已完成。

## 1. 任务裁决与范围

**用户裁决**：用户批准 T-M4-013 从计划阶段进入业务实施；本轮只实施 S3 PracticeTab 的既有 RPC renderer 接线，不自动启动 T-M4-014~021。Git 收口必须另行获得明确授权。

**范围**：
- 在当前 `courseId` 下复用 `modules.list`，由学生在 PracticeTab 内显式选择一个或多个模块；不默认第一条、不写死模块 ID、不新增 AppShell 跨 Tab 模块状态。
- 接通既有 `practice.createSession`、`practice.getQuestions`、`practice.submit`、`practice.getResult`，保持既有 typed contract、handler 与 schema 不变。
- 保证作答前不向 renderer DOM/state 泄露 `correct_answer`、`acceptable_answers`、`explanation`；结果阶段才展示允许的信息。
- 保持课程/会话切换竞态保护、卸载保护、分阶段错误净化、前端计时/超时标记、`practice.timer` 接收和 archived 学期只读边界。
- 测试数据、Electron user-data、SQLite 与结果文件全部隔离于 `H:\pi-studybuddy-tmp\runs\T-M4-013\`。

**非范围**：
- 不新增/删除 API、handler、schema，不改变 AppShell 全局状态。
- 不实现 S4 错题写入或启动 T-M4-014~021，不连接真实外部服务，不写 `%LOCALAPPDATA%\PiStudyBuddy`。
- `practice.listSessions` 虽出现在历史 Todo 描述和既有 API/handler 能力中，但本轮批准目标是 PracticeTab 的四个 RPC 接线；该能力保留为历史契约，不在本轮新增或删除。

## 2. 实际交付

- `H:\pi-studybuddy\src\renderer\components\tabs\PracticeTab.tsx`
  - 局部 `selectedModuleIds: string[]` 显式多模块选择；课程无效、无模块或 archived 时不允许进入写流程。
  - 接通 `modules.list`、`practice.createSession`、`practice.getQuestions`、`practice.submit`、`practice.getResult`。
  - 状态机拆分为 `idle → creating → questions_loading → answering → submitting → result_loading → result`；`getResult` 失败仅可重试结果读取，不会重复 `practice.submit`。
  - 计时/超时状态、timer subscription、会话/课程 context guard、卸载 guard、固定错误文本与隐私展示边界。
- `H:\pi-studybuddy\src\agent-host\handlers\s3\lookup.ts`、`practice.ts`
  - archived 学期拒绝 `practice.createSession` 与 `practice.submit` 写入。
  - 模块查询增加 `knowledge_modules.id = moduleId`、`course_instance_id = courseId`、`deleted_at IS NULL` 归属校验，阻止跨课程模块。
- 测试与证据
  - 新增/更新 `tests/integration/t-m4-013-practice-rpc.test.ts`、`tests/integration/t-m4-013-s3-host-boundaries.test.ts`、`tests/e2e/t-m4-013-practice-renderer.test.ts`。
  - 更新既有 S3/S4 handler 与备份恢复 fixture，补齐合法模块归属以适配新增 ownership guard。
  - 覆盖 RPC 参数、防泄露、竞态/卸载、计时/超时、归档只读、隐私展示和真实 Electron renderer 路径。
- 治理资产：更新 `.plan/00-当前任务.md`、本计划、`docs/04-任务清单-Todo-List.md`、`docs/00-文档索引-Index.md`，创建本记录；`AGENTS.md` 的既有用户 dirty 修改未覆盖、未暂存、未提交。

## 3. 偏差

- 初始质量门在 E2E-08 复用的 synthetic module 未归属当前 course 时，ownership guard 按预期拒绝，导致该轮失败；修复为合法 course/module fixture 后重新运行，最终通过。该偏差是测试夹具与新增安全约束不一致，不是生产语义回退。
- 初轮 RED 记录为 3/3 失败，随后扩展为 5 条 mounted renderer 断言；保留 RED 证据，不以实现生成 golden 预期。
- 历史计划曾把 `practice.listSessions` 列入接线描述；经范围复核，本轮仅交付四个批准的 PracticeTab RPC，未扩展范围，也未删除既有契约；该范围裁决 supersedes v0.1.112 中的“listSessions 接线”描述，历史记录保留。

## 4. 问题及根因

1. **P1-01 archived 学期可写**：原 renderer/host 组合缺少 S3 写入的归档防线；在 `practice.createSession`、`practice.submit` 入口补充 host 侧写保护，并保留 renderer 禁用态。
2. **P1-02 getResult 失败可能重复提交**：提交与结果读取共用过宽状态推进；将状态机拆分为 `submitting` 与 `result_loading`，失败时仅重试 `getResult`。
3. **P1-03 模块归属不足**：只按 `moduleId` 查询可能跨课程命中；补充 `course_instance_id = courseId` 与 `deleted_at IS NULL` 约束。
4. **P2 防护证据不足**：补充多模块显式选择、计时/超时、敏感字段实际值、防分阶段错误泄露、课程切换/卸载竞态、真实 Electron E2E 与归档双层边界测试。

以上问题均已修复，并在 Node24 完整质量门下复验；审查者 A/B 未发现遗留 P0/P1。

## 5. 关键决定及依据

- 模块只能由学生显式选择，且按当前课程过滤；依据 09-UI §4.6、06-API §3.5、AGENTS.md §4.4、§9。
- 作答 DTO 与结果 DTO 分阶段处理，作答前禁止 `correct_answer`、`acceptable_answers`、`explanation` 进入 renderer；依据 08-Test 防泄露不变量与 06-API §3.5。
- archived 学期采用 renderer 禁用 + host 拒绝写入的双层边界；依据 AGENTS.md §9、既有 S1-S2 归档写防线和本任务安全审查结论。
- 不将 `practice.listSessions` 作为本轮隐式交付；历史能力保留，若未来需要单独登记/批准任务或范围变更；依据权威链和 AGENTS.md §4.4、§4.5、§11.2。
- 不执行 commit/merge/push；依据用户明确的四步收尾顺序与 AGENTS.md §7、§8.4。

## 6. 测试证据

Node24 质量门前置：

```text
$env:Path = "C:\node-v24.14.0-win-x64;$env:Path"
node --version  # v24.14.0
pnpm --version  # 11.20.0
```

定向与专项证据：

- S3/S4 unit + integration 定向：43 tests passed。
- E2E-08 backup/restore 定向：5 tests passed。
- `pnpm test`：109 test files passed，1057 tests passed。
- `node scripts/check-uuid-leak.mjs`：7/7 passed。
- `node scripts/check-desktop-security.mjs`：6/6 passed。
- `node scripts/check-docs-governance.mjs`：通过。
- `git diff --check`：通过。

完整质量门：

```text
node scripts/verify.mjs --stage=full：exit code 0
unit/integration：109 files / 1057 tests passed
真实 Electron E2E：18 files / 120 tests passed
contract coverage：127/127 handlers，8 PiBridge，35 tools
smoke：6/6 passed
security：6/6 passed
UUID leak：7/7 passed
docs governance：passed
git diff --check：passed
```

证据文件（均在任务隔离根）：

- `H:\pi-studybuddy-tmp\runs\T-M4-013\pnpm-test.log`
- `H:\pi-studybuddy-tmp\runs\T-M4-013\verify-full-final.log`
- `H:\pi-studybuddy-tmp\runs\T-M4-013\logs\verify-full.log`
- `H:\pi-studybuddy-tmp\runs\T-M4-013\pnpm-smoke.log`
- `H:\pi-studybuddy-tmp\runs\T-M4-013\uuid-leak.log`
- `H:\pi-studybuddy-tmp\runs\T-M4-013\logs\check-uuid-leak.log`
- `H:\pi-studybuddy-tmp\runs\T-M4-013\git-diff-check.log`
- 真实 renderer 结果：`H:\pi-studybuddy-tmp\runs\T-M4-013\e2e-renderer\case-01\renderer-result.json`

## 7. Git 证据

- 实施分支：`agent/T-M4-013-s3-practice-rpc`；功能提交：`7d93560`。
- `7d93560` 已进入 `master`；`git ls-remote origin refs/heads/master` 返回同一提交，证明 `origin/master` 已同步。
- 本次 master 复验：Node24.14.0/pnpm11.20.0 下 `pnpm verify -- --stage=full` 返回成功。
- 提交对象携带 `2026-08-11` 时间戳，而本次治理日期为 `2026-08-10`；该未来时间戳作为审计备注保留，不改变 ref 已核验事实。
- 当前工作区另有用户既有 `AGENTS.md` dirty 修改及未跟踪 `.codex/`，本任务未覆盖、未暂存、未提交；不得用 `git add -A` 或 `git add .` 混入。
- 按 AGENTS.md §8.4，docs/04 证据、master 复验和 origin/master 核验三项已齐，T-M4-013 登记为 `done`。

## 8. 未解决事项/下一步约束

- T-M4-013 已完成；不得因本记录自动启动 T-M4-015~021。
- 后续业务任务必须重新满足 AGENTS.md §4.4 单一执行任务门禁；T-M4-014 已由用户单独选择并另建计划。
- 不启动 T-M4-014~021，不新增 API/handler/schema/AppShell 全局模块状态，不写真实业务数据根。
- `practice.listSessions` 不属于本轮批准范围；本范围裁决 supersedes v0.1.112 中的“listSessions 接线”描述，历史记录保留；如需 renderer 接线，必须单独登记、计划和授权。
- 不执行 reset、clean、stash，不覆盖用户既有 dirty 文件。
