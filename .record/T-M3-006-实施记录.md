# T-M3-006 实施记录：09-UI 对话 Tab 业务 UI + 会话管理 UI

- **日期**：2026-08-08
- **里程碑**：M3 对话与打磨（done 5/8，本任务完成后 6/8）
- **任务**：T-M3-006 09-UI 对话 Tab 业务 UI + 会话管理 UI（壳层，阶段4）
- **计划文件**：[.plan/T-M3-006-chat-session-ui.md](../.plan/T-M3-006-chat-session-ui.md)（已批准 2026-08-08，保留为验收证据）
- **关联文档**：09-UI §4.2/§3.3/§7/§11.2 + 06-API §3.1（sessions.rename/export handler 归本任务）+ 03-Arch §6.7 + 07-WF §2.8 + 08-Test §6.5（E2E-10~13 归 T-M3-007）

---

## 1. 任务裁决与范围

**五裁决（用户 2026-08-08 确认开工 Prompt 时批准，均选推荐方案）**：
1. **export 落点=runs 测试隔离目录**：`sessions.export` 写入 `<dataRoot>/exports/`（PI_STUDYBUDDY_DATA_ROOT 注入隔离 → 测试写 runs 目录，生产写业务数据根），不污染 `%LOCALAPPDATA%\PiStudyBuddy`
2. **新建会话=内存仓库空白会话 + 立即成为当前会话**：当前承载层无 create 契约，置空选中态，发送首条消息时 agent.send 携带新会话（Ctrl+N，09-UI §11.2）
3. **unread=SessionSummary 可选字段**：`unread?: number` + fixture 演示值，无后台事件源仅展示（09-UI §3.3）
4. **backup_* 无目标 Tab**（T-M3-004 裁决 1a 遗留）：确认维持不渲染跳转按钮，留 T-M3-008 最终评估
5. **选中会话状态=AppShell 提升**：`activeSessionId` 提升到 AppShell，SessionSidebar 点击 → setActiveSessionId → ChatTab 受控渲染

**范围（做）**：sessions.rename handler + SessionStore.rename；sessions.export handler + SessionStore.export（md|json → {path}，脱敏）；AppShell 左侧栏占位 → SessionSidebar 组件（日期分组/模糊搜索/unread/学科标签颜色/新建/操作菜单）；会话切换业务化（activeSessionId 提升 + ChatTab 受控）；ChatTab 业务态补全（受控标题栏/错误态重试）。
**非目标（留后续）**：E2E-10~13（T-M3-007）、E2E-01~13 全链回归（T-M3-008）、真实 LLM（08-Test §5.4 全 mock）、真实 pi 会话目录读取（AGENTS.md §9.5 物理隔离）。

## 2. 实际交付

| 交付物 | 文件 | 说明 |
|---|---|---|
| SessionStore 扩展 | `src/agent-host/session-store.ts` | `rename(id, name)`（trim 校验 + updatedAt 刷新 + 不存在/空名返回 undefined）+ `export(id, format, destDir)`（md\|json → {path}，`sanitizeExport` 剥离完整 UUID/`sk-` 密钥形态）+ `makeContext` 提取重构 |
| unread 字段 | `src/contract/types.ts` | `SessionSummary.unread?: number`（09-UI §3.3 unread 徽标） |
| handler 补齐 | `src/agent-host/handlers/sessions.ts` | `sessions.rename`（失败 throw）+ `sessions.export`（走 store，exportDir 注入）；`SessionHandlerOptions` 增 `exportDir` |
| 装配接线 | `src/agent-host/index.ts` | `exportDir = <dataRoot>/exports/` 注入 |
| SessionSidebar | `src/renderer/components/SessionSidebar.tsx`（新增） | 日期分组（今天/昨天/本周 `groupLabel` 纯函数，now 可注入）+ 搜索框 + `subjectColor` 学科颜色纯函数 + unread 徽标 + 新建会话 + 选中高亮 + 重命名/删除/导出操作（prompt/confirm 交互） |
| AppShell 接线 | `src/renderer/components/AppShell.tsx` | `activeSessionId/sidebarSessions/searchQuery` 状态提升（裁决 5）+ 左侧栏替换 + sessions.list/search 数据流（L3 未建库降级内存过滤）+ 新建/重命名/删除/导出 handler |
| ChatTab 业务态 | `src/renderer/components/tabs/ChatTab.tsx` | 受控 `activeSessionId`（标题栏/无匹配提示）+ `sessionLoadError` 错误态（重试按钮）+ `ChatSessionSummary` 扩展 subject/goal/unread |
| 测试 | `tests/unit/session-store-ext.test.ts` + `tests/unit/session-sidebar.test.ts` + `tests/unit/chat-tab-business.test.ts` + `tests/integration/session-rename-export-handlers.test.ts`（+ 既有 renderer-layout.test.ts 左侧栏断言更新） | 共 27 新增/更新测试，全绿 |

## 3. 偏差

- **无范围偏差**。实现细节修正：
  1. `.tsx` 测试文件不被 vitest glob（`tests/unit/**/*.test.ts`）匹配 → 新测试统一用 `.test.ts`（React.createElement，与既有 renderer 测试一致）
  2. `renderer-layout.test.ts` 既有断言"左侧栏（导航区占位）"随本任务权威变更更新为 SessionSidebar 断言（09-UI §3.3/§7 替换占位）
  3. 测试断言 `toBeGreaterThanOrEqual` 不支持 ISO 字符串 → 改字符串字典序比较
  4. 本周分组语义：diffDays ∈ [2,6] 为"本周"，7 天前不显示（测试 fixture 用 5 天前）

## 4. 问题及根因

| 问题 | 根因 | 处置 |
|---|---|---|
| sessions.rename/export 未知方法 | handler 未实现（契约已定义，06-API §3.1 明确归 T-M3-006） | 补齐 handler + store 方法 |
| SessionSidebar import 失败 | 组件不存在（RED 预期） | 新建组件 |
| ChatTab activeSessionId/错误态断言失败 | props 未实现（RED 预期） | 补受控 props + 渲染分支 |
| 集成测试 export 落点可能污染业务数据根 | 默认 exportDir 派生自 dataRoot | 测试注入 PI_STUDYBUDDY_DATA_ROOT 隔离目录（仿 models-config-handlers.test.ts 范式，AGENTS.md §5.3） |

## 5. 关键决定及依据

- **裁决 1（export 落点）**：`<dataRoot>/exports/`，测试经环境变量隔离。依据：06-API §3.1 sessions.export 契约 `{ path }` + AGENTS.md §5.3 测试数据隔离
- **裁决 5（activeSessionId AppShell 提升）**：09-UI §7"会话即对话 Tab 内容，左侧栏选中会话，主内容区加载该会话"。依据：跨 Tab 状态保持 + ChatTab 解耦
- **搜索降级**：sessions.search（L3 bigram）返回空数组时降级 sessions.list 内存过滤（name/subject/goal/preview）。依据：T-M3-003 检索库未建立返回空数组不阻塞语义
- **导出脱敏**：md/json 均过 `sanitizeExport`（UUID→[id]、sk-*→[key]）。依据：AGENTS.md §9.3 永不记录完整 UUID/密钥

## 6. 测试证据

- **TDD 顺序**：RED（12 失败：7 store + 0 sidebar 组件缺失 + 2 ChatTab + 5 handler 缺失）→ GREEN → REFACTOR
- **新增**：`session-store-ext.test.ts`（7）+ `session-sidebar.test.ts`（10）+ `chat-tab-business.test.ts`（5）+ `session-rename-export-handlers.test.ts`（5）= 27
- **回归**：`renderer-layout.test.ts` 左侧栏断言更新（1）
- **全量**：966 单元/集成（939 + 27）全绿 + 83 E2E + smoke 6/6 + verify 全绿（执行 8，跳过 2）+ 文档治理通过（1 条既有警告）+ 契约覆盖 127 handlers + 安全不变量 6/6 + UUID 泄漏 7/7

## 7. Git 证据

（收尾提交后填写：分支 agent/T-M3-006-chat-session-ui → feat/test/docs 提交号 → master ff-only 合并 → origin/master 推送）

## 8. 未解决事项/下一步约束

- E2E-10~13（对话默认主入口/工具调用/@引用/TTS+L3 检索）→ T-M3-007（前置 T-M3-001~006 done）
- E2E-01~13 全链回归 + 安全不变量最终校验 → T-M3-008（P0）
- backup_* 目标 Tab 决策 → T-M3-008 最终评估
- 新建会话承载层 create 契约（当前置空选中态过渡）→ 后续会话持久化任务
- unread 计数无后台事件源驱动 → 仅 fixture 展示，事件源属后续会话持久化
