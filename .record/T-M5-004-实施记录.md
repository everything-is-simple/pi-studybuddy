# T-M5-004 实施记录

**任务 ID**：T-M5-004
**标题**：S1-S5 结构化学习页面逐控件修订
**里程碑**：M5 用户可用性验收 + UI 修订 + 一键交付
**分支**：`agent/T-M5-004-test-database-real-data`
**基线**：`master=origin/master=dacec56`
**日期**：2026-08-13
**当前状态**：done（T-M5-004 的原生可见 UI UAT、质量门、独立审查、授权 Git 收口、ff-only master 合并与远端核验均已完成；本记录保留为历史证据，未启动后续任务）

## 1. 任务裁决与范围

> 2026-08-13 复查更正：本记录早期小节保留当时实施流水，但其中“真机 UAT 8 路径 / 纯 UI 完整闭环 / 已满足 / done”的表述已被后续复查 supersede；当前权威状态以“附：测试阶段数据库修正”与“原生可见 UI 复查补证”小节为准：T-M5-004 仍为 `in_progress`，未 Git 收口。

- 用户明确指令"请 按 prompt 进行 施工 按固定的开发工作流 进行每一个任务的开发"批准 T-M5-004 开工（v0.1.166 登记 in_progress）。
- 范围：首页/S1 残余动作、S2 资料（导入/转换/预览/笔记）、S2 笔记（局部选择/思维导图/证据回链）、S3 练习（作答/提交/结果/加入错题）、S4 错题（筛选/详情/重做双动作/完整复盘）、S5 冲刺（已确认考试门控/模拟考/失败重试）逐控件修订。
- **用户裁决（2026-08-13）**：CTRL-MISTAKE-04 采用方案 A——扩展 `mistakes.get` 返回 question 摘要（题干/题型/我的答案/正确答案/解析），handler 层数据已存在（suggestErrorCause 已查询），可选字段向后兼容，方法数不变（contract 保持 127/127）。
- 不纳入：S6/S7/TTS/备份/设置/依赖自包含/发行验收/新增 API-schema-handler-Stream/真实外部服务。

## 2. 实际交付

| 页面 | 控件 | 交付 |
|---|---|---|
| 首页 | CTRL-HOME-01 | 任务「完成」动作（tasks.complete）+ 成功后刷新 + 失败固定中文 + 简报/任务/考试加载失败重试 + 考试「查看」详情展开 + 归档禁用 |
| 资料 | CTRL-MATERIAL-01~05 | 已转换资料「预览」（files.previewMarkdown/files.read，storageKey 相对路径）+ 预览失败固定中文可重试 + note_generating「重试生成笔记」（materials.retryAiGeneration 接线）+ 列表失败重试 + 归档全禁用 |
| 笔记 | CTRL-NOTE-05/06 | 「思维导图」入口（notes.getMindMap）+ 导图失败重试 + 模块证据「查看来源」真实回链（files.read，替代静态"资料回链"文字）+ 归档禁用 |
| 练习 | CTRL-PRACTICE-06 | 结果页错误题「加入错题」（mistakes.archive）+ 成功提示 + 失败固定中文 + 重复点击防线 + 静态空态无 action 按钮移除 |
| 错题 | CTRL-MISTAKE-01~04 | 「重做正确」/「重做错误」显式双动作（mistakes.redo correct?: boolean，handler 既有能力，contract 补登）+ 重做结果反馈 + 详情失败重试 + 作答历史 + **方案 A 完整复盘（题干/我的答案/正确答案/解析）** |
| 冲刺 | CTRL-CRAM-01~03 | 静态空态「生成试卷」改禁用 + 后端拒绝（未确认考试）固定中文错误 + 生成失败后 idle 恢复可重试 |

## 3. 偏差

- 无范围偏差。`mistakes.redo` contract 类型补登 `correct?: boolean` 与 06-API 文档同步（handler 既有能力，非新增方法）。
- `mistakes.get` 返回 DTO 扩展 question 摘要字段（方案 A 用户裁决），06-API §3.6 同步。

## 4. 问题及根因

| 问题 | 根因 | 处置 |
|---|---|---|
| NotesTab evidenceState?.materialId === undefined 时短路误判 | 可选链未覆盖后续字段访问 | 全链路可选链修复 |
| E2E mistakes 复盘 reviewVisible=false | E2E seed 未插 explanation 列，而复盘断言要求"解析"标签 | seed 补 explanation |
| UAT 阶段 B 各 step 未选择学期/课程 | 每次 step 新 Electron 实例，AppShell 上下文初始为"未选择" | 统一 selectSemesterCourse 前置（真实上下文联动） |

## 5. 关键决定及依据

- **方案 A（用户裁决）**：扩展 `mistakes.get` 返回 question 摘要，不新增方法（prompt §4.5 纳入 + §5 不新增 API 的平衡；handler 数据已存在）。
- 重做显式化：`mistakes.redo({id, correct})` 双按钮，基于 handler 既有 `correct?: boolean` 能力（非新 API）。
- 资料预览：files.previewMarkdown/files.read 接受相对路径（storageKey），复用 allowed-roots 白名单（不新增路径能力）。
- 证据回链：解析 sourceEvidenceJson.materialId → materials 查 storageKey → files.read（真实可点击回链，不静态冒充）。

## 6. 测试证据（本次实际运行）

| 类别 | 结果 |
|---|---|
| unit/integration | 历史流水：128 files / 1174 tests；当前最终复验见文末 v0.1.171：130 files / 1183 tests |
| 真实 Electron E2E | 历史流水：33 files / 143 tests；当前最终复验见文末 v0.1.171：33 files / 141 tests |
| type-check | 通过 |
| contract 覆盖 | 127/127（方法数不变） |
| 安全不变量 | 6/6 |
| UUID 泄漏 | 7/7 |
| 文档治理 | 通过 |
| git diff --check | 无错误 |
| 真机 UAT | 复查更正：该证据为 renderer 自动化/早期 UAT 流水，不能作为 §6.6 原生真机 UAT；当前仅有原生 OS 输入局部补证，完整 UAT/范围裁决未完成 |

新增测试文件：renderer-home-tab-actions / renderer-materials-actions / renderer-notes-actions / renderer-practice-actions / renderer-mistakes-actions / renderer-cram-actions（integration）+ t-m5-004-uat-renderer（e2e）+ s4-handlers GET-03。

## 7. Git 证据

- 历史实施分支 `agent/T-M5-004-s1-s5-structured-learning-ui` 曾用于早期实现；当前复查修正位于 `agent/T-M5-004-test-database-real-data`，基线 `master=origin/master=dacec56`。
- 工作区：当前含测试数据库修正、E2E 启动器/测试主进程修正、renderer 自动化重命名、docs/08 测试数据库治理修订以及计划/记录同步；未 stage/commit。
- **未 commit / merge / push**（等待用户单独授权 Git 收口）。

## 8. 未解决事项/下一步约束

- 待用户裁决完整原生真机 UAT/S2-S4 范围边界；在裁决前不得进入 Git 收口。
- CTRL-CRAM-04/05（速背卡朗读/掌握标记、冲刺计划动作）明确标记为 T-M5-005，未纳入。
- OCR/WPS/whisper 随包自包含、干净机全功能 UAT、发行验收属 T-M5-006/007/008。
- 每任务真机 UAT 铁律（v0.1.111）尚未满足：`tests/e2e/t-m5-004-uat-renderer.test.ts` 使用 `webContents.executeJavaScript`，只能作为 renderer 自动化 E2E；原生 OS 输入补证仅覆盖首次创建→S1 可达→重启持久化，未覆盖 S1 任务闭环或 S2/S3/S4 完整闭环。

---

# 附：独立复验修正（2026-08-13，v0.1.169）

## 复验结论（独立审查者）

独立复验指出 v0.1.168 的 done 登记过早：
1. UAT v1 仅验证 S1 创建 + S2-S5 空态/可达性，未走「创建→使用→重启回查」完整闭环，不满足 §6.6 铁律；
2. 截图证据仅 01 步 PNG，其余只有 JSON/DOM；
3. 全量 verify 曾因 T-M4-021 安装包残留进程锁 + T-M5-003 截图 0 字节出现非全绿。

## 修正处置

1. **renderer 自动化闭环（后续复查确认：非原生真机 UAT）**：`tests/e2e/t-m5-004-uat-renderer.test.ts` 重写为单一长会话 renderer 自动化流程：
   - 01 向导创建学期/课程 → 02 S1 面板新增任务/考试 + 确认考试 → 03 首页任务「完成」+ 考试「查看」展开 → 04 冲刺已确认考试生成模拟卷→作答→提交→结果 → 05 重启回查持久化。
   - 证据：phase-a/phase-b JSON + DOM + 非空 PNG 截图（52KB/50KB）+ renderer-automation-报告.md；因驱动层使用 `webContents.executeJavaScript`，不得命名或登记为原生真机 UAT。
   - 无纯 UI 数据创建入口的闭环（S2 资料文件对话框、S2 笔记/模块 AI 生成、S3 练习依赖模块、S4 错题依赖练习）→ 报告中如实登记为 P0/P1 功能缺口，不以空态冒充 UAT 成功。
2. **修复 2 个生产缺陷**（renderer 自动化闭环暴露的真实 bug）：
   - `mock-exam-generator.ts`：空课程无知识模块时用 `default-module` 假引用 → mock_exam_questions FK 约束失败；改 null。
   - `mock-exams.ts`：submitAttempt 写 mock_exam_module_analyses 时 null/unknown 模块 → FK 约束失败；空课程跳过分析行写入。
   - 新增 GEN-05 测试（空课程模拟卷生成 + knowledge_module_id NULL 断言）。
3. **历史全量 verify full 全绿**（清理残留进程后）：unit/integration 128 files/1175 tests、真实 Electron E2E 33 files/142 tests（UAT v1 2 tests→renderer 自动化 1 test）、contract 127/127、安全 6/6、UUID 7/7、docs 治理、diff-check 全部通过；T-M4-021 单独稳定化通过（残留进程环境性，非代码回归）。当前最终复验见 v0.1.171 小节。
4. 任务状态：docs/04 v0.1.169 回退 done→in_progress；待本次补齐完成后重新登记。

## 复验修正后的质量门证据

| 类别 | 结果 |
|---|---|
| unit/integration | 历史复验：128 files / 1175 tests（+GEN-05）；当前最终复验：130 files / 1183 tests |
| 真实 Electron E2E | 历史复验：33 files / 142 tests（renderer 自动化闭环，非原生真机 UAT）；当前最终复验：33 files / 141 tests |
| 真机 UAT | 复查更正：未完成完整原生真机 UAT；仅有原生 OS 输入局部补证（首次创建→S1 可达→重启持久化） |


---

# 附：测试阶段数据库修正（2026-08-13，v0.1.171）

## 复查结论

用户明确要求：系统进入测试阶段后，业务数据不能依赖 renderer mock 数据；测试必须使用专门、隔离的测试数据库。复查确认生产数据层本来就是 SQLite：业务根包含 `global.db` 与 `semester/<semester-id>/sem.db`，默认落点为 `%LOCALAPPDATA%\PiStudyBuddy`；测试通过 `PI_STUDYBUDDY_DATA_ROOT` 指向 `H:\pi-studybuddy-tmp\runs\<task-id>`。

## 修正处置

1. 新增 `tests/helpers/test-database.ts`：启动真实 Electron 前，在专属运行根建立独立 `global.db` 与 `semester/<id>/sem.db`；学期、课程、资料、任务考试等优先通过正式 schema/handler 建立；当前没有公开 `modules.create` RPC 的知识模块仅写入该专属测试库，并保留真实 FK/CHECK/触发器约束。
2. `tests/e2e/e2e-04-sprint.test.ts` 改为从专用 SQLite 测试库启动，Electron 运行期间只调用正式业务 RPC；删除对运行中 `test.seedModule` 的依赖。
3. `tests/e2e/test-main.js` 删除 `test.seedModule` 测试 RPC，并改用 `initializeDataRoot()`；不再暴露运行中直接 seed 数据的测试入口。
4. `src/main/data-root-init.ts` 修复建库连接生命周期：`initializeDataRoot()` 返回前关闭 `createGlobalDb()` 打开的连接，避免 Windows WAL/SHM 锁阻塞测试库清理或后续 Electron 独占打开。
5. `docs/08-测试验收-Test-Plan.md` §9.1 增加专用测试数据库、禁止运行中 `test.*` seed RPC 和 SQLite 连接关闭边界。

## 证据

- 专用测试数据库单元/集成：`tests/integration/t-m5-004-test-database.test.ts`：2 tests 通过。
- S1/S2/S5 正式 handler + 专用 SQLite 建库验证：同文件 TESTDB-01 通过。
- E2E-04：真实 Electron、专用数据库、10 tests 通过。
- `pnpm test`：最终复验 **130 files / 1183 tests** 通过。
- `node scripts/verify.mjs --stage=full`：2026-08-13 通过；unit/integration **130 files / 1183 tests**，真实 Electron E2E **33 files / 141 tests**，contract 127/127，安全 6/6，UUID 7/7，docs governance 与 diff-check 通过；最终日志：`H:\pi-studybuddy-tmp\runs\T-M5-004\final-verify-full-clean-log-20260813.log`。

## 当前边界

- `tests/integration` 的 renderer 组件行为测试继续使用 `createMockRpcClient`，其定位是组件级故障/重试/禁用语义测试，不是系统业务数据验收；系统 E2E 数据已切换为专用 SQLite。
- S3/S5 的确定性题目生成器仍是外部 AI 的受控替代，符合 08-Test §9.3 “外部服务不连真实服务”的测试边界；题库实体、作答结果、模拟考结果均由真实 handler 持久化到专用 SQLite。
- T-M5-004 仍保持 `in_progress`：现有 renderer 自动化驱动仍使用 `webContents.executeJavaScript`，而唯一计划曾明确禁止 CDP 注入；S2/S3/S4 无纯 UI 创建入口的范围裁决仍未完成。
- 本轮未 commit、merge、push；不得报告 done。

---

## 2026-08-13 原生可见 UI 复查补证（不替代完整 UAT）

为核对“真机 UAT 不得以 renderer 注入替代”的边界，使用真实 Electron 应用和全新隔离根 `H:\pi-studybuddy-tmp\runs\T-M5-004\native-os-input-20260813\` 做了操作系统级鼠标、键盘驱动；未使用 `webContents.executeJavaScript`、CDP、handler/RPC 直调、数据库预置或真实业务数据根。

- NATIVE-01：空数据首次启动界面可见。
- NATIVE-02：经可见首次向导创建学期和课程。
- NATIVE-03：可见地选择该课程并进入 S1“学习计划管理”。
- NATIVE-04：停止后以相同隔离根重启，展开学期树后仍可见所创建课程；对应 `global.db` 与 `semester/<id>/sem.db` 的持久化观察记录在同一运行根。
- 证据：`native-uat-evidence.json`、`native-uat-database-observation.json`、`00/02/04/05/08/09/13/14/15` 的 PNG 截图；可用关键截图为非空截图，个别占位文件不作为验收证据。
- NATIVE-05 尝试继续进入 S1 任务表单时导航落入无关工作台；**没有创建或完成任务，不能作为 S1 任务闭环证据**。该负面结果已写入 `native-uat-evidence.json`，不得以旧的 renderer 注入 UAT v2 或此局部补证声称 8 路径完成。

**结论（supersedes 本记录先前将 UAT v2 叙述为“纯 UI 完整闭环”的表述）：** 当前仅新增“非注入的首次创建→S1 可达→重启持久化”局部真机证据。T-M5-004 仍为 `in_progress`；S1 任务/考试/冲刺完整闭环及 S2/S3/S4 的纯 UI 创建入口或范围裁决尚未完成，Git 收口继续等待用户明确授权。


---

## 2026-08-13 用户同意的 S2→S3→S4 最小闭环扩展

- **裁决**：用户在明确说明“创建前置条件必须属于 T-M5-004”后，对“增加 S2 可见模块创建入口与最小 `modules.create` API/handler”作出明确“同意”。该裁决依 AGENTS.md §2 优先级 supersede 本任务原“无新增 API/handler”限制。
- **原因**：现有生产契约只有 `modules.list` / `modules.get` / `modules.updateLearnStatus`；模块仅能由未装配的 S2 AI job processor 产生。禁止 DB 预置、RPC/handler 直调、renderer 注入与真实外部 AI 后，S3/S4 没有合规的纯 UI 前置路径。
- **最小范围**：仅 `modules.create` typed RPC、S2 host handler、NotesTab 在学生显式选定资料后的局部创建表单；要求课程/资料归属校验、归档写保护、`source_evidence_json` 回链、`ai_generated=0`。不改 SQLite schema、Stream、跨 Tab 全局状态，不接真实外部 AI。
- **后续顺序**：先增加失败的 handler/renderer 测试（RED），再最小实现（GREEN），最后以全新隔离根和可见 Electron UI 重做 S1-S4 UAT；Git 仍禁止收口。

---

## 2026-08-13 原生可见 UI 完整 UAT 补证与最小修复

### 结论

本节 supersede 本记录中“原生真机 UAT 尚未满足”及“`modules.create` RED 待执行”的当前态描述：在真实 Electron、全新隔离根 `H:\pi-studybuddy-tmp\runs\T-M5-004\native-uat-v3-20260813-0119\app-data` 中，已经仅通过可见 UI 完成 S1-S4 的创建/使用/重启回读闭环。全过程未使用 `webContents.executeJavaScript`、CDP、RPC/handler 直调、数据库预置或真实业务数据根；允许的本地合成 Markdown 文件仅经系统可见文件选择器导入。

任务状态仍为 `in_progress`：本节只完成本轮真机 UAT、实现和质量门证据同步；Git 收口尚未获得用户单独授权，04-Todo 也不得登记 done。

### 原生 UAT 证据（按用户裁决）

| 路径 | 可见 UI 操作与结果 | 关键证据 |
|---|---|---|
| S1 | 首次创建“2026 秋季学期 / 高等数学” → 创建“完成极限复习” → 完成任务 → 原生关闭并以同一隔离根重启 → 回读“完成极限复习 · 已完成” | `62-task-completed.jpg`、`75-s1-complete-readback-after-restart.jpg` |
| S2 | 通过 Windows 文件选择器导入 `limits.md` → 首次转换失败暴露真实缺陷 → 修复后以可见“重试转换”完成转换 → NotesTab 显式选择资料 → 创建“极限基本概念”模块 → 重启后回读资料“已转换”和模块（即使资料暂无笔记也仍显示） | `82-file-picker-path-typed-visible.jpg`、`83-material-upload-post-submit.jpg`、`84-material-conversion-result.jpg`、`93-s2-native-retry-conversion-result.jpg`、`99-s2-module-create-confirmed.jpg`、`122-repaired-s2-module-readback-after-restart.jpg` |
| S3 | 以 S2 可见创建的模块作为前置 → 选择模块并开始练习 → 作答/提交，得到“练习结果”与逐题结果 → 重启后练习页仍可见“极限基本概念”模块可再次选择 | `103-s3-practice-start-result.jpg`、`104-s3-practice-submission-result.jpg`、`125-s3-after-restart-module-readback.jpg` |
| S4 | 从 S3 提交结果对错误题执行“加入错题” → 错题列表/详情 → 选择并确认错因 → “重做正确” → 状态变为“已掌握” → 重启后列表仍显示“已掌握” | `107-s4-after-archive.jpg`、`108-s4-mistake-detail.jpg`、`112-s4-error-cause-confirmed-controls.jpg`、`115-s4-redo-correct-result.jpg`、`124-s4-after-restart-readback.jpg` |

- 证据目录：`H:\pi-studybuddy-tmp\runs\T-M5-004\native-uat-v3-20260813-0119\evidence\`。
- S3/S4 采用受控、确定性题目生成器，不连接真实外部 AI；练习、错题与状态均经真实 UI → 正式 handler → 隔离 SQLite 持久化。
- 仅为验证隔离边界观察该运行根下 `global.db` 与学期库；数据库观察不代替任何用户路径证据。

### 原生 UAT 暴露的缺陷与 RED → GREEN 修复

| 缺陷 | 复现/根因 | RED → GREEN 处置 |
|---|---|---|
| `.md/.txt` 可上传但无法转换 | 上传类型允许 Markdown/文本；转换 job 路由把它们按 `pdf` 传给提取器，导致 UAT 中 `limits.md` 显示“转换失败” | 先在 `tests/unit/agent-host/s2/text-extractor.test.ts` 和 `tests/integration/t-m1-007-convert-pipeline.test.ts` 建失败断言；`materials.ts` 按资料真实 `file_type` 解析，`text-extractor.ts` 加入 UTF-8 `md/txt` 提取。定向 17 tests GREEN，之后可见“重试转换”成功。 |
| 无笔记资料的已持久化模块在重启后不可见 | `notes.get` 返回 NOT_FOUND 时 NotesTab 提前返回，仅显示“该资料暂无笔记”，隐藏已通过 `modules.list` 加载的模块 | 先新增 `renderer-notes-actions.test.ts` 的“资料暂无笔记时，已持久化知识模块仍必须可见”失败测试；在该空态分支渲染既有模块列表。定向 renderer 测试 6/6 GREEN，重启截图 `122-...jpg` 可见模块。 |

### 本轮质量门（Node 24.14.0 / pnpm 11.20.0）

- 定向回归：`pnpm vitest run tests/integration/renderer-notes-actions.test.ts tests/unit/agent-host/s2/text-extractor.test.ts tests/integration/t-m1-007-convert-pipeline.test.ts tests/integration/s2-handlers.test.ts tests/integration/t-m5-004-test-database.test.ts` → **5 files / 61 tests 通过**。
- 全量：`pnpm verify --stage=full` → **exit 0**，日志 `H:\pi-studybuddy-tmp\runs\T-M5-004\final-verify-full-native-uat-v3-20260813.log`，退出码记录 `...native-uat-v3-20260813.exit.txt`。
  - unit/integration：**130 files / 1189 tests**；
  - contract coverage：**128 Api handlers / 8 PiBridge methods / 35 工具**；
  - desktop security：**6/6**；
  - 真实 Electron E2E：**33 files / 141 tests**；
  - docs governance：14 份设计文档 + 2 个 Skill + 2 个 prompt 通过；
  - verify：执行 8、跳过 2，全部通过。

### 收口边界

- 已完成：用户裁决的 S1-S4 非注入可见 UI 闭环、最小 `modules.create` 扩展、UAT 发现缺陷的 RED→GREEN 修复、全量质量门。
- 未完成：docs/04 尚未登记 done、master 复验/推送尚未发生、用户也尚未单独授权 Git 收口；因此不得宣称 T-M5-004 已正式完成，也不得 stage/commit/merge/push。

---

# 附：审查 P1 修正与原生 UAT v3 证据降级（2026-08-13，v0.1.175）

> **本节 supersedes 本记录中任何将 `native-uat-v3-20260813-0119` 的 S3 路径表述为“已通过完整闭环”的结论；历史截图、数据库与原始日志均保留，不删除、不改写。**

## 复核发现与状态修正

双独立审查发现：v3 隔离 SQLite 中存在同一资料下两个同名、未归档的“极限基本概念”知识模块，创建时间相差约一分钟。因此 v3 的 S3“选择模块→练习→提交→重启回读”证据无法证明选择对象唯一，**S3 路径自 2026-08-13 起降级为不可判定，不得作为通过证据**。S1、S2 的创建/重启历史事实以及 S4 的错题闭环历史事实保留，但不能替代“唯一模块前置条件”的重新证明。

## RED → GREEN 修正

1. `src/agent-host/handlers/s2/modules.ts`：在正式 `modules.create` handler 写入前，按 `material_id + module_name + deleted_at IS NULL` 拒绝同资料同名活动模块；不改 schema、Stream、跨 Tab 状态或真实外部 AI。
2. `src/renderer/components/tabs/NotesTab.tsx`：将该受控 `BAD_REQUEST` 映射为“该资料下已存在同名知识模块，请修改模块名称。”；其他异常保持泛化提示，DOM 不展示内部错误文本。
3. `tests/integration/s2-handlers.test.ts`：新增真实 SQLite 的重复创建拒绝与仅一行持久化断言。
4. `tests/integration/renderer-notes-actions.test.ts`：新增友好错误显示且不泄露内部错误文本的组件断言。
5. `docs/08-测试验收-Test-Plan.md` 与 `tests/e2e/test-main.js`：明确 `test.turnEndIndex` 是只读/纯计算的 E2E 进程桥接例外；禁止任何创建、预置或篡改业务实体的 `test.*` RPC，且例外不能被 renderer 调用。

定向回归（Node 24.14.0 / pnpm 11.20.0）：

```text
pnpm vitest run tests/integration/t-m5-004-test-database.test.ts tests/integration/s2-handlers.test.ts tests/integration/renderer-notes-actions.test.ts
3 files / 46 tests passed
pnpm test:e2e -- tests/e2e/e2e-04-sprint.test.ts
1 file / 10 tests passed
pnpm type-check
passed
node scripts/check-docs-governance.mjs
OK: 14 docs + 2 skills + 2 prompts
```

## 时间与运行数据说明

- 原 `final-verify-full-native-uat-v3-20260813.log` 的 `completed_utc=2026-08-14...` 来自执行主机时钟，晚于治理日 **2026-08-13**；它只能作为日志字段保留，**不能用作任务日历事实**。
- 专属测试库/WAL 文件保留在 `H:\pi-studybuddy-tmp\runs\T-M5-004\`，用于审计与失败复现；不得把“Windows 进程内 SQLite WAL 句柄可立即清理”写成已验证事实，更不得触碰 `%LOCALAPPDATA%\PiStudyBuddy` 真实业务数据根。

## 当前门槛

必须以新的全新隔离数据根，使用真实 Electron 的非注入、可见 UI 重新完成：创建唯一 S2 模块 → 尝试同名创建并看到无泄露错误 → 重启确认仅一项 → S3 选择该唯一项、练习/提交 → 重启回读；之后才可重新进行两份独立审查。T-M5-004 继续为 `in_progress`，且未获单独 Git 收口授权，禁止 `git add`、commit、merge 或 push。

---

## 复验附录：v4c S3 重启结果读取缺口与最小修复（2026-08-14）

### 事实与范围

- v4c 原始原生 UI 流程已完成 S2 单模块创建、同名拒绝、首次重启唯一模块回读，以及 S3 唯一模块练习创建、作答、提交。
- 原始第二次重启观察 `93-s3-second-restart-readback.json/png` 如实显示：已提交练习没有可见历史入口，用户只能重新创建练习。因此 S3 的“结果读取”不能以旧证据宣称通过。
- 本轮只在 T-M5-004 既定 S3“结果读取”范围内，消费既有 typed RPC `practice.listSessions` 并通过既有 `practice.getResult` 增加 PracticeTab 局部、可见的“已完成练习 / 查看结果”入口；未新增 API、schema、handler、Stream、跨 Tab 全局状态或真实外部 AI。

### RED → GREEN 与回归

- RED：`tests/integration/renderer-practice-actions.test.ts` 新增“RED 5: 重启后读取已完成练习，点击可见入口后展示持久化结果且不泄漏会话标识”。在 renderer 未调用 `practice.listSessions` 时失败，预期的调用数组为空。
- GREEN：PracticeTab 在课程上下文下读取既有已完成 session，并将可见“查看结果”连接到既有 `practice.getResult`；不在 DOM 显示 session ID。
- 定向回归：`renderer-practice-actions`、`t-m4-013-practice-rpc`、`s2-handlers`、`renderer-notes-actions`、`t-m5-004-test-database` 共 **5 files / 59 tests** 通过；`pnpm type-check` 与 `pnpm build` 通过。

### 原生可见 UI 复验

证据根：任务专用 v4c 运行根下的 `evidence/`（不提交、不访问真实数据根）。

- `68-duplicate-module-friendly-error.json/png`：同资料同名模块显示脱敏可操作错误且仍仅一个模块；
- `76-restart-unique-module-readback.json/png`：首次重启回读唯一模块；
- `83-s3-practice-submit-result.json/png`：S3 作答并提交；
- `93-s3-second-restart-readback.json/png`：保留原始“无可见结果读取入口”缺口；
- `94`–`99`：构建边界、关闭和可见重启事实；
- `105-second-restart-practice-history-visible.json`：修复后的第二次重启显示“已完成练习”和可见“查看结果”；
- `106-s3-second-restart-result-readback.json/png`：点击可见入口后显示持久化的得分、正确数、用时、提交状态和逐题结果；
- `107-app-closed-after-result-readback.json`：可见窗口关闭完成。

整个 UAT 未使用 `webContents.executeJavaScript`、CDP、RPC/handler 直调、数据库预置或 renderer 注入。详见同一运行根的 `UAT-v4c-原生可见UI-补证报告.md`。

### 收口状态

- T-M5-004 仍为 `in_progress`。
- 当前完整质量门已通过：2026-08-14 以 Node v24.14.0 / pnpm 11.20.0 执行 `pnpm verify --stage=full`，unit/integration **130 files / 1192 tests**、真实 Electron E2E **33 files / 141 tests**，verify 执行 8、跳过 2，退出码 0；日志位于同一 v4c 运行根的 `verify-full-s3-readback-20260813.log`。双独立复审仍待，任何 P0/P1 均须处置。
- 本轮未执行 Git stage / commit / merge / push；仍等待用户单独授权 Git 收口。

# 附：非 Git 收尾准备与双独立复审闭合（2026-08-14）

- v4c 的 S3 修复遵循最小范围：PracticeTab 只消费既有 `practice.listSessions` 和 `practice.getResult`，增加局部可见的已完成练习/查看结果入口；不新增 API、schema、handler、Stream、跨 Tab 全局状态或真实外部 AI。
- 原生 UAT 的修复前缺口 `93-s3-second-restart-readback` 保留；修复后 `105`/`106` 证明第二次重启的可见历史和结果读取。全过程继续满足非注入、非直调、非预置的用户 UAT 边界。
- 2026-08-14 使用 Node v24.14.0 / pnpm 11.20.0 执行 `pnpm verify --stage=full`，unit/integration **130 files / 1192 tests**、真实 Electron E2E **33 files / 141 tests**，退出码 0；文档治理与 `git diff --check` 通过。
- 独立审查 A（业务/UI/UAT）与 B（治理/测试边界/收尾门禁）最终均为 **P0=0、P1=0**；审查记录为 `.record/T-M5-004-审查-A.md` 与 `.record/T-M5-004-审查-B.md`。
- 这不改变任务完成状态：T-M5-004 仍为 `in_progress`，未执行 stage / commit / merge / push，等待用户单独授权 Git 收口。
---

# 附：Git 收口执行中（2026-08-14）

- 用户已明确授权 T-M5-004 Git 收口。
- 功能与非 Git 实施证据已在提交 `47639f9`（`feat(m5): T-M5-004 真实学习闭环与测试治理`）中提交；该提交已由 `agent/T-M5-004-test-database-real-data` 通过 `git merge --ff-only` 快进合并至本地 `master`。
- 合并后以 Node v24.14.0 / pnpm 11.20.0 执行本地 master `pnpm verify --stage=full`，结果为 unit/integration **130 files / 1192 tests**、真实 Electron E2E **33 files / 141 tests**、执行 8 / 跳过 2、exit 0；日志：`H:\pi-studybuddy-tmp\runs\T-M5-004\git-closeout-20260814\master-verify-full.log`。
- 本附录的推送前状态已完成：治理登记 `715add3` 已成功 `git push origin master`，并核验 `HEAD=origin/master=refs/heads/master=715add3`。本最终完成登记随后的推送及远端同位核验完成后，按 AGENTS.md §8.4 任务由 `in_progress`→`done`；不启动 T-M5-005~008。
---

# 附：正式 Git 收口完成（2026-08-14）

## 7. Git 证据补充

- 用户已明确授权 Git 收口。
- 功能提交：`47639f9 feat(m5): T-M5-004 真实学习闭环与测试治理`。
- 本地收口登记：`715add3 docs(m5): T-M5-004 本地收口登记`；已推送并核验 `master=origin/master=715add3`（远端 `refs/heads/master` 一致）。
- 最终完成登记：本节随本次显式 stage/commit/push 提交；push 后再次核验当前 `HEAD`、`origin/master` 与远端 `refs/heads/master` 同位。

## 8. 未解决事项/下一步约束

- T-M5-004 已完成，不再修改其范围或证据。
- T-M5-005~008 仍为 pending；本轮不创建其计划、不写其实现、不预授权其 Git 收口。
