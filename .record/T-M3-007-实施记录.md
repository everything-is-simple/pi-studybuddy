# T-M3-007 实施记录：E2E-10~13 对话默认主入口/工具调用/@引用/TTS+L3 检索

- **日期**：2026-08-08
- **里程碑**：M3 对话与打磨（done 6/8，本任务完成后 7/8）
- **任务**：T-M3-007 E2E-10~13（对话默认主入口/工具调用/@引用/TTS+L3 检索，测试，阶段5）
- **计划文件**：[.plan/T-M3-007-e2e-chat.md](../.plan/T-M3-007-e2e-chat.md)（已批准 2026-08-08，保留为验收证据）
- **关联文档**：08-Test §6.5（通用 AI 对话 E2E）+ 06-API §3.1/§3.1.1/§3.2（sessions.*/agent.send/files.read）+ 05-ERD §4.3（L3 对话 Tab 会话承载）+ 07-WF §2.8（工具→Tab 映射表）+ AGENTS.md §9.4（allowed-roots 白名单）

---

## 1. 任务裁决与范围

**任务裁决（用户 2026-08-08 确认开工 Prompt 时批准）**：
1. **test-main.js 装配对话承载层 handler**：复用 dist 现有 `createAgentHandlers` / `createSessionHandlers` / `createModelHandlers` / `createFileHandlers`（files.read @引用白名单），不新增契约方法
2. **agent.events 事件推送承载**：IPC `{"type":"event","topic","key","payload"}` + `eventForwardServer` shim，模拟 Streams["agent.events"] 推送
3. **RpcDriver waitForEvent 订阅辅助**：事件缓冲回溯 + 谓词匹配，测试可断言事件序列
4. **L3 检索测试直调生产链路**：`test.turnEndIndex` 测试专用 handler 直调生产 `indexTurnEndChunks`（不引入 mock 重写索引逻辑）
5. **electron-launcher 二次 launch 复用 dataRoot**：`reuseDataRoot` 选项保留数据根，验证 L3 跨进程持久化

**范围（做）**：E2E-10（对话默认主入口 + 流式事件序列 message_start→token→context_compressed + 防泄露）+ E2E-11（触发词→tool_call/tool_result 事件对 + toolJumpTarget 映射出题→练习 Tab + sessionMeta 写回）+ E2E-12（@引用 allowed-roots 白名单通过 + 越权拒绝 + 相对路径解析 + TTS 朗读 + 标记已复习）+ E2E-13（turn_end 增量索引幂等 + 跨进程持久化 + search 无 UUID）+ 三处承载层根因修复（global.sql.ts 幂等建表 / indexer.ts 建父目录 / turn-end.ts 幂等计数修正）。
**非目标（留后续）**：E2E-01~13 全链回归最终校验（T-M3-008）、backup_* 目标 Tab 最终评估（T-M3-008）、真实 LLM（08-Test §5.4 全 mock）。

## 2. 实际交付

| 交付物 | 文件 | 说明 |
|---|---|---|
| 测试承载装配 | `tests/e2e/test-main.js` | 装配 `createSessionHandlers`/`createAgentHandlers`/`createModelHandlers`/`createFileHandlers` + `eventForwardServer` shim + `test.turnEndIndex` 直调生产 `indexTurnEndChunks` |
| 事件订阅辅助 | `tests/e2e/helpers/rpc-driver.ts` | `waitForEvent`（事件缓冲回溯 + 谓词匹配 + 超时拒绝） |
| 启动器扩展 | `tests/e2e/helpers/electron-launcher.ts` | E2E_RUN_DIR → `runs\T-M3-007\e2e` + `reuseDataRoot` 二次 launch 保留数据 |
| E2E 用例 | `tests/e2e/e2e-10-chat-entry.test.ts` | E10-01~06：默认主入口 + 流式事件序列 + 防泄露 |
| E2E 用例 | `tests/e2e/e2e-11-tool-jump.test.ts` | E11-01~04：触发词事件对 + toolJumpTarget 映射 + sessionMeta 写回 |
| E2E 用例 | `tests/e2e/e2e-12-attach-tts.test.ts` | E12-01~06：@引用白名单 + 越权拒绝 + 相对路径 + TTS + 标记已复习 |
| E2E 用例 | `tests/e2e/e2e-13-l3-search.test.ts` | E13-01~04：幂等索引 + 跨进程持久化 + search 无 UUID |
| 承载层修复 | `src/data/schema/global.sql.ts` | 建表/索引幂等 `IF NOT EXISTS`（复用 dataRoot 二次启动） |
| 承载层修复 | `src/data/l3/indexer.ts` | `openConversationDb` 建父目录（memory/l3/ 不存在报 unable to open） |
| 承载层修复 | `src/agent/turn-end.ts` | `indexTurnEndChunks` 幂等计数修正（seq 推进 + `RETURNING id` 判定实际新增） |

**测试规模**：4 E2E 文件 +20 用例（+27 含相关断言），E2E 总数 83 → 110。

## 3. 偏差

- **无范围偏差**。实现细节修正：
  1. `test-main.js` 初版 require 路径错误 → 修正为 `../../dist/agent-host/session-store` 与 `../../dist/agent-host/handlers/sessions`
  2. E2E-12 初版缺 files.read handler → 补 `createFileHandlers(noopFileWatch, { dataRoot })`（no-op FileWatchService）
  3. E2E-13b 复用 dataRoot 二次启动报 `table semesters already exists` → global.sql.ts 全量建表/索引加 `IF NOT EXISTS`
  4. E2E-13 报 `unable to open database file`（memory/l3/ 目录缺失）→ openConversationDb 建父目录
  5. E13-03 断言 `expected 2 to be less than or equal to 1`（written 虚增）→ turn-end.ts 用 `RETURNING id` 判定实际新增，仅实际新增才写 chunks_fts 并计数
  6. `DatabaseSync.changes` 类型缺失 → 类型断言 `(db.db as unknown as { changes: number }).changes`

## 4. 问题及根因

| 问题 | 根因 | 处置 |
|---|---|---|
| agent/sessions/modelsConfig handler 未知方法 | test-main.js 原装配缺对话承载层 handler | 复用 dist 现成`create*Handlers`装配 |
| files.read 未知方法 | E2E-12 缺 @引用白名单 handler | 补 `createFileHandlers` |
| table semesters already exists | 复用 dataRoot 二次启动，建表非幂等 | global.sql.ts 加 `IF NOT EXISTS` |
| unable to open database file | L3 库父目录 memory/l3/ 不存在 | openConversationDb 建父目录 |
| written 计数虚增 | seq 停在冲突处导致 seq0 重复 chunk + 幂等跳过仍计数 | RETURNING id 判定实际新增 + seq 推进 |

## 5. 关键决定及依据

- **裁决 1（复用 dist handler）**：不新增契约方法，E2E 直接打真实 handler 装配。依据：06-API 契约已定 + AGENTS.md §4.2 不越权
- **裁决 2（eventForwardServer shim）**：模拟 Streams 推送，使 renderer 外部测试可断言事件序列。依据：08-Test §6.5 需断言 message_start→token→context_compressed
- **裁决 4（test.turnEndIndex 直调生产）**：避免 mock 重写索引逻辑导致 E2E 无法验证真实 turn_end 行为。依据：§5.1 TDD 不失真 + 05-ERD §4.3
- **裁决 5（reuseDataRoot）**：验证 L3 跨进程持久化（二次 launch sessions.search 命中）。依据：05-ERD §4.3 会话承载
- **幂等修复三处**：数据库 schema/索引幂等 + 建库建目录 + 索引计数幂等。依据：AGENTS.md §5.3 测试运行数据隔离 + 复用 dataRoot 语义

## 6. 测试证据

- **TDD 顺序**：RED（4 E2E 文件初建，缺承载 handler/事件/目录/幂等失败）→ GREEN（装配 + 三处修复）→ REFACTOR
- **新增**：E10-01~06（6）+ E11-01~04（4）+ E12-01~06（6）+ E13-01~04（4）= 20 用例（+27 含相关断言）
- **全量**：110 E2E 全部通过（+27）+ 966 单元/集成测试 + smoke 6/6 + verify 全绿 + 文档治理 + 契约覆盖 127 handlers + 安全不变量 6/6 + UUID 泄漏 7/7

## 7. Git 证据

（收尾提交后填写：分支 agent/T-M3-007-e2e-chat → feat/test/docs 提交号 → master ff-only 合并 → origin/master 推送）

## 8. 未解决事项/下一步约束

- E2E-01~13 全链回归 + 安全不变量最终校验 → T-M3-008（P0，§7.5 全局执行顺序表第 18 行，前置 T-M1-010 + T-M2-009 + T-M3-007）
- backup_* 目标 Tab 决策 → T-M3-008 最终评估
- 三处承载层修复（幂等/目录/计数）已入本任务，无后续依赖