# T-M4-019 实施记录

**任务 ID**：T-M4-019
**标题**：备份恢复面板 RPC 接线 + TabBar 入口
**日期**：2026-08-11
**分支**：agent/T-M4-019-backup-restore-rpc
**基线**：master=origin/master=586d1f0（T-M4-018 Git 收口事实核验）
**状态**：本地实施与验收完成；Git 收口待用户单独授权（AGENTS.md §8.4 未满足前不报告完成）

---

## 1. 任务裁决与范围

- **用户选择**：2026-08-11 明确选择 T-M4-019~021 序列继续（"下一个任务 T-M4-019~021 开始了 先做prompt 做plan"；T-M4-019 prompt 资产已就绪 v0.1.96）；计划 `.plan/T-M4-019-backup-restore-rpc.md` 经用户"批准"后实施，四项设计决策按推荐方案 A 生效：
  1. BackupPanel 可达入口 = TabBar 新增"备份"Tab（第 10 个，09-UI v0.1.5 §4.1 同步）；
  2. 恢复冲突策略 = 恢复前显式选择（覆盖/新建，默认新建）；
  3. 历史行"恢复"按钮 = 触发 zip 选择恢复流程；
  4. 备份目标目录选择 = dialog directory capability（shell 层）。
- **范围**：BackupPanel 静态壳 → 受控 RPC 接线（backup.course/allCourses/restore/list/configureSchedule/listSchedules/toggleSchedule + backup.progress 订阅）；TabBar 备份入口；dialog directory capability；agent-host backup.progress 生产推送。
- **非目标**：不新增/改 API/handler/schema（contract 127/127 不变）；不实现运行态 cron 守护进程（T-M2-005 偏差已记录）；不做归档触发备份 UI；不执行真实用户资料备份/恢复；不启动 T-M4-020/021。

## 2. 实际交付

| 文件 | 变更 |
|---|---|
| `src/renderer/components/BackupPanel.tsx`（重写） | 静态壳 → 受控：手动备份（backup.course/allCourses + 目录选择 seam）+ 调度（configureSchedule/listSchedules/toggleSchedule）+ 历史（backup.list，不显示 targetPath）+ 恢复（backup.restore + 冲突策略 radio + RestoreResult 摘要）+ backup.progress 订阅 + 竞态/卸载/净化 + 静态兼容 props |
| `src/renderer/tabs.ts` | TABS 新增 backup Tab（第 10 个，"💾 备份"） |
| `src/renderer/components/AppShell.tsx` | renderTab case "backup" 传 semesterId/courseId |
| `src/contract/types.ts` | `DialogOptions.directory?: boolean`（shell capability，非 RPC 契约） |
| `src/main/ipc.ts` | showDesktopDialog 支持 directory 模式（openDirectory → 返回目录路径，stat.isDirectory 校验） |
| `src/agent-host/index.ts` | 生产接入 `Streams["backup.progress"]`：`BackupContext({ emit })` → `server.pushEvent("backup.progress", event)`（对齐 T-M4-018 tts.state 先例） |
| `tests/integration/t-m4-019-backup-rpc.test.ts`（新增） | C-RED-01~11（手动备份/目录/备份全部/调度/历史/恢复/冲突策略/progress/竞态卸载/错误净化/TabBar） |
| `tests/unit/renderer-backup-panel.test.ts`（更新） | 适配新组件（备份目录选择入口断言更新） |
| `tests/unit/renderer-layout.test.ts`（更新） | Tab 断言 9→10（含 backup，09-UI v0.1.5 同步） |
| `tests/e2e/t-m4-019-backup-renderer.test.ts`（新增） | 真实 Electron：备份 Tab → 备份此课程（真实 zip 产出）→ 轮询 zip → 恢复（integrity_check）+ 隐私断言 |
| `docs/09-使用者介面-UI-Design.md` | v0.1.5 §4.1 增补"备份"Tab（决策 1A） |

## 3. 偏差

- **无范围偏差**。计划内补充说明：`renderer-layout.test.ts` 既有 9-Tab 断言随 TabBar 入口同步更新为 10-Tab（决策 1A 的直接后果，属既有测试适配而非范围外变更）。

## 4. 问题及根因

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | C-RED-01/06 断言 calls[0] 失败 | mount 效果先发 backup.list/listSchedules，calls[0] 非目标方法 | 测试改用 `calls.filter(c => c.method === ...)` 定位目标调用 |
| 2 | C-RED-03 "按钮不存在: 备份中…" | 立即 resolve 的 mock 使 busy 状态瞬时消失 | 改用 deferred mock 测 in-flight |
| 3 | type-check TS2322（toggleSchedule effectiveSemesterId 可空） | toggleSchedule 缺 semesterId 守卫 | 补 `if (!rpc \|\| !effectiveSemesterId) return` |
| 4 | 全量测试 2 项失败（renderer-layout 9 Tab） | TabBar 新增备份 Tab 与既有 9-Tab 断言冲突 | 既有测试同步更新为 10 Tab（决策 1A 后果） |
| 5 | 独立审查发现备份完成后进度残留 | progress 状态在备份完成后未清除，与历史"✅ 完成"矛盾 | backupCourse/backupAll 成功后 setProgress(undefined) |

## 5. 关键决定及依据

| 决策 | 依据 |
|---|---|
| 四项设计决策按方案 A（TabBar 入口/冲突策略/历史恢复按钮/directory capability） | 用户 2026-08-11"批准"计划（计划 §11） |
| TabBar 新增备份 Tab（10th） | T-M4-019 任务标题"TabBar 入口" + 04-Todo §6.6"BackupPanel 可达"；09-UI v0.1.5 同步（治理基线修订登记） |
| 恢复冲突显式选择（默认新建） | host conflictResolution 参数语义（zip-restorer "none" 实际落 create_new） |
| targetPath/zipPath 只存 ref 不进 DOM | AGENTS.md §9.3 + 09-UI §11.1（E2E 隐私断言覆盖） |
| backup.progress 生产推送经 BackupContext.emit → server.pushEvent | 06-API §4 + T-M4-018 tts.state 先例；contract 127/127 不变 |

## 6. 测试证据

- **RED**：初次定向运行 4 项失败（calls 定位/busy 瞬时/单件标签），证据 `H:\pi-studybuddy-tmp\runs\T-M4-019\red\red-initial-failure.log`。
- **GREEN**：定向 integration 11/11（C-RED-01~11）+ unit 17/17 = 28/28。
- **真实 Electron E2E**：`t-m4-019-backup-renderer.test.ts` 1/1（备份 Tab 可达 → 目录 seam → 备份此课程真实 zip 产出 → 轮询 zip → 恢复 integrity_check；DOM 无完整 UUID/Windows/POSIX/file URI/错误栈/密钥）。
- **全量质量门**（Node24.14.0/pnpm11.20.0）：
  - `pnpm test`：unit/integration/security 118 files/1130 tests（基线 117/1119，+11）全绿；
  - `pnpm test:e2e`：真实 Electron E2E 24 files/130 tests（基线 23/129，+1）全绿；
  - `pnpm smoke`：6/6；`check-contract-coverage`：127 Api handlers + 8 PiBridge + 35 tools；
  - `check-desktop-security`：6/6；`check-uuid-leak`：7/7；
  - `check-docs-governance`：OK；`git diff --check`：通过；
  - `pnpm verify -- --stage=full`：全部通过（执行 8，跳过 2）。
- 测试运行数据隔离：`H:\pi-studybuddy-tmp\runs\T-M4-019\`（含 e2e-renderer/case-01 备份 zip），未写 `%LOCALAPPDATA%\PiStudyBuddy`，未连真实外部服务。

## 7. Git 证据

- 隔离分支：`agent/T-M4-019-backup-restore-rpc`（自 `master=586d1f0` 切出）。
- 变更文件：新增（t-m4-019-backup-rpc.test.ts / t-m4-019-backup-renderer.test.ts / .plan/T-M4-019-backup-restore-rpc.md / .record/T-M4-019-实施记录.md）+ 修改（BackupPanel.tsx、tabs.ts、AppShell.tsx、contract/types.ts、main/ipc.ts、agent-host/index.ts、renderer-backup-panel.test.ts、renderer-layout.test.ts、docs/04、docs/00-索引、docs/09-UI、.plan/00-当前任务.md）。
- 未执行 commit / merge / push（用户未授权 Git 收口）；工作区既有用户 dirty 文件（未跟踪 pi-session html）未混入。

## 8. 未解决事项/下一步约束

- **Git 收口待用户单独授权**：功能提交 + 治理登记提交 → master 快进合并 → Node24 master 完整 `verify --stage=full` 复验 → origin/master 推送核验。
- **运行态 cron 守护进程**：不在本轮（T-M2-005 偏差已记录）；调度仅为配置表 + 启停。
- **归档触发备份 UI**（07-WF §5.4 pre/post_archive）：不在本轮 renderer 范围，留待后续按需裁决。
- **T-M4-020/021**：E2E 全链回归 / M4 收官验收，各在前序完成后按 §4.4 单独做计划；不自动启动。
